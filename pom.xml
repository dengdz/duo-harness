<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>dev.duo</groupId>
  <artifactId>duo-harness</artifactId>
  <version>0.15.0</version>
  <packaging>pom</packaging>

  <description>Java 插件化 AI agent harness：内核为自研轻量插件容器，能力以插件组装</description>

  <modules>
    <module>duo-harness-core</module>
    <module>duo-harness-attachment</module>
    <module>duo-harness-tools</module>
    <module>duo-harness-hooks</module>
    <module>duo-harness-mcp</module>
    <module>duo-harness-llm</module>
    <module>duo-harness-session</module>
    <module>duo-harness-agent</module>
    <module>duo-harness-web</module>
    <module>duo-harness-cli</module>
    <module>duo-harness-example</module>
  </modules>

  <properties>
    <maven.compiler.release>21</maven.compiler.release>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <jackson.version>2.21.2</jackson.version>
    <junit.version>6.0.3</junit.version>
    <slf4j.version>2.0.17</slf4j.version>
    <mcp-sdk.version>0.18.1</mcp-sdk.version>
    <!-- 跟随 mcp-json-jackson2:0.18.1 的 compile 依赖要求（2.0.0）；1.5.0 缺 SDK 所需类 -->
    <networknt.version>2.0.0</networknt.version>
    <!-- web 工具族 HTML 解析清洗（M20，ADR-0021；单 jar 零传递依赖，新依赖已获 grill 同意） -->
    <jsoup.version>1.18.3</jsoup.version>
    <!-- 附件规范化/缩放（M21，ADR-0022；纯 Java 零 native，新依赖已获 grill 同意 A1） -->
    <twelvemonkeys.version>3.10.1</twelvemonkeys.version>
    <thumbnailator.version>0.4.20</thumbnailator.version>
    <exec-maven-plugin.version>3.1.0</exec-maven-plugin.version>
    <!-- reactor 模式：exec:java 仅在 example 模块启用（其余模块跳过） -->
    <exec.skip>true</exec.skip>
    <!-- 演示入口可切换：-Dexec.mainClass=... 选本模块的其他 Demo（默认 M1 的 DemoMain） -->
    <exec.mainClass>dev.duo.harness.example.DemoMain</exec.mainClass>
  </properties>

  <dependencyManagement>
    <dependencies>
      <!-- 版本集中在根 pom，子模块不写版本号（duo-project-structure 规范） -->
      <dependency>
        <groupId>com.fasterxml.jackson</groupId>
        <artifactId>jackson-bom</artifactId>
        <version>${jackson.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
      <dependency>
        <groupId>org.junit.jupiter</groupId>
        <artifactId>junit-jupiter</artifactId>
        <version>${junit.version}</version>
        <scope>test</scope>
      </dependency>
      <dependency>
        <groupId>org.slf4j</groupId>
        <artifactId>slf4j-api</artifactId>
        <version>${slf4j.version}</version>
      </dependency>
      <dependency>
        <groupId>io.modelcontextprotocol.sdk</groupId>
        <artifactId>mcp</artifactId>
        <version>${mcp-sdk.version}</version>
      </dependency>
      <!-- 工具输出契约校验（工单 04；票据注明 nexus 已核实、新依赖已获同意） -->
      <dependency>
        <groupId>com.networknt</groupId>
        <artifactId>json-schema-validator</artifactId>
        <version>${networknt.version}</version>
      </dependency>
      <!-- web 工具族 HTML 解析清洗（M20，ADR-0021） -->
      <dependency>
        <groupId>org.jsoup</groupId>
        <artifactId>jsoup</artifactId>
        <version>${jsoup.version}</version>
      </dependency>
      <!-- 附件规范化/缩放（M21，ADR-0022） -->
      <dependency>
        <groupId>com.twelvemonkeys.imageio</groupId>
        <artifactId>imageio-jpeg</artifactId>
        <version>${twelvemonkeys.version}</version>
      </dependency>
      <dependency>
        <groupId>com.twelvemonkeys.imageio</groupId>
        <artifactId>imageio-webp</artifactId>
        <version>${twelvemonkeys.version}</version>
      </dependency>
      <dependency>
        <groupId>net.coobird</groupId>
        <artifactId>thumbnailator</artifactId>
        <version>${thumbnailator.version}</version>
      </dependency>
      <dependency>
        <groupId>org.slf4j</groupId>
        <artifactId>slf4j-simple</artifactId>
        <version>${slf4j.version}</version>
        <scope>test</scope>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <build>
    <pluginManagement>
      <plugins>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-surefire-plugin</artifactId>
          <version>3.5.2</version>
          <configuration>
            <!-- 用例内含子进程（MCP 夹具）：卡死要以失败+线程转储呈现，而不是无限挂起 -->
            <forkedProcessTimeoutInSeconds>240</forkedProcessTimeoutInSeconds>
            <environmentVariables>
              <!-- 测试隔离：fork JVM 的 DuoHome 指向本模块构建目录——不触碰真实
                   ~/.duo（用户数据与在跑实例的会话独占锁）；LLM 三项以假值兜底，
                   装配测试只需"配置可得"，不会真实发请求 -->
              <DUO_HOME>${project.build.directory}/test-duo-home</DUO_HOME>
              <DUO_LLM_BASE_URL>http://127.0.0.1:9/v1</DUO_LLM_BASE_URL>
              <DUO_LLM_API_KEY>test-key</DUO_LLM_API_KEY>
              <DUO_LLM_MODEL>test-model</DUO_LLM_MODEL>
            </environmentVariables>
          </configuration>
        </plugin>
        <plugin>
          <groupId>org.codehaus.mojo</groupId>
          <artifactId>exec-maven-plugin</artifactId>
          <version>${exec-maven-plugin.version}</version>
          <configuration>
            <!-- 占位：3.1.0 的 required 参数校验先于 skip，无值即失败；
                 非 example 模块经 exec.skip=true 跳过实际执行 -->
            <mainClass>${exec.mainClass}</mainClass>
            <skip>${exec.skip}</skip>
          </configuration>
        </plugin>
      </plugins>
    </pluginManagement>
  </build>
</project>
