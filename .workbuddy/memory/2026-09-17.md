## 2026-09-17 项目全面质量评审（砚）

- 对 duo-harness 0.10.0 分支（640e9c6）做全量质量评审，报告落盘 `.scratch/quality-review-20260917.md`。
- 实证：`mvn test`（~/.m2/bin/mvn，注意 mvn 不在会话 PATH）457 用例全绿（1 跳过=性能基准）。
- 结论：加权总分 8.0/10。代码质量 8.43（错误处理/并发纪律突出）、文档 7.5（README 落后 8 个版本+缺 cli 模块行）、工程配置 6.5（无 Java CI 门禁、无 JaCoCo/静态分析/mvnw）、结构可维护性 9.0。
- Top 严重问题：①无 Java CI（.github/workflows 只有 docs.yml）②WebFace 无 Host/Origin 校验（loopback 防不了浏览器 CSRF/DNS rebinding 打 /api/message、/api/answer）。
- 其他发现：47 处 System.out（agent/web/cli/llm/session 无 slf4j 依赖）；LLM 流读无 idle 超时；WebFace 777 行巨类+响应样板 15+ 处；/api/answer 用"拒绝"魔法字符串当协议；Session.java 两处 javadoc 错位；热路径 new ObjectMapper()。
- BSD grep 坑（两次）：`\|` 交替语法在 macOS grep 不生效，用多个 -e 参数代替。
