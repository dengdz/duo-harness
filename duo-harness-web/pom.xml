<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2000/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>dev.duo</groupId>
    <artifactId>duo-harness</artifactId>
    <version>0.10.0</version>
  </parent>

  <artifactId>duo-harness-web</artifactId>
  <description>Web 双面呈现域：HTTP 服务 + 会话事件流（SSE）+ 对话/状态界面 + HITL Web answerer——机制不变换呈现位（ADR-0008）</description>

  <dependencies>
    <dependency>
      <groupId>dev.duo</groupId>
      <artifactId>duo-harness-core</artifactId>
      <version>${project.version}</version>
    </dependency>
    <dependency>
      <groupId>dev.duo</groupId>
      <artifactId>duo-harness-tools</artifactId>
      <version>${project.version}</version>
    </dependency>
    <dependency>
      <groupId>dev.duo</groupId>
      <artifactId>duo-harness-session</artifactId>
      <version>${project.version}</version>
    </dependency>
    <dependency>
      <groupId>dev.duo</groupId>
      <artifactId>duo-harness-agent</artifactId>
      <version>${project.version}</version>
    </dependency>
    <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter</artifactId>
    </dependency>
  </dependencies>
</project>
