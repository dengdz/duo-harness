/**
 * duo-harness-agent 模块根包：agent 循环契约——ChatAgent 对话服务、
 * AgentListener 过程回调、AgentReply 任务结果、ToolInvocation 工具调用
 * 记录。契约平铺在本包（小域平铺形态），实现（循环）在
 * {@code agent.internal}，不对外暴露。
 */
package dev.duo.harness.agent;
