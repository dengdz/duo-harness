/**
 * 子代理域（M15，ADR-0015）：模板制装配（config 定义工具清单 + 可选专属提示，
 * 模型只点名模板）与五件工具的条件注册（spawn/fork + 控制面三件，未配置模板的
 * 部署零变化）。子代理可用集经强制过滤收窄——交互与控制面工具不进子模板
 * （治理底线不可绕过，spawn/fork 不进即禁嵌套，深度恒为 1）。
 * 子代理运行期（后端、子会话生命周期、fork 播种）自工单 03 起在本包展开。
 */
package dev.duo.harness.agent.subagent;
