# mattpocock-skills × duo 技能机制对比报告（C1-02）

- **锚点**：Matt 侧 = 插件 mattpocock-skills 1.2.3 插件缓存（同 [机制清单](机制清单.md)，行号锚点全部经过一手核实）；duo 侧 = 本仓库 `.agents/skills/duo-*` 15 个 + `.duo/skills/release-notes`，锚点为 main（98ce7e8 立项 / 2a202a8 C1-01 时代工作树），行号出自行内一手通读。
- **对照系**：机制清单的 15 个机制主题（下文以 M1–M15 指代，编号同机制清单 §0 总表）。
- **地位**：C1 分析段第二份产物（工单 C1-02）。§2 逐技能 gap 是改写组（工单 05–11）的直接输入；§4 是对 Downloads 两份轻量分析的三态重验终审；[讲义](讲义.md) 随本报告交付（4 课机制主题）。
- **方法声明**：16 个技能全部一手通读（15 duo-* 共 1458 行 + release-notes 14 行）；每条 gap 带双方 `文件:行号`；背景线索重验逐条给采纳/修正/推翻三态。

## 1. 量化画像（实测）

| 维度 | mattpocock-skills（正式桶） | duo 侧（15 duo-*） |
|---|---|---|
| 技能数 / 行数 | 25 个，7–140 行，中位 71 | 15 个，26–328 行，中位 **68**，合计 1458 行 |
| 最长技能 | teach 140 | duo-skill-evolution **328**（次长 duo-prose-standard 141） |
| `disable-model-invocation` | 14/25（56%，user-invoked 档） | **0/15**（全模型可达） |
| `argument-hint` | 2/25（handoff、teach） | **0/15** |
| description 长度 | user 档均值 22 词（10–42）；model 档均值 31 词（11–68） | 均值 **约 160 字**（86–221，中位约 166） |
| description 常驻成本比 | —（基准） | ≈ Matt model 档的 **4–5 倍**、user 档的 6 倍上下（中文 1 字≈1 token、英文 1 词≈1.3 token 粗算） |
| 跨技能引用形态 | operative 位置一律显式 Skill tool 调用 | 全部 markdown 相对路径链接或"用法以该技能为准"（含 7 处 `../../../` 三级外链：tracker 4 处 + research 3 处） |
| 变更历史位置 | 零进 SKILL.md（changeset/CHANGELOG 承担） | duo-code-review 尾部 6 段（:71–110） |
| 每技能 experience.md 回流 | 无（changeset 人工维护） | **14/15 有**（duo-java-review 用 `reference/` 单数目录装规则文件）——duo 独有资产 |
| 状态机 | triage 角色状态机、wayfinder claim | bug-ledger 档案状态机、工单 Status、research 锚点三态——**两侧打平，duo 自发长出** |

## 2. 逐技能 gap 对照（16 个）

每条格式：**命中**（已具备的机制）/ **缺失**（机制清单要求而未见）/ **改写方向**（工单 05–11 的候选输入）。行号为当前工作树实测。

### 2.1 duo-tracker（26 行）
- **命中**：M8 单一事实源（"不在任何 skill 里留副本"，:25）；M13 表格分支（场景→做法表，:15–21）。
- **缺失**：M1（description 身份开头 + 8 种触发说法塞一段，:3）；M2（`../../../docs/agents/` 三级外链 ×4，:10–11,:19–20）；M4（"提示用户显式调用 `/to-spec`" 是转发提示非阻塞步骤，:17–18）。
- **改写方向**：description 领先词前置剥身份句；配置引用改"读 `docs/agents/issue-tracker.md` 后按其执行"式显式指令；触发分支按概念收敛。

### 2.2 duo-workflow（62 行）
- **命中**：M4 部分（提交前核对清单三查，:49–56）；M12 雏形（"一单一会话，会话间 /handoff 交接"，:18）；定义约束式表达（"agent 测试全绿 ≠ 验收"，:43）；路由同步治理（"路由表与 description 同一 diff 改两处"，:62——duo 独有，Matt 侧对应物是 invocation.md:10 的双 harness 同步）。
- **缺失**：M1（"模型不可自动触发"是正文散文声明——约束力最弱档，:16）；M2（验收/关卡/设施三处相对路径链接，:43,:47,:60）；M3（L1 流程七节点箭头链零完成判据，:14）；M10（三级判定无"判定不了级怎么办"的兜底步）。
- **改写方向**：箭头链每节点补一行可判定判据（L1"拆工单"= issues/ 下每张含三要素）；分流的兜底方案按工单 03/05 的探测结论落；引用改显式调用。

### 2.3 duo-acceptance（58 行）——duo 判据写法正例标杆
- **命中**：M3 ✓✓（四步各带"完成判据："，:18,:22,:24,:30——全库判据密度最高）；M4 ✓（"给出命令与对照表，等结果""用户实测现象优先于任何测试绿灯"，:28）；M5 雏形（"三件套"）；证据先行（"静默绿条不算可验收的测试输出"，:21）；**no-op 纪律的自觉版**（"「详细」由示例锁定，不以形容词为准"，:44——与 writing-for-agents:81 的 weak-word 判定同源）。
- **缺失**：M1（description 181 字）；M6 一处残留（"不得以'测试是绿的'反驳"，:28——禁令但带强理由，可保留为硬护栏）。
- **改写方向**：判据写法**作为全库范本推广**（工单 04 规范的正例来源），本体只需 description 瘦身。

### 2.4 duo-trim-cot-leakage（58 行）——正向表述正例（背景线索未识别的增量）
- **命中**：M3 ✓（"唯一判据"可判定，:23）；M6 ✓✓（"不是泄漏的（保留规则）按字面执行"，:39–49——用"什么要保留"的正向清单替代"不要误删"禁令，是 M6 理论的全库最佳实践）；M7 ✓（检索模式集/校准示例外置 references，:10–11）；M13（判例，:19）。
- **缺失**：M1（description 110 字，全库较短的正面例子）。
- **改写方向**：保留规则节收录为工单 04 规范 M6 正例；本体改动最小。

### 2.5 duo-doc-standards（67 行）
- **命中**：M3 ✓✓（导航对账"故固化为命令而非口头纪律"，:50–56——证据先行判据的成熟实践；"从源码重新数，不信文档转述"，:48）；M8 ✓（"同一行为说明只留一个家"，:39,:47）。
- **缺失**：M2（operative 引用 duo-trim-cot-leakage 用相对链接，:8,:46）；description 带跨技能分工说明（"质量和写作规范由 duo-prose-standard 负责"，:3——分工属正文，放 description 是常驻税）。
- **改写方向**：分工句迁正文；对账命令节作为"证据先行"正例收录工单 04。

### 2.6 duo-comprehension（68 行）
- **命中**：M3 ✓✓（90 分硬闸门，:36；"题目必有唯一正确答案；干扰项来自真实误区"，:29）；M13（产物模板，:43–52；字面示例锁"好题"，:58–68）；inline 落盘纪律（"每课讲完考核后立即追加"，:41——与 domain-modeling 的 update-inline 同构）。
- **缺失**：M4（"课程清单先给用户过目确认"嵌在第一步第 3 子项——括注化检查点的代表实证，:14）；M1（description 203 字）。
- **改写方向**：清单确认升格为独立阻塞步骤（出示清单 → Wait → 增删 → 开讲）；description 瘦身。

### 2.7 duo-project-structure（68 行）
- **命中**：M3 ✓（"同一 diff 完成，缺一即拆分未做完"——demand 式判据，:32；"写不出一句话职责说明边界没想清楚"，:56）；M13 ✓✓（判例节，:62–68——具体 case 锚定行为，Matt 侧无此形态的差异化资产）；分级处置（四级结论，:51）。
- **缺失**：M1（description 内含禁令"禁止在一个目录里堆一大堆文件"——常驻税与否定双重问题，:3）；M6 半命中（"禁止按技术层横切"紧跟正向理由"让改一个功能要跨五个目录"，:20——禁令+理由的可接受形态）。
- **改写方向**：判例节保留并作为工单 04 的"判据+判例"正例；description 剥禁令。

### 2.8 duo-pre-push-checks（77 行）
- **命中**：M13 ✓✓（变更类型→必跑证据表，:28–36——writing-docs.md:78 "分支进表格"的高分实现）；M10 ✓（"停下来修复或说明阻塞原因，不要先推了赌 CI"，:62；失败举证"记录确切命令、失败测试"，:63）；M3 ✓（"报告时如实区分：已验证通过 / 已跳过及原因 / 无法本地验证"，:77——诚实分级判据）；M5（"最窄测试"领先词，:8）；M8（"不要重复跑刚通过的检查"——no-op 防御带理由，:37）。
- **缺失**：M1（description 94 字次短；触发短语已前置，尾部仍罗列口语变体——概念收敛未达标）。
- **改写方向**：证据表收录为工单 04 表格分支正例；description 触发词前置。

### 2.9 duo-bug-ledger（87 行）
- **命中**：M15 ✓（档案状态机 reported→…→done，:27）；M3 部分（防复发三问逐条回答，:66；"查同族：grep 台账，同根因第二次出现 → 升级为结构化防线"，:72——判据式升级规则）；症状原样引用（证据先行，:19,:58）；M5 雏形（"建档""防复发三问"）。
- **缺失**：M1（description 约 220 字全库最长：7 种触发说法 + 与 diagnosing-bugs 的分工说明全塞进常驻上下文，:3）；M6 ✗✗（"反模式（不要做）"节四条禁令——否定作组织单元的最大实证，:82–87）；M8 部分（与 diagnosing-bugs 分工在 description 与正文 :12 **两处重复**——duplication 实证）。
- **改写方向**：反模式节改正向完成判据（24 版 S4 方案经重验成立）；分工说明只留正文一处；description 触发按概念收敛（"任何'现实与预期不符'的陈述"）。

### 2.10 duo-release-workflow（118 行）
- **命中**：M13 ✓（需求分级表，:22–26；审查方式表，:53–58）；M4（"用户确认通过后才可合并"——阻塞判据，:66）；M10 ✓（引导态守卫"不硬套流程"，:12）；**M8 ✓✓（"红线的唯一权威是根 AGENTS.md——本流程不抄录副本"，:115——全库最自觉的单一事实源实践）**；推送后验证远端一致（:117）。
- **缺失**：M1；M3 部分（八步流程仅 :60 一处判据"全部修复并验证后才进入下一步"，各步无 Done when）。
- **改写方向**：红线不抄录原则收录工单 04 正例；各步补完成判据；description 瘦身。

### 2.11 duo-java-review（134 行）
- **命中**：M7 ✓✓✓（L0/L1/L2 渐进披露——全库最完整的 information hierarchy 实现，:24–51；加载决策逻辑可判定，:39–42）；M13（L1 加载表 + 速查索引表，:32–37,:96–112）；M3 部分（BLOCKER/CRITICAL/MAJOR 分级定义表，:16–20）。
- **缺失**：M1（description 153 字）；M9 对照点（"渐进式披露"，:24——与 Matt "progressive disclosure" 同概念异词，词汇治理的活案例）。
- **改写方向**：结构不动（改写范围限定入口/工作流/措辞，spec Out of Scope）；术语向工单 04 规范的统一词表靠拢。

### 2.12 duo-prose-standard（141 行）——duo 侧最接近元文档的存在
- **命中**：M3 ✓（"只有当每个事实分句都存活、且改后确实更清晰时，才删"——可判定边界，:34）；M6 ✓（正向表述为主，情态词保留规则成文，:28–34）；M8 ✓（"一个解释只有一个家"，:36）；M7 ✓（各位置必需覆盖=层级表，:42–51）；"机制名称保持可搜索"（:53）。
- **缺失**：定位差异——它管 **prose 质量**，不覆盖**技能机制层**（调用权/判据/领先词/剪枝纪律作为写作规范）；这正是 duo 无 writing-for-agents 等价物的空档（M0 层缺失，背景线索 §0.1 结论重验成立）。
- **改写方向**：工单 04《技能写作规范》以它为文风基座、机制清单为内容来源，避免两部规范并行打架。

### 2.13 duo-code-review（110 行）——治理最强、指针最弱、沉积最重的三重样本
- **命中**：M11 ✓✓（brief 填空模板六要素"禁止即兴手写"，:20–26——Matt "pasted in full" 纪律的**形式化超集**）；M3 ✓✓（收口硬判据"四轴齐全且均落盘"，:33）；豁免治理（双处记档 + 用户显式确认 + 每次独立决定，:33——Matt 侧无此审计链，duo 独有且更严）；M13（报告模板，:39–54）。
- **缺失**：M2 ✗✗（"调用 mattpocock `code-review` 技能的 Standards 子代理，**用法以该技能为准**"，:15；"用法以 open-code-review-delegate 技能为准"，:17——弱指针全库最大实证：目标材料不在上下文，凭记忆执行）；M6 ✗✗（豁免条款三重否定"现场裁量豁免无效，沿袭先前工单的豁免同样无效"，:33）；M8 ✗✗（变更历史 6 段占尾部 :71–110≈40 行——sediment 活体，且其变更历史自诊断"描述性条款无机械校验点跨会话必然衰减"（:94）与 M3 理论互为印证）。
- **改写方向**：跨技能引用改显式调用 + brief 随附材料原文；豁免条款改正向（"出示两处记档路径 + 本轮用户确认记录，缺一即未豁免"）；变更历史迁 experience.md。

### 2.14 duo-skill-evolution（328 行）——sediment 最大活体
- **命中**：M3 部分（快速判断流程可判定，:58–69）；experience.md 回流机制本体（duo 独有资产）。
- **缺失**：M8 ✗✗✗（328 行全库最长：第六节两个完整示例（:230–281）与 §3.1/§5 输出模板（:66–69,:202–224）大面积换皮重复；输出模板硬编码"达，"人格前缀 **5 处**（:68,:202,:208,:250,:280）——用户全局 AGENTS.md 规则写死进技能模板，属分层耦合，规则一改全库 rot）；M6（第七节"不要过度进化/文档化/修订"三连否定，:289–291）。
- **改写方向**：24 版 T7 经重验成立——328→≤120 行：删第六节、4.1/4.2 合并、"达，"剥离（模板输出交由模型按全局规则套用）、三连否定合并为一句正向进化判据（"下次少犯一个已记录的错误"）。

### 2.15 duo-research（56 行）
- **命中**：M3 ✓（"已有三件套且锚点一致 → 直接复用，本次结束"——机械判据，:34）；M10 ✓（"失败时报告原因，不擅自 rebase/merge"，:32）；M13（三件套固定结构表，:44–48）；M15（锚点三态：当前/源已更新/历史存档）；experience.md 回流 ✓。
- **缺失**：M1；M2（`../../../docs/research/index.md` 等三级外链 ×3，:8,:27,:33）。
- **改写方向**：引用改仓库根相对路径约定；其余结构保留。

### 2.16 release-notes（14 行，演示技能，改写范围外）
- **命中**：M3 ✓（"CHANGELOG 里没有的变更不写入"，:14）；M8 ✓（"唯一事实来源是 CHANGELOG.md"，:9）；M4（"有疑问先向用户确认"，:14）。
- **结论**：14 行做完一件事带判据——全库最"Matt"形态的技能，反向印证体量与判据密度无关、与剪枝纪律有关。仅作对照，不进工单 05–11。

## 3. 机制覆盖矩阵（15 机制 × duo 侧）

图例：✓ 命中（有成熟实践）/ ◐ 半命中（有意识无制度或个别正例）/ ✗ 缺失。

| 机制 | 覆盖 | duo 侧证据与缺口 |
|---|---|---|
| M1 调用权分流 | ✗ | 0/15 分流字段；流程类技能全模型可达；"模型不可自动触发"停留在正文散文（workflow:16）。约束力排序（frontmatter > 系统红线 > Stop hook > 路由表 > 正文散文）里 duo 押后四档 |
| M2 显式工具调用 | ✗ | operative 引用全部弱指针：相对路径（workflow:43 等）、"用法以该技能为准"（code-review:15,:17）、三级外链（tracker:10、research:8）。无一处 "Call the Skill tool" 形态 |
| M3 完成判据 | ◐ | 判据意识全库普遍（13/16 有判据语句），acceptance/comprehension/doc-standards 为正例；但用语不统一（"完成判据/硬判据/收口"），"出示已运行证据"型只在 acceptance:18 与 doc-standards:50，workflow:14 箭头链零判据为最大缺口 |
| M4 用户检查点 | ◐ | 检查点存在（acceptance:28、release-workflow:66、comprehension:14、release-notes:14）但全部括注化或散文化，无独立阻塞步骤 + Wait 语义；非阻塞变体无显式声明 |
| M5 领先词 | ◐ | 雏形丰富：三件套、收口、硬闸门、防复发三问、最窄测试、建档、查同族、引导态守卫；无"招募预训先验"的自觉、无 Avoid 配套、跨技能不统一 |
| M6 否定治理 | ◐ | 负面集中在**最关键位置**：code-review:33 三重否定豁免条款、bug-ledger:82–87 反模式节、skill-evolution:289 三连"不要"；正面范本已存在：trim-cot-leakage:39 保留规则、prose-standard 通篇正向 |
| M7 信息层级 | ◐ | java-review L0/L1/L2 是完整实现；trim-cot-leakage references 外置；但 15 个 description 全部超载（身份+职责+边界+触发+分工塞常驻上下文，均值约 160 字 ≈ Matt model 档 4–5 倍） |
| M8 剪枝纪律 | ◐ | 单一事实源意识强（tracker:25、doc-standards:39、release-workflow:115"红线不抄录"）；sediment 实证两处：code-review 变更历史 6 段、skill-evolution 328 行换皮；"环境即真相源、不复述"无成文纪律 |
| M9 词汇治理 | ✗ | 无 CONTEXT.md 等价物（docs/agents/domain.md 非 Avoid 格式）；同义词漂移实证：工单/ticket/issue 三词跨技能混用、"渐进式披露"（java-review:24）与 Matt "progressive disclosure" 同概念异词 |
| M10 fail-fast | ◐ | 正例：research:32（不擅自 rebase）、release-workflow:12（引导态守卫）、pre-push-checks:62–64（失败停下举证）；缺口：workflow 三级判定无兜底步 |
| M11 子代理纪律 | ✓ | code-review brief 填空模板（:20–26）是 Matt "pasted in full" 的形式化超集——**duo 强项**；唯一缺口是派发目标技能本体不随 brief 加载（回到 M2） |
| M12 路由分层 | ◐ | AGENTS.md 路由表 + workflow 三级分流 = router 形态存在；路由同步红线（workflow:62）为 duo 独有治理；无 context hygiene/smart zone 概念（workflow:18 "一单一会话"是雏形） |
| M13 模板防陈旧 | ✓ | 模板文化与 Matt 打平：bug-ledger 档案模板、comprehension 产物模板、code-review 报告模板、java-review 表格、判例节；缺"防陈旧条款"（禁文件路径/代码片段入册，to-spec:55 对照） |
| M14 格式纪律 | ◐ | CHANGELOG 记账、commit 惯例成熟；YAML 引号纪律无意识（现 15 个 frontmatter 实测合法，无半角": "风险，属未踩雷而非有防线）；无桶分层（草稿/正式）概念 |
| M15 状态机并发 | ✓ | bug-ledger 档案状态机、工单 Status、research 锚点三态自发长出；多会话 claim 原语无（当前单人仓库，非缺口） |

**矩阵结论**：duo 侧 3 项 ✓（M11/M13/M15）、9 项 ◐、3 项 ✗（M1/M2/M9）。三项 ✓ 全是 Matt 侧也认可的形态——**改写不是推倒，是把 9 个 ◐ 升格为 ✓、3 个 ✗ 按机制清单补建**，且 M1/M2 的补法取决于工单 03 的 harness 能力探测。

## 4. 背景线索三态重验（逐条终审）

对 `skill-writing-analysis-20260924.md`（24 版）与 `skill-writing-comparison-20260923.md`（23 版）的核心条目逐条重验：

### 采纳（重验成立，进入改写输入）
1. 五机制缺口论（M1/M2 全缺、M3 判据不统一、M4 括注化、M6 关键位置否定堆砌）——本报告 §3 矩阵逐项证实。
2. 元文档缺失是最高杠杆改进点（24 版 §0.1）——duo-prose-standard 只覆盖 prose 质量不覆盖技能机制层（§2.12），工单 04 定位由此成立。
3. 中文非执行漂移根因论 + 四维补偿（24 版 §5）——重验成立，量化修正见下，课 4 展开论证。
4. 差异化资产清单（experience.md 回流、豁免治理、brief 填空模板、表格化分支、L0/L1/L2、判例节）——全部一手证实（§2 各节），改写不得损失。
5. "达，"是全局 AGENTS.md 人格前缀非残字（24 版对 23 版的修正）——维持；且其**硬编码进技能输出模板属分层耦合**的判断成立（§2.14）。
6. 工单/ticket/issue 三词混用横跨 tracker/workflow/code-review（23 版 §3.4）——一手证实，进工单 04 词汇治理。
7. description 重写须与 AGENTS.md 路由表同 diff + 单独 commit 可整体回滚（24 版 §6 T5/§8⑤）——与 workflow:62 既有路由同步红线吻合，采纳为改写组纪律。

### 修正（方向对、数字或定位偏）
1. **description 长度**：24 版 §5.3 "duo 平均 90–130 字" → 实测均值 **约 160 字**（86–221，中位约 166）；倍率"3~5 倍"精确化为 **Matt model 档的 4–5 倍**（model 档均值 31 词 ≈ 40 token，duo 约 160 字 ≈ 160–190 token）。"砍到 40–60 字"的改写目标相应校准为"user 档 ≤60 字、model 档 ≤80 字"（触发词表占大头时上限放宽）。
2. **行号漂移**：24 版引 duo-workflow:11（箭头链）现位于 :14；duo-code-review 变更历史现为 6 段 :71–110（23 版记 5 段 ~40 行，24 版已修，再证实）。改写组引用以本报告为准。
3. **"达，"出现次数**：24 版记 4 处（68/202/208/250）→ 实测 **5 处**（+280，示例 2 输出）。
4. **duo 中位行数**：24 版"约 70" → 实测 **68**（15 值第 8 位，与 24 版基本一致，仅精确化）；总行数"约 1400" → **1458**。
5. **软约束分层数**：24 版 §4.M1 的三层排序（frontmatter > 系统红线 > 正文散文）漏了 duo 实际存在的另外两层——**workspace Stop hook 兜底**与 **AGENTS.md 路由表**；duo 现状是五层里押后四层（见 §3 M1 行）。
6. **Matt 侧分流幅度**：机制清单 §16.2 已修（22 个 user-invoked 而非 8 个）——本报告 §1 表沿用，改写组对"哪些技能切 user 档"的参照比例从 8/37 修正为正式桶 14/25。

### 推翻（重验不成立）
1. 23 版 §0.5 "输出模板含残字'达，'"——"残字"定性推翻（维持 24 版修正：是人格前缀），但"应从模板剥离"的处方**成立**（分层耦合论）。
2. 23 版 §1 "A 侧最大 168 行（in-progress/pr）"——1.2.3 无此技能（机制清单 §16.2 已推翻），此处终审维持：23 版全部行号锚点不采。
3. 24 版 §2.4 "duo 体量治理靠 experience.md 复盘事后纠偏，有自觉无制度"——**部分推翻**：doc-standards:50–56 的导航对账已"固化为命令而非口头纪律"、release-workflow:115 红线不抄录、pre-push-checks 证据表，均为制度形态；duo 缺的是**剪枝方向的制度**（何时删），不是全部体量治理。

## 5. 对改写组（工单 05–11）的输入清单

1. **正例池**（改写时对齐的 duo 自产范本）：acceptance 判据写法、trim-cot-leakage 保留规则、doc-standards 对账命令、pre-push-checks 证据表、release-workflow 红线不抄录、project-structure 判例节、java-review L0/L1/L2。
2. **反例池**（改写优先级最高的位置）：code-review:15/:17 弱指针、code-review:33 三重否定、workflow:14 箭头链、bug-ledger:82 反模式节、skill-evolution 全文。
3. **待探测决策**（工单 03 裁决框架）：M1 落地形态——harness 支持 `disable-model-invocation` 则按 Matt 两档切分（流程命令档候选：grill-me/grill-with-docs 对应物、workflow 的显式命令表内技能）；不支持则按 spec 记档走"正文点名文件+章节 + AGENTS.md 显式命令表"兜底。
4. **词汇治理首批词条**（工单 04/11）：工单（首选）↔ ticket ↔ issue；收口 ↔ 完成；验收 ↔ 验证 ↔ 确认；渐进式披露（统一 Matt 术语 progressive disclosure 的中文定名）；领先词/上下文指针/证据先行（机制清单新概念的中文定名）。
5. **description 预算**：user 档 ≤60 字、model 档 ≤80 字；领先词前置、身份句删除（正文 H1 已有）、触发按概念收敛不罗列口语变体、中英并列锚点（课 4）。
