# ZCode 模块手册

> 源锚点：`872ad960de7ec172591f7e1952f7849229f94521`（2026-09-21 本地核验，github 离线未 pull；v3.14.0，开源快照测试近乎全剥离）。生成方式：四组源码探查（契约与内核 / adapters / 装配与终端 / 根 packages）汇编，每模块统一模板（用途 / 核心接口 / 关键参数 / 调用方式 / 依赖 / 注意），锚点 = ZCode 仓库内 `文件:行号`，默认值均抄自源码；个别未核实处如实标注。**权限域**（core/src/permission/service.ts、broker、规则持久化）另有专册 [沙箱与权限.md](沙箱与权限.md)，本手册只留指针。配套：[总览.md](总览.md)。

## 一、契约与内核（apps/zcode-cli/packages）

### contracts（`@zcode/contracts`）
- **用途**：全仓契约层——ports、session 事件、工具 schema、config、hooks/trust schema，供 core 与 bootstrap 共同依赖。
- **核心接口**：25 个 port（`interfaces/*.port.ts`）：AutomationPort:43、BrowserControlPort:537、ContextSourcePort:92、DynamicWorkflowRunPort:600、ExecutionPort:278（shell+后台 Bash）、FileSystemPort:280（带 revision）、HttpClientPort:89（含 egress 信息）、ImageProcessorPort:78、McpPort:290、ModelCatalogPort:29、OffPeakPort:47、PdfDocumentPort:41、PermissionBrokerPort:95、SessionMailboxPort:12、SessionStorePort:1089（另有 UsageStorePort:1070/LocalSettingStorePort:1079）、SessionEventStorePort:57、SubagentPort:113、ToolArtifactStorePort:106、WorkflowPort:36 等。
- **SessionEventType 词表**：`events/session.events.ts:73-174`，60+ 键值（session_created…error），覆盖 turn 生命周期、steer/queue v4 控制（turn_steer_*:89-108）、模型流（model_*:115-129）、工具（tool_call_*:130-135）、后台（background_task_*:136-138）、dwf 进度、权限/hook/compact/rewind/subagent（144-173）。
- **工具 schema 声明**：`tools/contract.ts`：`ToolContractDeclaration:168`（inputSchema/outputSchema/strict/permission:182/resultBudget/timeout/cancellation/trace）；`ToolPermissionSpec:87`；超时 `ToolTimeoutPolicy:153`（timed: defaultMs/maxMs/cleanupGraceMs:141-151）；`isWorkspaceMutatingToolCall:30`（缺 scope 保守算写）。
- **ConfigKey**：`config/index.ts:12-80`（30+ 键）；`DefaultRuntimeConfig:290-359`：modelStream.idleTimeoutMs=600_000:284、permission.mode="build"、storage.dir="~/.zcode"、network.timeout=180000、skills.metadataBudget=20_000、toolConcurrency.maxConcurrency=10、hooks{enabled:false,maxOutputBytes:32768,timeoutMs:60000}、ui.locale="en-US"。Scope 优先级 System0→Cli50:168-175。
- **注意**：session.events.ts 整文件 eslint-disable max-lines（:2）防拆分漂移；事件注释即语义权威（如 :139-143 dwf 事件进父会话）。zcode-protocol-v4 的投影实现在 bootstrap（见下），shared 只放 schema。

### core/runtime（`core/src/runtime`）
- **用途**：AgentRuntime——会话级编排核心：turn 循环、工具调度、队列/steer、权限、compact、后台任务、fork/rewind。
- **核心接口**：`AgentRuntime` class `agent-runtime.ts:132`；接口合并 `AgentRuntime` :337-662（executeTurn:572、scheduleTools:577、executeTools:578、steerTurn:424、stopActiveForegroundExecution:568、admitPrompt:388、rewindConversationToMessage:628）；方法经 `installAgentRuntimeMethods`（:664，methods/index.ts）挂 prototype。
- **deps 注入面**：`AgentRuntimeDeps`（`types.ts:309-378`）必填仅 eventStore:314、modelFactory:317；可选端口缺省降级：permissionBroker 缺→createDenyPermissionBroker（agent-runtime.ts:249）、toolRegistry 缺→createToolRegistry:295、subagentPort 缺→createDefaultSubagentPort:291。
- **关键参数**：queueAutoDrain=true:216、maxConcurrency=config.toolConcurrency ?? 10（scheduler.ts:48）、appVersion 缺省 "0.0.0":259。
- **调用方式**：bootstrap create-app 构造 runtime→executeTurn 内循环调 modelFactory+scheduleTools；runtime commands 经 `createRuntimeCommandQueue`（:279，command-queue.ts:10 优先级 now/next/later）。
- **abort 语义**：beginShutdown():327 置 shuttingDown 阻断 terminal 事件再唤醒模型；pendingInput reservation/lease:417-446 防 drain/多端重复提升（仅进程内）。
- **注意**：rewindConversationToMessage 故意不走 command queue（:622-627 防自等队列）；旧会话缺 modelSelection 不造默认（:274）。

### core/agent（`core/src/agent`）
- **用途**：turn 状态机与历史水合。
- **核心接口**：`TurnPhase` 枚举 `turn-state.ts:25-36`（idle/processing_input/awaiting_model_response/streaming/scheduling_tools/executing_tools/aggregating_results/awaiting_permission/completing/error）；`canTransitionTo:230-263` 迁移表；`isTerminalPhase:218`（终态=completing|error）；`TurnMachineImpl` `turn-machine.ts:68`（start:106、scheduleTools:151、startToolExecution:166 按 waiting_permission 分流:168、resolvePermission:251、fail:299）。
- **关键参数**：`TurnResultType`:149-156：success/cancelled（用户中断复用 TurnComplete）/error_max_turns/error_max_budget/error_during_execution/error_max_tool_calls；`PermissionDecision`="allow|deny|escalate|modify":147。
- **注意**：非法迁移抛 CoreErrorType.InvalidTurnPhase（turn-machine.ts:92-104，recoverable:true）；fail():299 不走 transition 直写 Error 相；状态为不可变替换（{...state}）。

### core/tool（`core/src/tool`）
- **用途**：工具注册、调度与执行管线。
- **核心接口**：`ToolRegistryImpl` `registry.ts:30`（register:34/unregister:73/get:88/toContracts:104 过滤 providerVisible===false）；`ToolScheduler` `scheduler.ts:50`（schedule:60 拓扑排序+分层并行分组 groupByParallel:154，环检测抛 InvalidStateTransition:138/214；canRunInParallel:85：destructive→false、concurrentSafe===true→true、readOnly→true、sideEffectScope==="none"→true）；`ToolExecutorImpl` `executor/impl.ts:16`。
- **关键参数**：DEFAULT_MAX_CONCURRENCY=10（scheduler.ts:48）；executor defaultTimeoutMs=300000（impl.ts:32）；READ_ONLY_TOOLS 集合 scheduler.ts:233-243。
- **executor 管线**（call-runner.ts:65 起）：别名归一（:72-76）→输入校验/归一→PreToolUse hooks（hook-flow.ts:16）→权限（permission-flow.ts:41 resolveToolPermission：checkPermission:86 + preTool hook 覆盖:92 + memory 文件特判:93；deny 即 PermissionDenied 事件:130）→PermissionRequest hooks:49→超时（timeout.ts:181 resolveTimeoutMs= min(requested,maxMs)+cleanupGraceMs:203；abort 走 ToolCancelled:152-167）→handler→PostToolUse/失败 hooks（hook-flow.ts:125/159）。批执行 batch-runner.ts:24：≤maxConcurrency 全并行，否则切片。
- **后台任务注册表**：`executor/background-task-registry.ts:47`——dwf 分派名（CreateWorkflow/AmendWorkflowRun/ResumeWorkflowRun，:39）跨 resume「重臂」复位结算面（:100-107）；BackgroundTaskTracker（background-tasks.ts:86）挂 executor。
- **注意**：alias 冲突拒绝本 alias 保留已注册身份（registry.ts:62-68）；canonical 永远压过同名旧 alias（:35-45）；未注册工具名遥测统一落 "unknown" 桶（call-runner.ts:77-79）；timeout policy kind:"none" 不设墙钟（timeout.ts:131,188）。

### core/compact（`core/src/compact`）
- **用途**：自动/手动压缩与本地 microcompact 决策。
- **核心接口**：`shouldAutoCompact` `policy.ts:90`（返回 AutoCompactDecision:41，reason: disabled/not_enough_messages/circuit_breaker/below_threshold/above_threshold）；`getEffectiveContextWindowSize:67`；`getAutoCompactThreshold:84`；microcompact `maybeLocalMicrocompactMessages` `microcompact.ts:83`；`buildManualCompactBoundary` `manual.ts:77`、`buildCompactPrompt` `prompt.ts:111`。
- **关键参数**：DEFAULT_COMPACT_CONTEXT_WINDOW=200_000（policy.ts:6）、DEFAULT_AUTOCOMPACT_OUTPUT_RESERVE_TOKENS=32_000:9（上限夹到 21_000 preflight:10,78-81）、MAX_OUTPUT_TOKENS_FOR_SUMMARY=20_000:11、AUTOCOMPACT_BUFFER_TOKENS=13_000:12、MAX_CONSECUTIVE_AUTOCOMPACT_FAILURES=3:14；microcompact：keepRecentToolResults=5:14、idle 阈值 60min:15、minTokenSavings=256:16、threshold ratio 0.9 / buffer 2_000:17-18；MAX_COMPACT_PROMPT_TOO_LONG_RETRIES=3（manual.ts:55）。
- **调用方式**：runtime 回合前调 shouldAutoCompact；summary 请求 `methods/compact-summary-model-request.ts:74`；清空结果前缀 "[Old tool result content cleared]"（microcompact.ts:12）。
- **注意**：连续失败熔断 circuit_breaker（policy.ts:136-144）；token 来源 estimate vs provider_usage:28。

### core/memory（`core/src/memory`）
- **用途**：项目持久记忆：目录定位、抽取调度、召回清单、受限 agent 循环。
- **核心接口**：`resolveProjectMemoryRoot` `project-root.ts:10`（`<cliStorageRoot>/memories/projects/<slug>-<sha256前16>/…/memory`）；路径加固 `resolveSafeMemoryFilePath` `memory-file-path.ts:43`（contained 相对路径 + SENSITIVE 段黑名单:5-25，控制字符/Windows ADS 清洗:78-83）；`createMemoryExtractionScheduler` `extraction.ts:85`；受限循环 `runMemoryAgentLoop` `memory-agent-loop.ts:41`（只读 Read/Grep/Glob:39，写限 memory 目录内 markdown:165）。
- **关键参数**：MINIMUM_USER_WORDS=3（extraction.ts:6）；error 不推进 cursor:136；drainMemoryExtractions 上限 60s（agent-runtime.ts:660）。
- **注意**：shutdown 必须 abort 否则进程存活时抽取继续写（extraction.ts:172-173）。

### core/context（`core/src/context`）
- **用途**：system prompt 装配（分节 + 缓存提示 + meta-user 附件）。
- **核心接口**：`ContextBuilder` `builder.ts:39`（build():82、addSection:65、setEnvInfo:54）；注入排序 `orderSectionsForInjection:310`（system/stable → system/dynamic → meta_user/stable → meta_user/dynamic）。
- **节顺序**（build:100-205）：cli-prefix → identity|customSystemPrompt|workflowActor（三者互斥，workflowActor+custom 同在抛错:93-96）→ desktop → dynamic behavior → session guidance → memory → env-info → output style → context management → git context → skills（meta_user，需 Skill 工具在表:179/225）→ request-user-context（项目指令+memory 索引）→ current-date → custom sections。
- **关键参数**：system message 三块各带 cacheControl ephemeral:35。
- **注意**：setToolRegistry 已成 no-op:50（工具说明不再镜像进 prompt）。

### core/subagent（`core/src/subagent`）
- **用途**：子代理 profile 解析、预设与 spawn 运行器。
- **核心接口**：`AgentProfile` `profile.ts:21-38`（frontmatter：name/description/systemPrompt/tools/disallowedTools/modelSelection/permissionMode("auto|plan":16)/memory("user|project|local":19)/mcpServers/skills/maxTurns/background/color/injectAgentsMd/source）；`parseAgentProfileFromMarkdown:154`；`normalizeAgentProfiles:89`（内置预设先行、同名覆盖）；spawn `createExploreSubagentPort` `runner.ts:131`（inactivity 超时 :214，maxTurns 透传 :1146）。
- **预设差异**：general-purpose `profile.ts:115-130`：tools:["*"]、injectAgentsMd:true、color:"blue"；Explore `profile.ts:66-80`：只读工具集 [Bash,Glob,Grep,Read,WebFetch,WebSearch,TodoWrite]、injectAgentsMd:false、color:"cyan"。DEFAULT_SUBAGENT_TYPE="general-purpose"（profile.ts:10）。
- **注意**：用户/项目同名 profile 覆盖内置 Explore（需 source==="built-in" 判定，profile.ts:82-87）。

### core/hooks（`core/src/hooks`）
- **用途**：hook 事件执行器与工作区 hook 信任链。
- **核心接口**：`InMemoryHookRunner` `runner.ts:35`（register:48、run:52：匹配 event+matcher → admission → 逐个 dispatch）；信任评估 `evaluateWorkspaceHookEntry` `workspace-hook-trust-evaluation.ts:15`。
- **事件面**：`HookEventName` contracts/hooks/index.ts:7-15（SessionStart/UserPromptSubmit/PreToolUse/PermissionRequest/PostToolUse/PostToolUseFailure/Stop）；`HookOutcome`:19-25（success/blocked/failed/cancelled/timed_out）。
- **信任链**：`WorkspaceHookTrustState` contracts/hooks/workspace-hook-trust.ts:31-39（pending_trust/trusted_persistent/blocked_untrusted/blocked_policy/revoked/stale_digest/not_applicable）；审查超时 WORKSPACE_HOOK_REVIEW_TIMEOUT_MS=10min:4；**授权决定不缓存、dispatch 前重验**（runner.ts:87-90）。
- **关键参数**：defaultTimeoutMs=60000（runner.ts:42，hook 级覆盖 :340）。

### core/telemetry（`core/src/telemetry`）
- **用途**：runtime 统一遥测出口（turn/step/compaction/detached span 工厂）。
- **核心接口**：`RuntimeTelemetryFacade` `runtime-telemetry.ts:34`（turn:61、step:75、compaction:82、detached:88）。
- **注意**：port 缺省 NOOP（:46）；launchSurface 缺省 "standalone_cli":51；port 缺席时全链路 no-op 不抛错。

**权限指针**：PermissionService.checkPermission、createDenyPermissionBroker、contracts/interfaces/permission.port.ts:95——见 [沙箱与权限.md](沙箱与权限.md)。

## 二、适配器（adapters）

### model（`adapters/src/model/`）
- **用途**：Vercel AI SDK 封装：文本/流式调用、重试、流事件归一与 IO 落盘。
- **核心接口**：`AiSdkModelAdapter`（model/runner.ts:80）；`runGenerateText`（runner-generate.ts:65）；`runStreamText`（runner-stream.ts:93，async generator）；`StreamingToolCallAssembler.handle(event): ModelStreamEvent[]`（streaming-tool-call-assembler.ts:24）；`normalizeReasoningHistory`（reasoning-history-normalization.ts:31）与 `repairReasoningHistoryAfterSignatureRejection`（:64）。
- **关键参数**：`maxAttempts=10+1`、`baseDelayMs=2000`、`backoffFactor=2`、`maxDelayMs=60000`、`jitter=true`（retry-policy.ts:13-30；env `ZCODE_MODEL_RETRY_MAX_RETRIES/BASE_DELAY_MS/BACKOFF_FACTOR/MAX_DELAY_MS`:18-21）；流空闲超时默认 600_000（contracts config:284），每次重试 +30s（stream-idle-timeout.ts:5,23）；debug 落盘上限 rollout 3 文件/64MB、dev 256MB（runner-debug.ts:34-38）。
- **注意**：idle timeout ≤0 关闭看门狗（stream-idle-timeout.ts:106）；流式中 final tool_call 不能替代缺失的 `tool_input_end`（streaming-tool-call-assembler.ts:82-89）；reasoning 回放按 provider 兼容组裁剪（reasoning-history-normalization.ts:20-27）；`ZCODE_RUNTIME_ENV=test` 不写 IO（runner-debug.ts:65）。

### storage（`adapters/src/storage/`）
- **用途**：SQLite 会话持久化（node:sqlite DatabaseSync）。
- **核心接口**：`SqliteSessionStore`（session-store/sqlite-session-store.ts:225，实现 5 Port）；`createSqliteSessionStore`(:1010)；`openStartup`(:272)；`runSqliteSessionMigrations[Async]`（migration-runner.ts:49,59）；`getDefaultSessionDbPath()`=`~/.zcode/cli/db/db.sqlite`（paths.ts:6）。
- **关键参数**：`DEFAULT_SQLITE_STARTUP_LOCK_TIMEOUT_MS=5000`（migration-runner.ts:13）；迁移锁等待 60min（:45）、async 期 busy_timeout=25ms（:46,66）；`pragma foreign_keys=on`（:137）、`journal_mode=wal`（:248）。表族：session/message/part/session_entry/session_input/session_target/todo/input_history/local_setting/permission/*_usage/dwf_*/workflow_*（migrations/）。
- **注意**：多 Agent 共库，timeout 须在首条 PRAGMA 前生效（sqlite-session-store.ts:249-251）；迁移后必须恢复 5s busy_timeout，半迁移连接不外发（migration-runner.ts:88-99）。

### config（`adapters/src/config/`）
- **用途**：多层配置合并为 RuntimeConfigPatch。
- **核心接口**：`createConfig(options): ConfigResult`（config-factory.ts:129）；`mergeConfigs`（config-merger.ts:26）；`loadProjectConfigs`（project-config.adapter.ts:31）；`ZCodeConfigFileSchema`（schema.ts:286）。
- **关键参数**：scope 压栈序 System→User→Project→Env→Cli（config-factory.ts:132-207）；插件 scope 仅 user|workspace（:103）。
- **注意**：explicit project 配置若绕过统一发现会重复入栈、使 Trust stale（:170-176 注释）。

### exec（`adapters/src/exec/`）
- **用途**：Node 子进程执行适配（bash/argv）。
- **核心接口**：`NodeExecutionAdapter`（node-execution-adapter.ts:5，继承链 Lifecycle→Run→Process→Results）；`run()`（node-execution-adapter-run.ts:27）；`resolveExecutionCommand`（execution-command.ts:71）。
- **关键参数**：`BASH_MAX_OUTPUT_LENGTH` 默认 30000、上限 150000（bash-output-policy.ts:1-2）；POSIX 进程组 SIGTERM→750ms→SIGKILL（process-tree.ts:4）、bash 专用 1500ms（:5）。
- **调用方式**：`run`→`prepareChildSpawn`（node-execution-adapter-process.ts:60：buildExecutionEnv→resolveExecutionCommand→输出目录/snapshot）→spawn→collector。
- **注意**：Windows `.cmd/.bat` shim 须经 cmd.exe（execution-command.ts:112-125）；Windows 清理用 `taskkill /T /F`（node-execution-adapter-process.ts:153）；posix-bash provider 用 `-c -l` 登录 shell（execution-command.ts:143-150）。

### fs / http / image / pdf / mailbox / context（各短条目）
- **fs**：`NodeFileSystemAdapter`（fs/index.ts:115）；glob 默认 100、grep head 250、ripgrep 超时 30000ms、排除 .git/.svn/.hg/.bzr/.jj/.sl（:51-54）；内嵌 ripgrep worker 源码字符串（:79）；快路径 10MB（text-range-reader.ts:19）。
- **http**：`NodeHttpClientAdapter`（http/index.ts:46）；默认超时 180000ms、响应上限 10MiB、trace 头 `x-zcode-trace-id`（:31-33）；公网出网策略 `assertPublicEgressDestination`（public-egress-policy.ts:26）。
- **image**：Jimp 适配（image/index.ts:12）；webp 直通不重压（:27）；JPEG 质量 [80,60,40,20]、缩放 [0.75,0.5,0.25]（jimp-compression.ts:32-34）；token 预算比率 0.125（image-budget.ts:11）。
- **pdf**：Poppler 适配（pdf/index.ts:26），经 ExecutionPort 调 `pdfinfo` 取页数，失败返回 `undefined`（:39-55）。
- **mailbox**：`NodeSessionMailboxAdapter`（mailbox/index.ts:15）；unread→read 靠 `rename` 原子转移，默认 drain 20 条（:27-42）；sessionId 强校验 `sess_*` 且路径不得逃逸 root（:13,52-56）。
- **context**：`NodeContextSourceAdapter`（context/index.ts:31）；优先文件 `AGENTS.md`、上限 100KiB（:24-25）；git 快照 `resolveGitSnapshot`（git-snapshot.ts:27）。

### skills（`adapters/src/skills/`）
- **用途**：技能根解析与 SKILL.md 扫描。
- **核心接口**：`resolveDefaultSkillRoots`（skills/roots.ts:20）；`scanSkillFilesUnderRoot[Sync]`（skills/scan.ts:43,77）。
- **关键参数**：优先级步进 10（roots.ts:8）；根序：extraRoots→user `~/.zcode/skills`+`~/.agents/skills`（合并非 fallback，:101-104）→project 自 cwd 向上到 `.git` worktree 根（:61-83）。
- **注意**：`followSymbolicLinks` 默认 true（用户级允许 symlink 导入），plugin-scope 必须传 false——根/子目录/SKILL.md 三粒度一律拒链（scan.ts:16-23,47-49,105-106）；错误契约只吞 ENOENT，EACCES 上抛（:144-155）。

### mcp（`adapters/src/mcp/`）
- **用途**：MCP 客户端适配：stdio 传输、连接池、OAuth、工具桥接。
- **核心接口**：`ProcessTreeStdioClientTransport`（stdio-transport.ts:37，覆写 `_dispose` 先回收整棵进程树再交 SDK）；`createMcpConnectionPool`（pool.ts:57）；`createMcpAdapter`（index.ts:157）；`createCredentialKeyPrefix`（oauth.ts:16）。
- **关键参数**：池空闲宽限 `DEFAULT_IDLE_GRACE_MS=30_000`（pool.ts:16）；凭据键 `mcp:oauth:`+sha256 前 24 hex（oauth.ts:22-34）。
- **调用方式**：core→`createMcpAdapter`→pool acquire/release lease（引用计数，pool.ts:162-182）→Client+transport→工具描述符桥接。
- **注意**：SDK 2.0 探测兄弟进程会孤儿化 watchdog 后代，故在 dispose 钩子收树（stdio-transport.ts:30-36）；Windows 挂 Job Object 兜底（windows-job-object.ts:27）。

### plugins（`adapters/src/plugins/`）
- **用途**：marketplace/插件安装校验与 hook 来源枚举。
- **核心接口**：`addMarketplace`/`installMarketplacePlugin`/`validateMarketplacePlugin`（marketplace.ts:330,587,672）；`resolveGitHubArchiveSource`（github-archive-source.ts:82）；`listPluginHookSources`（hook-sources.ts:22）。
- **关键参数**：marketplace.json ≤10MiB、重定向 ≤5、超时 180000ms（marketplace.ts:50-52）；git clone 至多 3 次、单命令 90000ms（:58-59）；标准 hook 路径 `hooks/hooks.json`（hook-sources.ts:8）。
- **注意**：GitHub 归档仅接受 https github.com 公共仓库（2 段 path、无凭据/query，github-archive-source.ts:38-56），不满足时抛 `GitHubArchiveRequiresGitError` 由 git fallback 接管（:60-67,119）；hook 相对路径经 `resolveInside` 防逃逸（hook-sources.ts:44-52）。

### auth（`adapters/src/auth/`）
- **用途**：共享凭据存储与各登录形态。
- **核心接口**：`createSharedZCodeCredentialStore`（shared-credentials.ts:68）；`createBigmodelOAuthClient`（bigmodel-oauth.ts:45）；`createCliOAuthClient`（cli-oauth.ts:75）。
- **关键参数**：键表 `SHARED_ZCODE_CREDENTIAL_KEYS`：`oauth:active_provider`、`oauth:zai:*`、`oauth:bigmodel:*`、`zcodejwttoken`（shared-credentials.ts:15-24）；数据根 env `ZCODE_DATA_BASE_DIR`（:12）。
- **注意**：损坏文件备份后重建（backupCorruptFile，:5）；凭据变更监听按 key 分组（:14-19）。

## 三、装配与终端

### bootstrap（`bootstrap`）
- **用途**：进程内装配层——把 adapters/storage、config、MCP、skills、plugins、subagents、telemetry 组装成 AgentRuntime 与各 facade。
- **核心接口**：`createZCodeApp(options)`（app/create-app.ts:196 起）；`listZCodeSkills/inspectZCodeSkill`（skills.ts:26,43）；`loadZCodeAgentProfiles/loadPluginAgentProfiles`（subagents.ts:49,126）；`resolveZCodePlugins`（plugins.ts:250,590）；`prepareZCodeTelemetryEnv/shutdownZCodeTelemetry`（telemetry-bootstrap.ts:12,31）。
- **关键参数**：hooks fallback `timeoutMs=60_000`、`maxOutputBytes=32_768`（create-app.ts:312-313）；mailbox 根 `ZCODE_MAILBOX_ROOT ?? ~/.zcode/mailbox`（:364）；artifact 缓存 `<storage>/cli/{image,pdf,video}-cache`（:352-355）；动态工作流灰度关闭时剔除 dwf 技能与 `/workflow` 命令（:755-757,804-806）。
- **调用方式**：cli `run.ts` → create-app：adapters 顺序 storage/logging/config/exec/fs/http/image/pdf/mailbox/context/skills/mcp（create-app.ts:2-19）→ plugins(214)→subagents(208,223)→runtime-config(244)→PermissionService(342)→`new AgentRuntime(...)`(726)。
- **注意**：node-repl browser broker 仅在 `browserUse` 特性 + stdio node_repl 时注入（:263-286）；skillPort 需 features.skill && skills.enabled（:746）。

### slash-command-surface + custom-commands（`bootstrap/src/`）
- **用途**：命令面守门：内置/保留名清单；自定义命令发现/加载。
- **核心接口**：`isReservedZCodeSlashCommandName(name)`（slash-command-surface.ts:32）；`listZCodeCustomCommands`（custom-commands.ts:32）；`inspectZCodeCustomCommand`(:41)；`loadZCodeCustomCommand`(:62)。
- **关键参数**：App 可见内置名 `["goal","compact","init"]`（slash-command-surface.ts:3-7）；App 专属 `plan`（:10-17）；额外保留 `["compress","plan"]`（:19）。
- **注意**：inspect 未命中抛 `Custom command not found`（custom-commands.ts:50）；名字统一去 `/`+小写（:100-102）。

### cli（`cli`）
- **用途**：进程入口：stderr/协议边界、子命令分流、SEA 打包。
- **核心接口**：`main()`（cli/src/main.ts:16）；`run(context, deps)` 分发（run.ts，默认命令名 `"tui"` :65）；`isProtocolServerInvocation`（arguments.ts:109）；`runPluginHostCommand`（main.ts:63）。
- **关键参数**：`--prepare-storage` 跳过进程名/SEA 工具/provider env（main.ts:19,51,66）；SEA 脚本 `build:sea = turbo 全量 build + scripts/build-sea.mjs --all`（cli/package.json:17-18）。
- **注意**：protocol/TUI 独占 stdout，console 必须重定向 stderr，否则 JSON 帧崩（main.ts:30-35）；plugin-host 在 `server.connect()` 后返回属正常，exit 0 免 watchdog（:100-104）。

### tui（`tui`）
- **用途**：opentui-react 终端界面。
- **核心接口**：`runTui(options)`（tui/src/tui.tsx:13）；`TuiApp`（app.tsx:48）；`useTuiKeyboardControls`（app-keyboard.ts:95）；`filterSlashCommands`（app-input.ts:31）；`useTuiModeSwitcher`（app-mode.ts:24）。
- **关键参数**：非 TTY 直接 exit 1（tui.tsx:14-18）；`targetFps:30`、`exitOnCtrlC:false`、exitSignals 不含 SIGPIPE（:28-49）；可切模式 `["plan","build","edit","yolo"]`（app-mode.ts:5），默认 `"build"`（app.tsx:68）；tab 切模式（app-keyboard.ts:261）；ctrl+c 两次退出（:201-373）；审批键 ↑/↓/return/escape（app-approval.ts:25-49）；`@` 触发文件补全（app-file-mentions.ts:10）。
- **注意**：主题含终端探测（600ms 并行，不阻塞首屏，tui.tsx:141-145）；斜杠过滤仅在 draft 以 `/` 开头且无空白（app-input.ts:35）。

### dynamic-workflow + dynamic-workflow-runtime
- **用途**：工作流脚本 DSL（TS 编译→lowering→沙箱子进程执行）与父进程宿主。
- **核心接口**：`compileWorkflowScript(text)`（compiler/compile.ts:124）；`WorkflowEngine`（engine/engine.ts:136）宿主 API：`createActor`(:377)、`ask`(:417)、`worldRead`(:442)、`report`(:473)、`declareArtifact`(:489)、`complete`(:496)、`stop`(:507)、`fail`(:518)；DSL：`agent(name?, persona?)`（facade/dts.ts:65）、`phase(name)`（:256，每脚本必须至少含一个 phase 或 world.run，:224,247）；runtime `runWorkflowScript`（harness.ts:51 起）。
- **关键参数**：`SCRIPT_FILE_NAME="workflow-script.ts"`（compile.ts:18）；NDJSON 消息 create-actor（FIFO 映射 local#N→ActorId）、request（ask/world-read/publish-artifact）、event（log/report/declare-artifact/phase-entered）（runtime/src/protocol.ts:44-110）。
- **调用方式**：bootstrap `script-workflow-process.ts:43-45` spawn `process.execPath --input-type=module --eval CHILD_SOURCE -- <base64url payload>`；harness 写 `<cwd>/.zcode/workflow-runs/<runId>.mjs` 再 spawn（harness.ts:11-13）。
- **注意**：终态失败（脚本抛错/崩溃/超时/JSON 解析失败）走 `engine.fail`；abort 是唯一真取消走 `engine.stop`（harness.ts:23-28）；harness 不校验 scriptText/lowered 自洽（:57-62）。

### node-repl-host（`node-repl-host`）
- **用途**：独立 JS 运行时 MCP 宿主（node_repl），桥接浏览器与 CUA。
- **核心接口**：单工具 `js`（server.ts:76-83），执行体 `NodeReplSession`（经 `@zcode/core/repl` 深路径导入，:12-16）；browser/cua bridge（browser-bridge.ts、cua-bridge.ts）。
- **关键参数**：`NODE_REPL_DEFAULT_TIMEOUT_MS=60_000`（tool-contract.ts:3）、`MAX_SYNC_TIMEOUT_MS=120_000`（server.ts:39）、`UNTRUSTED_SESSION_KEY="__unscoped__"`（:40）、每次调用独立 Worker（:41）。
- **注意**：runtime_scope 仅 `main|subagent`（server.ts:62）；core 必须走深路径否则 bundle 21.7MB 且 ERR_AMBIGUOUS_MODULE_SYNTAX（:7-11）。

### telemetry（`telemetry`）
- **用途**：OTel 模型/Agent 遥测：trace+metrics OTLP 导出。
- **核心接口**：`createModelTelemetry({owner?,sessionId?})`（bootstrap.ts:39）；`prepareModelTelemetryEnv`(:120)；`shutdownPreparedModelTelemetry`(:139)。
- **关键参数**：未配 endpoint 或 `ZCODE_MODEL_TELEMETRY_ENABLED∈{0,false,off,disabled}` 则禁用（:124,414-416）；flush/关闭超时 `1_500ms`（:61,143）；serviceName 默认 `"zcode-cli-agent"`（:173）；device mid 落 `~/.zcode/v2/telemetry-state.json`（:226-228）。
- **注意**：disabled 路径不 import SDK（:117-118）；身份文件 I/O 失败只丢匿名关联不得影响主链路（:258-261）。

### i18n + debug（短条目）
- **i18n**：`getZCodeCopy(locale?, detected?)`（i18n/src/index.ts:30）；`DEFAULT_LOCALE="en-US"`、`SUPPORTED_LOCALES=["en-US","zh-CN"]`（locale.ts:3-4）；types 区分 `CliCopy/TuiCopy`。
- **debug**：浏览器调试台（React+vis-timeline 甘特渲染 span），HTTP fetch 拉观测服务导出的 trace/网络/context 快照（App.tsx:1219）；端口约定未核实。

## 四、根 packages

### shared（`packages/shared`）
- **用途**：跨端 zod 契约/类型/纯函数库（160+ 文件），含 v4 协议族 schema。
- **核心接口**：`V4_WIRE_PROTOCOL_VERSION = 3`（src/zcode-protocol-v4/core.ts:7）；`SERVER_REMOTE_PROTOCOL_VERSION = 1`（src/server-remote.ts:3）；`ZCODE_RPC_HOST_CAPABILITY_HEADER = "x-zcode-rpc-host-capability"`（src/channels.ts:498）。
- **注意**：v4 目录纪律=只放 schema+纯函数，运行时不进本包（zcode-protocol-v4/index.ts:1-4）；v4 wire 版本=3 与包名 v4 不同步，升级须改 core.ts 常量。

### rpc（`packages/rpc`）
- **用途**：VS Code 式二进制 IPC：帧协议+Channel 多路复用+流控。
- **核心接口**：`PersistentProtocol`（src/persistent-protocol.ts:46）；`ChannelClient`（channelClient.ts:19）/`ChannelServer<TContext>`（channelServer.ts:13）；`SocketProtocol`（protocol.ts:236）/`MessagePortProtocol`（:361）；`IPCServer/IPCClient`（ipc.ts:95/280）；`WebSocketRemoteConnection`（remote.ts:44）。
- **关键参数**：`KEEP_ALIVE_INTERVAL = 5000`（persistent-protocol.ts:77）；`ACK_TIMEOUT = 20000`，超时断开（:81）。
- **注意**：未 ACK 字节缓存于 outgoingUnackMsg，ack 序号读于 header offset 5（:179）。

### provider + provider-node
- **用途**：provider/model 注册表与配置解析（纯逻辑）；node 侧负责文件仓库与内置 provider 下载物化。
- **核心接口**：`ProviderRegistry`（src/registry.ts:47，getView/listProviders/getProvider/getModel :58-70）；`ProviderConfigResolver`（resolver.ts:182）；`NodeProviderRegistryRuntime`（provider-node/src/provider-registry-runtime.ts:21）；`decodeZCodeBuiltinRelease`（zcode-builtin-release.ts:38）。
- **关键参数**：`ZCODE_BUILTIN_RELEASE_SCHEMA_VERSION = 1`（:10）；内置源拉 `/api/v1/client/configs`，失败 stage 转 cdn（zcode-builtin-download.ts:50-57）；下载 URL 仅允许 https 无凭据（:15-16）。
- **注意**：下载日志禁止携带 URL query/响应正文/schema 输入以防凭据泄漏（:59-60）。

### services（`packages/services`）
- **用途**：领域服务集合（约 40 子域），node/desktop 共用。
- **核心接口**：`createZCodeSessionService({agentService,taskIndexSyncer,...})`（zcode-session/zcodeSessionService.ts:47）；`IStorageService`（storage/contract.ts:31）；`createTerminalService`（terminal/terminalService.ts:323）；cron `computeNextRunAt`（session/automationCron.ts:59）；undici 传输 `createHostApiNetworkTransport`（providers/api/nodeApiNetwork.ts:95）。
- **关键参数**：node-pty ^1.0.0、undici ^6.23.0（package.json:28,30）。
- **注意**：node-pty 在 desktop 需 postinstall rebuild（desktop/package.json:25）；server 侧用 @lydell 预编译 pty 分平台包（server/package.json:21-24）。

### server（`packages/server`）
- **用途**：Hono HTTP/WS 无头服务与 remote 后端接入。
- **核心接口**：`createHttpServer` 路由面：`/api/server-info`（src/http.ts:317）、`/api/rpc-host-capability`(:318)、`/ws`(:322，默认 terminal-client)、`/ws/host`(:336-343，trusted-host)、`/api/connect-remote`(:346)、`/ws/remote/:id`(:368)。
- **关键参数**：`staticRoot`+`spaFallback ?? true`（:138,399-403）；clientMode=`"desktop-continuous"|"web-remote-replayable"`(:93)；后端 ssh/docker/wsl（src/remote/）。
- **注意**：非 trusted-host 强制覆写 Provisioning.apply 为 throw（:100-108）；单文件集中注册保持同一鉴权顺序（:1 注释）。

### zcode-server-cli（`packages/zcode-server-cli`）
- **用途**：无头 server CLI/守护：安装、启停、服务注册、监督。
- **核心接口**：`runServerCli`（src/cli.ts，main.ts:18 调用）；`runServerCore(generation)`（server-core/core.ts:17）；`daemonStartupMode(): "service"|"fallback"`（cli.ts:58）。
- **注意**：candidate 启动时 Supervisor 重算 wiring，避免继承旧 release 的 zcode.cjs（main.ts:7-9）。

### desktop（`packages/desktop`）
- **用途**：Electron 主应用：main/preload/host（utility）/scheduler/renderer 五进程面。
- **核心接口**：`spawnHostProcess`/`loadWindow`（src/main/desktopHostProcess.ts:151/109）；`spawnCronScheduler`（desktopCronScheduler.ts:60）；协议注册 `app.setAsDefaultProtocolClient`（desktopOAuthDeepLink.ts:409,426）；`createWindowsDesktopTray`（desktopTray.ts:21）。
- **关键参数**：deep link scheme `zcode://`（desktopFinderOpenFolderWorkflow.ts:25）。
- **注意**：内置浏览器为 main/browserView/（browserGuestManager 等）；cookie 迁移走 chromeCookieManager；AppImage 下 zcode:// 经 xdg-open 二次启动（desktopLinuxDeepLinkRegistration.ts:64）。

### web + client + ui（短条目）
- **web**：纯浏览器 3 页——主应用（src/main.tsx）、OAuth 回调（auth/WebCallbackPage.tsx）、分享落地页（share/ConversationShareLandingPage.tsx）；经 `@zcode/client` connectViaWebSocket 连 server `/ws`；无 trusted-host 能力。
- **client**：`connectViaWebSocket(wsUrl, options?)`（src/websocket.ts:62）；`connectViaProtocol`(:102)；`connectViaMessagePort`（messageport.ts:57）；WS 在 open 前 close/error 即 reject，错误文案区分 reason（websocket.ts:73-97）。
- **ui**：React 应用主体——`App()`（src/App.tsx:91）；`createTabStore(storage?)`（store/tabStore.ts:249）；`useTheme()`（useTheme.ts:82）；v4 投影消费集中在 src/v4/（Conversation* 行族）；组件分域（settings/command-center/terminal/browser-use/onboarding 等）细节另见总览。

### zcode-cua + harness/remote（短条目）
- **zcode-cua**：Computer Use 占位包：API 兼容桩，所有运行时面报 unavailable 并 fail closed（package.json:5）；`BROKER_SOCKET_ENV="ZCODE_CUA_PERMISSION_BROKER_SOCKET"`（broker.js:6）；仅 .js/.d.ts 桩。swift-bridge 在 872ad96 树内未找到（未核实是否外部仓）。
- **harness/remote**：本地 SSH 容器测试三件套（Dockerfile+build.sh+README）；ubuntu:22.04+openssh-server，root/dev 双账号、开放密码登录（Dockerfile:6-15）；仅测试用途，root 密码 hardcode。
