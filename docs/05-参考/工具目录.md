# 工具目录

> 状态：对齐 demo 全量装配（M8 收官版）。框架内置工具 + MCP 挂载示例——工具随插件增删，本目录由[对账测试](https://github.com/dengdz/duo-harness/blob/0.3.0/duo-harness-example/src/test/java/dev/duo/harness/example/ToolCatalogTest.java)锁定：注册清单与文档漂移会在测试里炸出来。机制语义见[设计主线](../04-架构/设计主线.md)，逐行装配见[插件配置参考](插件配置参考.md)。

## 框架内置工具

### ask_user —— 向用户提问

- **来源**：`tools.AskUserTool`（演示入口经交互服务注册）
- **描述**：向用户提问并等待回答。当任务信息不足、存在多种做法需要用户拍板时调用；给出具体明确的问题，可用 options 提供预设选项（用户也可自由输入）。调用会阻塞直到用户回答。
- **参数**：`question`（必填，一句话具体明确）、`options`（可选，预设选项数组；省略表示自由文本）、`multiSelect`（可选，默认 false）
- **无人应答**：fail-closed 收敛为错误结果

### skill —— 按名加载技能指令

- **来源**：`agent.SkillTool`（SkillsPlugin 装配）
- **描述**：按名加载技能的完整指令。当任务匹配某个可用技能的描述时调用；调用后会返回该技能的指令内容，请严格遵循执行。
- **参数**：`name`（必填，技能名；可用技能见系统提示里的技能清单）

### exit_plan_mode —— 呈交计划

- **来源**：`agent.ExitPlanModeTool`（演示入口装配）
- **描述**：呈交计划等待用户批准。仅在计划模式下、完成完整设计后调用；参数 plan 为完整计划（markdown）。用户批准后立即开始执行；被要求继续时按用户反馈修改计划后再次呈交。
- **参数**：`plan`（必填，完整计划 markdown）
- **无人应答**：fail-closed——计划不批准，保持计划模式

## MCP 挂载示例（demo 的 files 服务器）

MCP 远端工具以 `mcp__<server>__<tool>` 命名注册进工具域，随连接增删自动同步。demo 以迷你 filesystem 服务器演示（根目录锁定临时沙箱）：

### mcp__files__read_file —— 读取文件

- **描述**：读取根目录下的文本文件（相对路径）（MCP: files）
- **参数**：`path`（相对路径）

### mcp__files__write_file —— 写入文件

- **描述**：向根目录写入文本文件（相对路径）（MCP: files）
- **参数**：`path`（相对路径）、`content`（完整内容，覆盖式写入）
- **治理示例**：demo 的写保护插件对本工具声明 ask（写操作逐次审批）

## 接入新工具

- **Java 工具**：实现 `ToolDefinition`（name / description / parameters / execute），`tools.register(...)` 注册——三段管线与治理链自动生效
- **远端工具**：挂一个 MCP 服务器连接，工具自动同步，无需注册代码
- 增删工具后记得更新本目录——对账测试会替你盯着
