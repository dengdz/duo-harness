# 泄漏判定示例

提炼自前身项目（duo-agent）的清理实践与通用的校准案例。**用它们学习判定原则，不要当文本模板套用。** 本文件刻意引用泄漏原文作为校准材料——[检索模式集](检索模式集.md) 排除了本技能目录，这里的措辞不是在别处保留它们的许可。

## 死会话引用

### 有已提交载体的审查编号

**泄漏：** `// High #2: 在入口就拒绝不支持的格式，避免到 build() 才失败`

**修复：** `// 不支持的格式在 apiFormat() 入口即拒绝，避免延迟到 build() 才失败`

审查编号（High #2）只在那次会话中可解析；它携带的事实（入口校验、避免延迟失败）重述为独立成立的现在时陈述。这是前身项目真实发生过的案例——四轮审查期间代码里积累了 9 处此类编号注释，最终全部清理。

### 无载体的决策引用

**泄漏：** `// 名称扁平不分区（决策 7）`

**修复：** `// 名称扁平，无命名空间分区`

仓库里没有任何文档拥有"决策 7"这个编号，引用删除；事实分句（名称扁平）保留并独立成立。

### 阶段编号

**泄漏：** `// P1 阶段先支持 openai，anthropic 后续接入`

**修复：** `// 当前仅支持 openai 格式；anthropic 见已知限制文档的路线图`

阶段编号指向一份从未提交的计划。替换为阶段产出的陈述，未完成工作路由到已提交的载体（TODO 或路线图文档）。

## 对话与审查视角

### "本次修复"叙述

**泄漏：** `// 修复 maxOutputTokens 默认值引入的回归：改为 null 由模型决定`

**修复：** `// 默认不设置，由模型决定输出长度，避免截断推理模型的长输出`

读者不关心这里曾经有 bug。"修复"视角只存在于提交信息里；注释陈述现在的行为与理由。

### "如前所述"

**泄漏：** `// 与前面 streamOnce 的处理一致，这里也关闭回调入口`

**修复：** `// 超时/失败后关闭回调入口：迟到的 chunk 不得再写入 session`

"前面"是阅读顺序视角，链接位置的代码重构后引用会漂移；直接陈述契约本身。

## 变更叙述与版本戳

### 历史叙述 + 编号

**泄漏：** `// 颜色以前来自 --widget-* tokens（没人定义，总是渲染回退值），别名 tokens 修复了这个问题（PR #88）`

**修复：** `// 颜色来自别名 tokens；未定义的 token 渲染回退值`

两个存活事实（当前机制、既有的失败行为）以现在时保留。bug 的传记属于提交信息和 PR。

### 修掉的 bug → 现在时反事实

**泄漏：** `// 这里以前会把多字节标签双重编码`

**修复：** `// 没有字节长度防护时，多字节标签会双重编码`

回归锚点以"命名了防护机制"的反事实形式存活；"以前"把它钉在了仓库考古上。

### 『本版本』式版本戳

**泄漏：** `// 本版本同步渲染，异步路径是路线图工作`

**修复：** `// 同步渲染。`（推迟由调用点的 `TODO(异步渲染):` 承载）

"本版本"/"目前"在合并那一刻就过期了。

## 审查过程记录

**泄漏：** `// 已按审查建议删除了空分支，保留说明注释`

**修复：** `// DeepSeek-R1 推理模式无需额外参数，模型本身支持`

保留幸存的决定（无需参数）作为平实事实；"谁建议的、第几轮"不是理由的一部分。

## 对审查者辩解

**泄漏：** `// 这个 cast 是安全的——SDK 构造的对象只是声明类型不够严格`

**修复：** `// SDK 构造此对象时所有可选字段已填充；声明类型比运行时保证更宽松`

改述维护者不能破坏的不变量。"它只是…"是对一个 HEAD 上无人提出的异议的回话。若不变量代码本身可见，直接删注释。

## 复述与推导

### 流程复述

**泄漏：** `// 先规范化标签，再截断，最后换行`

**修复：**（删除。）

下面三行代码说的是同一件事。

### 测试走查

**泄漏：** `// 本测试创建会话、发送两条消息、等待第二条回复，然后断言日志有四条记录`

**修复：** `// 两轮往返必须恰好产生四条日志记录——投影会去重共享前缀`

只保留不显然的断言理由；走查是对测试体的复述。

## 含糊措辞与规划残留

### 无标记推迟

**泄漏：** `// 暂时这样渲染应该没问题`

**修复：**（删除；推迟已有 `TODO(批量渲染):` 标记。若没有，写一个 `TODO(名称): 按帧合并`。）

无主的含糊语是规划残留。

### 模糊定量

**泄漏：** `// 64KiB 缓冲对大多数情况应该够了`

**修复：** `// 64KiB 容纳观测到的最大帧（48KiB）并留余量；更大的帧在 decode 处显式失败`

用真实边界和超界行为替换含糊语。

## 保留项（不是泄漏）

### issue 引用在任何位置都耐久

**保留：** `// 上限适用于完整渲染值，含包装层（后续由 issue #1470 跟进）`

不加判断的清理曾以"issue 引用属于别处"为由删掉它——方向错了：issue 在 HEAD 上可解析，"#N 拥有后续"是 README 上推迟工作的合法归宿。

### 死点名不等于"命名载体"

**删除：** `// 徽章渲染器位于 widget 接缝之上（见 widget 渲染 RFC）`

一次清理以"按主题命名了拥有文档"为由保留了它。判据是**可解析性**，不是形式：仓库里没有文件叫"widget 渲染 RFC"，指针是死的。有已提交载体就重定向，没有就删。

### 抑制理由是必需文案

**保留（修正后）：** `@SuppressWarnings("unchecked") -- 缓冲元素类型由投影器泛型保证`

理由子句必需。原理由错了（说"上面的循环保证"但根本没有循环）就修理由，永不删。

### 实测边界

**保留：** `// 深度上限（实测：512 层嵌套 ≈ 0.15s 同步；4096 阻塞循环）`

"实测"是区分数据与猜测的出处标记，防止无依据地调参。

### 运行时新旧不是仓库历史

**保留：** `// 旧连接排空后，新连接才开始接收`

这里的"旧/新"命名的是**交接期同时存活的运行时对象**，不是仓库状态。

## 过度修正陷阱

每个陷阱都在真实清理中发生过、在复查中被抓住。删改前先枚举段落的命题。

### 把义务翻转成认可

**原文：** `// 这些直接注册是待迁移到 slots 的例外`

**过度修正：** `// 这些直接注册是认可的例外`

**正确：** 保留原文。"待迁移"是义务，"认可"是给现状盖棺——压缩改写时句子的情态被反转了。

### 把假设提升成已交付功能

**原文：** `// 未来基于 IPC 的 shell 会子类化执行器并重写 spawn`

**过度修正：** `// 基于 IPC 的 shell 子类化执行器并重写 spawn`

**正确：** `// 假设性的 IPC shell（当前不存在）会子类化执行器并重写 spawn`

只删未来标记会把设计示意变成"该类已交付"的断言。

### 连同过程记录删掉真实事实

**原文：** `// 网关提示叙述检查顺序；提示文本同时是 verify-doc-typecheck 的编译对象`

**过度修正：**（整句作为叙述删除）

**正确：** `// 提示文本是 verify-doc-typecheck 的编译对象`

半句是叙述，另外半句是承重的耦合。命题共享一行时按分句删，不按句删。

### 保留数字删掉出处

**原文：** `// 4MiB 上限是实测的：最大的生成模块 3.1MiB`

**过度修正：** `// 上限 4MiB；最大的生成模块 3.1MiB`

**正确：** 保留"实测"。没有它，3.1MiB 读起来像定义而非观测，再也没人调上限前重新测量。
