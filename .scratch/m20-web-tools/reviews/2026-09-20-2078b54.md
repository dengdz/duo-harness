# M20 审查报告与处置（2026-09-20，范围 ad240ba...2078b54 + 验收反馈修复）

> 两轮流水线（duo-code-review）：第 1 轮双轴（Standards/Spec 并行子代理）→ 修复 → 第 2 轮 OCR（ad240ba → 工作树快照，含修复）→ 修复 → 测试收口。覆盖率按 `git diff --stat` 全集口径（40 文件）：OCR 名单默认不含 `src/test/**` 与文档——这两类由双轴轮与修复轮覆盖，见各轮标注。

## 审查报告（第 1 轮·双轴）：ad240ba...2078b54

### Standards 轴（原样分列）

**硬违规（文档化标准）**

1. **中文注释泄漏过程叙述**（review-log 种子模式 + duo-prose-standard）：主代码 4 处残留"验收实测"式变更归因——WebFetchTool.java:166"限额后置（验收实测 baeldung 教训）"、WebSearchTool.java:81"（验收实测）"、WebPlugin.java:60"（M20 验收实测补齐）"、WebPluginAssemblyTest.java:39。限额后置/optionalInject 的 Why 应保留，"何时发现"的过程应归 CHANGELOG。
2. **版本字面量漂移**（AGENTS 红线 6 + 文档同步模式）：WebToolsConfig 硬编码 `DEFAULT_USER_AGENT = "duo-harness/0.15.0"`，而 pom 现为 0.14.0、发布时再升——升版散点改代码；插件配置参考写"缺省 duo-harness/版本"口径含混。

**Judgement call（Fowler 基线）**

3. Duplicated Code：HttpClient 构建（connectTimeout 10s 魔法数 + Redirect.NEVER）在 WebFetchTool/TavilyProvider 两处同形；parameters() 内联 `new ObjectMapper()`（注：FsGrepTool 等仓库先例同款——仓库先例覆盖基线）。
4. Primitive Obsession：BodyReader.read 用 `boolean[] truncatedOut` 出参；killed 置位后的真实 IO 错误会被误报为超时（竞态窗口，语义可接受但形态糙）；maxResponseBytes 用 int 校验，>2GB 配置被误拒。
5. 小疵：WebPlugin/WebPluginAssemblyTest/TavilyProvider 内联 FQN 不 import；WebFetchTool.execute 约 120 行多段职责同居。

**正面**：package-info/测试镜像同 diff 就位、limitations 三条如实、demo yml 无 key 字面量（红线 2）、档位联动有 WebTierLinkageTest 实证。

### Spec 轴（原样分列）

总体：七张工单 Checklist 与 spec 11 条实现决策基本落地（配置面 11 字段缺省一致、SSRF 三道齐且 resolver 可注入、key 链/网络读三档含声明侧/并发声明/文档·CHANGELOG·limitations 三条均在）。

1. **maxBodyChars 语义三处口径不一（c）**：spec 决策 5 要求"解码后 100k 字符"，实现为"转换后施加上限"（工单 03 验收修复），《插件配置参考》仍写"解码后"。
2. **demo yml 违反工单 05"注释示例"且注释自相矛盾（a）**：`search:` 三行未注释直接生效，行内注释却写"去掉行首 #"；acceptance.md 说"解开注释段"。
3. **search 去重属 scope creep 且弱化截断语义（b/c）**：spec/ADR 只要求"工具层截断上限 8 条"；去重发生在截断后，结果可低于 maxResults，且截断实为 provider 侧参数，工具层无二次兜底。
4. **超时非单一 30s（c）**：实现为 10s 连接（硬编码）+30s 响应头+30s body 看门狗三段独立。
5. **搭车变更（b）**：WebPlugin optionalInject 修复 M19 权限档恢复缺口、duo-code-review experience.md 追加——均不在 M20 spec 22 条故事内（前者 CHANGELOG Fixed 已记账）。

其余偏差（UrlGuard 额外拒 CGNAT/TEST-NET/ULA、search 输出加 "Web search:" 头行）符合"保留段全拒"精神，不算问题。

**一轮小结**：Standards 5 项（硬违规 2）；Spec 5 项（缺口 1 / 矛盾 1 / 偏差 2 / 搭车 1）。双轴各自最重：Standards=注释过程叙述残留；Spec=maxBodyChars 语义三处口径不一。

## 处置（第 1 轮修复，2026-09-20）

| # | 发现 | 处置 |
|---|---|---|
| S1 | 注释过程叙述 ×4 | ✅ 已剪：保留设计 why（限额后置防 head 重量级空正文 / optionalInject 的错误前移机理），删"验收实测/M20 补齐"过程短语；另自查补剪 WebFetchTool 构造器"工单 01 临时形态/工单 02 换"陈旧叙述（双轴漏网、自审扫出） |
| S2 | UA 版本字面量漂移 | ✅ 已修：UA 去版本化（缺省 `duo-harness`，可配覆盖），配置参考/ADR 同步。拒绝 manifest 方案：为 UA 引入 maven-jar-plugin 打包配置不成比例（拒绝理由记录） |
| S3 | HttpClient 构建/魔法数重复 | ◑ 部分修：connectTimeout 改派生 `min(timeoutMs, 10s)` 消魔法数（两处）；builder 两处保留（形态小、语义各异——fetch 手动逐跳 vs search 静态）；parameters() 内联 ObjectMapper 沿用仓库先例（FsGrepTool 同款，仓库标准覆盖基线）不修 |
| S4 | boolean[] 出参 / killed 竞态 / int 校验 | ✅ 已修：`BodyReader.ReadResult` record 替代出参；maxResponseBytes 改 long 校验（>2GB 可配，新增回归用例）。killed 竞态接受：deadline 到点后的失败无论真因统一按超时报告，语义等价（记录不修） |
| S5 | FQN 不 import / execute 长方法 | ✅ 已修 FQN（WebPlugin/WebPluginAssemblyTest/TavilyProvider）；execute 不拆——抓取管线单流程内聚，拆段反增跳转（judgement，记录不修） |
| P1 | maxBodyChars 语义口径不一 | ✅ 已同步：插件配置参考改"转换后正文字符上限"、ADR-0021 决策 6 补"于转换后施加（验收调整）"；spec.md 为立项时点历史文档不回改，偏差已由工单 03 Comments 记录 |
| P2 | demo yml 未注释且自相矛盾 | ✅ 已修：search 三行恢复注释态（工单 05"注释示例"形态，解开缩进已验证），与 acceptance.md 指引一致 |
| P3 | 去重 scope creep / 截断弱化 | ✅ 去重保留（验收实测质量缺陷：tradingkey ×3，工单 05 Comments + ADR 决策 3 补记）；补工具层兜底截断（去重后 subList(maxResults)，截断权不依赖 provider 自律，新增用例）。"可低于 maxResults"合规——上限语义非恰数语义 |
| P4 | 超时三段独立 | ✅ 已修：connectTimeout 派生 `min(timeoutMs, 10s)`（常量命名），配置参考措辞同步（最坏 10s 连接 + 30s 头 + 30s body = 70s < 管线 120s） |
| P5 | 搭车变更 | ✅ 已记账不回退：WebPlugin 修复为 M19 缺口的用户可见缺陷（CHANGELOG Fixed），experience.md 追加是 duo-skill-evolution 约定动作 |

## 审查报告（第 2 轮·OCR）：ad240ba → 工作树快照 ab2c97c（含第 1 轮修复）

**覆盖**：40 文件全集。OCR 分三批执行（首批 10 个主代码文件合组超时，按 `--exclude` 拆三批重跑）：P1=8 文件 7 条、P2=9 文件 6 条（其中 BodyReader/HtmlToMarkdown/SearchSource/UrlGuard/WebPlugin 的第 2 轮因 LLM 超时部分缺失，第 1 轮意见已出）、P3=7 文件 3 条。测试与文档不在 OCR 名单——由双轴轮与两轮修复覆盖。合计 16 条，全部 low/medium（无 critical/high）。

### OCR 意见与处置（P1）

| # | 位置 | 级别 | 意见 | 处置 |
|---|---|---|---|---|
| 1 | WebFetchTool send:193 | bug·medium | 超时 RuntimeException 链 cause——rootMessage 取最深 cause，"[web_fetch 错误] 超时"精修指引永远不达模型 | ✅ 修：不链 cause（并顺带修复本轮自查发现的回归：限额后置重构时 body 阶段超时的 try/catch 丢失，补"头后 stall"mock 模式与用例） |
| 2 | WebFetchTool parameters | perf·low | 每次调用 new ObjectMapper | ⛔ 抑制：FsGrepTool 等仓库先例同款（仓库标准覆盖基线） |
| 3 | agent-demo.yml:14 | doc·medium | "config 可省"与内核 bindConfig 契约冲突（声明 config 类型须提供块） | ✅ 修：改"config 块须在场（可写空块），块内字段可省" |
| 4 | WebToolsPlugin:12 | doc·medium | "yml 一行 opt-in"与内核契约矛盾 | ✅ 修：改"yml 行声明 + config 块（可写空块 {}）即装配" |
| 5 | WebPlugin:62 | doc·low | "浏览器切档不可用"归因不符——/permission 由 CliPlugin 注册（ANY），可用性随 cli 行 | ✅ 修：收窄表述；CHANGELOG 同步修正（避免误导） |
| 6 | WebToolsConfig:98 | maint·low | 文本字段无 isTextual 前置校验，数字被 asText 静默转字符串 | ✅ 修：userAgent/search 四字段 textFieldOrNull 点名 |
| 7 | WorkspacePolicy:55 | maint·low | 工具名字面量与 NAME 常量双写 | ✅ 修（测试侧）：WorkspacePolicyTest 改用 NAME 常量钉契约；生产保持字面量避免 fs→web 包依赖成环 |

### OCR 意见与处置（P2）

| # | 位置 | 级别 | 意见 | 处置 |
|---|---|---|---|---|
| 1 | HtmlToMarkdown pre | bug·low | `<pre><span><code>` 深嵌套时 text() 折叠换行 | ✅ 修：wholeText() 取未归一化文本 + 用例 |
| 2 | BodyReader 看门狗 | bug·low | 进程级单线程看门狗内联 close——close 阻塞则后续 deadline 全部失效 | ✅ 修：close 移至独立虚拟线程执行 + 单点前提注释 |
| 3 | BodyReader 异常口径 | doc·low | FetchTimeoutException 消息不含时长，与工具层口径不一 | ✅ 修：构造器接收 deadlineMs 写进消息 + 断言 |
| 4 | UrlGuard:168 | security·low | 255 冗余条件注释误导；198.18.0.0/15（RFC 2544）未拒 | ✅ 修：注释修正 + 补段 + 用例 |
| 5 | HtmlToMarkdown 行内净化 | bug·low | 行内码含反引号提前闭合；链接 href 未过滤 javascript:/data: | ✅ 修：双反引号定界；href 仅放行 http(s) + 用例 |
| 6 | HtmlToMarkdown 表格 | bug·low | 单元格含 "|" 破坏行列对应 | ✅ 修：竖线转义 + 用例 |

### OCR 意见与处置（P3）

| # | 位置 | 级别 | 意见 | 处置 |
|---|---|---|---|---|
| 1 | TavilyProvider baseUrl | bug·low | 尾斜杠拼出 "//search" 运行期 404 | ✅ 修：构造期归一化尾斜杠 |
| 2 | TavilyProvider ofString | perf·low | search 响应体无上限全量读入 | ✅ 修：Content-Length 预检 + BodyReader 限量读（复用 maxResponseBytes），截断/超限报错 |
| 3 | WebSearchTool parameters | perf·low | schema 每次重新解析 | ✅ 修：静态 MAPPER（新代码取优写法；WebFetchTool 同款为既有家族模式，不追改） |

**二轮小结**：OCR 16 条全处置（修 15 / 抑制 1），新增回归测试 5 个（body 阶段超时、大字节上限、去重、工具层兜底截断、同站去重）+ 既有用例断言补强 3 处。

## 测试收口

`./mvnw test` 全仓 BUILD SUCCESS（2026-09-20）；duo-harness-tools 174、duo-harness-web 54 全绿。按技能约定不自动复跑 OCR。
