# 06: 后台任务可见化

## What to build

用户不查注册表也能知道有什么在后台飞：CLI 执行区提示行显示当前后台任务数与最近完成情况；Web 状态面新增后台任务区块（taskId/命令摘要/状态），随状态面刷新更新。本项是 04 后台任务的呈现完整性（ADR-0024 原文要求）。

决策依据：ADR-0025 决策二（可见化段）；ADR-0024 M23 节「用户侧后台任务可见化属本项呈现完整性」。

## Blocked by

04（可见的数据源是后台任务注册表）

## Status
done

## Comments
- 2026-09-22：实现与双轴审查完成，全量 BUILD SUCCESS（web 75 / cli 36）。Web 视觉验证待用户确认；CLI 提示行渲染抽纯函数直测（E2E 轮询竞态根源消除）。
- 2026-09-22：验收实测修正两项——①后台任务按发起呈现位归属过滤（复用 presenterId，CLI 与 Web 双开互不串显：CLI 触发的任务不再出现在 Web 新会话状态面，完成通知各归各位；无归属任务双面可见）；②清理 5 处 [dbg-*] System.err 调试残留。全量复跑 BUILD SUCCESS（web 77 / cli 36 / agent 176），CHANGELOG 补工单 06 条目。用户复验通过，转 done。另记：CLI 首验未现提示符计数段属操作路径（模型自行 task-output 等完任务，无「任务在飞+REPL 空闲」窗口），非缺陷。

## Checklist
- [x] CLI 提示行：后台任务数 + 最近完成提示，不干扰输入行
- [x] /api/status 扩展后台任务字段（id/命令摘要/状态），Web 状态面新增区块渲染
- [x] 任务结束/被 stop 后可见状态正确收敛（无幽灵条目）
- [x] 测试（先例 WebFaceTest seam）：状态字段、任务终态收敛
- [x] 工单级验收件：CLI + Web 双面观察后台任务演示，用户手动确认（2026-09-22 通过：CLI 通知排队合并消费符合设计；Web 归属过滤修正后复验通过）
- [x] CHANGELOG 未发布段记账

## 审查轮（2026-09-22，双轴，基点 c752f89）

**覆盖**：改动 3 代码文件 + 2 测试文件全审（覆盖率 100% 全集口径）

### 发现（已修/记档）

- **statusJson 后台区块**（已实现）：`backgroundTasks` 数组（taskId/command/state/完成带 exitCode）；注册表缺席无字段（fetch-only 同款零感）。
- **CLI 提示行**：`backgroundHint()` 纯函数（registry 快照→提示段），提示符前缀拼接；无任务空串零噪声。
- **终态收敛**：任务完成/被 stop 后 state 转 EXITED/KILLED，区块随之收敛（测试锁定）。
- **记档**：状态面 5s 轮询粒度下完成通知与状态刷新可能有秒级相位差（可接受）；区块样式复用既有 mono 类（视觉验证时如需微调属工单 11 打磨轮）。

### 测试覆盖

- WebFaceTest +1：/api/status 携带任务数组（运行中可见→stop 后收敛 KILLED，无幽灵 RUNNING）；注册表缺席不出现条目
- CliPluginTest +1：提示行计数出现（bg 运行中）→ 完成后回归零噪声（E2E，与 04 通知轮消费共用链路）
