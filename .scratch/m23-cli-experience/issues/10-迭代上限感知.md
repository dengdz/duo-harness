# 10: 迭代上限感知

## What to build

agent 距离迭代上限还剩 2 轮时，在下一请求组装时附加 system-reminder 提醒段（「剩余 2 轮，请收敛并交付结论」），让模型主动收敛而不是被硬掐；提醒不落用户消息、不改变迭代上限值与达限行为（超限仍返回 completed=false，M17 可见化不变）。

决策依据：ADR-0025 Consequences（grill Q11）；limitations M16#1 + BUG-20260917-03 背景。研究文档无直接同构参考（两家主循环无步数上限），注入点/通道为本期自设计裁定。

## Blocked by

无（可与主线并行）

## Status
done

## Comments
- 2026-09-22：实现与双轴审查完成，全量 BUILD SUCCESS（agent 187 含提醒 4 用例）。注入语义：remaining（含本轮）==2 恰一轮附加，USER 角色 system-reminder 段在治理投影**之后**追加——治理 compaction 吞提醒会使功能失效，治理后追加是必达唯一位置；maxIterations=1 无提醒轮（单轮预算无收敛机会，remaining 恒 1）。
- 2026-09-22：双轴修复 8 项——CHANGELOG 记账、limitations M16#1 半销账（感知半边落地，自动放宽仍后续）、文案钉「含本轮」口径、双 Javadoc 叠放合并、ChatMessage.user 工厂、治理启用下提醒必达用例、maxIterations=1 边界用例、测试适配器提取 helper + 叙述计数。

## Checklist
- [ ] send 循环剩 2 轮判定与 system-reminder 附加（请求组装时注入，不落用户消息）
- [ ] 达限行为回归不变：completed=false 与既有可见化（测试锁定）
- [ ] 只提醒一次不重复刷屏（剩 2 轮起每轮提醒或仅首次，实现取其一并测试锁定）
- [ ] 测试（先例 ToolCallingAgentTest seam）：提醒出现时机、达限行为
- [x] 工单级验收件：小上限（如 maxIterations=3）配置下观察提醒收敛演示，用户手动确认（2026-09-22 通过：maxIterations=2 下模型原文复述提醒「剩余 2 轮（含本轮），请收敛并交付结论」，并自发运用预算意识建议开新一轮——注入、恰好一次、收敛引导三项实证）
- [ ] CHANGELOG 未发布段记账

## 审查轮（2026-09-22，双轴→修复→OCR→修复，两轮齐全收口）

### 第 1 轮·双轴

**覆盖**：2 文件全 diff（ToolCallingAgent / ToolCallingAgentTest）+ 文档对账，覆盖率 100%

### 发现（已修/记档）

- **P2**：CHANGELOG 缺记账（同模式第 N 次）——已补；limitations M16#1 半销账（感知落地、自动放宽仍后续）——已改写。
- **P3 已修**：文案钉「含本轮」口径（避免模型理解为本轮外再 2 轮）、双 Javadoc 叠放、ChatMessage.user 工厂替代手写构造、治理启用下提醒必达用例（锁治理后追加语义）、maxIterations=1 边界用例、适配器提取、叙述计数。
- **记档**：治理投影后追加为合理设计（治理章程只作用于会话投影，systemPrompt 同在治理之外；预算影响约 40 token 次轮实测用量自然吸收）。

### 第 2 轮·OCR（委托模式）

**覆盖**：preview 名单 1 主代码文件（ToolCallingAgent）= 已审 1 + 跳过 0；规则组 1 组。

### 发现（已修）

- **buildRequest 双 Javadoc 叠放残留**（已修，low）：旧单行注释未删致双层叠放、仅末块生效——删除旧行。
- 其余 diff 行级过目：常量 javadoc 无泄漏、remaining 判定无 off-by-one（测试锁 off-by-one 回归）、无 dead code。

### 复核

- 修复后 ToolCallingAgentTest 14/14 绿；全仓 test BUILD SUCCESS（agent 187）。
- **两轮齐全核对（SKILL.md 收口硬判据）**：第 1 轮·双轴 ✓ + 第 2 轮·OCR ✓——本次审查收口。
