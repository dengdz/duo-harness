# 10: 迭代上限感知

## What to build

agent 距离迭代上限还剩 2 轮时，在下一请求组装时附加 system-reminder 提醒段（「剩余 2 轮，请收敛并交付结论」），让模型主动收敛而不是被硬掐；提醒不落用户消息、不改变迭代上限值与达限行为（超限仍返回 completed=false，M17 可见化不变）。

决策依据：ADR-0025 Consequences（grill Q11）；limitations M16#1 + BUG-20260917-03 背景。研究文档无直接同构参考（两家主循环无步数上限），注入点/通道为本期自设计裁定。

## Blocked by

无（可与主线并行）

## Status
ready-for-agent

## Checklist
- [ ] send 循环剩 2 轮判定与 system-reminder 附加（请求组装时注入，不落用户消息）
- [ ] 达限行为回归不变：completed=false 与既有可见化（测试锁定）
- [ ] 只提醒一次不重复刷屏（剩 2 轮起每轮提醒或仅首次，实现取其一并测试锁定）
- [ ] 测试（先例 ToolCallingAgentTest seam）：提醒出现时机、达限行为
- [ ] 工单级验收件：小上限（如 maxIterations=3）配置下观察提醒收敛演示，用户手动确认
- [ ] CHANGELOG 未发布段记账
