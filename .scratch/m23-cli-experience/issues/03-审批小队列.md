# 03: 审批小队列

## What to build

一次 turn 内多个工具先后请求审批时升级为排队应答：CLI 逐个呈现带「第 i/N」计数，y=允许、n 或直接回车=拒绝（空回车 deny 的 fail-closed 兜底），其他输入提示后仍视为拒绝；Web 端审批/提问卡片排队展示、真监听 Esc=deny。暂停/中断时队列剩余待答项全部合成 deny 移除，不留悬挂请求。决策枚举本期只做 allow/deny，「总是允许（项目/会话）」归 M24。

决策依据：ADR-0025 Consequences（grill Q8 键位与枚举）；ADR-0024 M24 节（权限规则引擎边界）；探测文档两家 审批与提问交互.md（ZCode approvalQueue 默认 deny / escape=deny 硬编码、DSH outcome 闭集与迟到答案丢弃）。

## Blocked by

02（中断合成 deny 依赖暂停语义）

## Status
done

## Comments
- 2026-09-22：**用户手动验收通过**（真终端实测）：连续三审批序号递增呈现（首项零噪声 → 第 2/3 项「本轮第 i 项审批」）、逐个 y/n 作答、审批等待中 /stop 逃逸闸门生效（余项 deny + 中断收口）、续接正常；Web Esc=deny 已由 browser-use 视觉验证通过。
- 2026-09-22：实现与两轮审查完成（报告见下），全量 BUILD SUCCESS；Web Esc=deny 视觉验证通过（browser-use：审批卡出现→Esc→卡冻结「✗ 已拒绝」→工具失败卡→模型收到拒绝继续）；CLI 序号与中断余项 deny 测试锁定。待用户手动验收后转 done。
- 审查修复 3 项：Esc 与 FIFO 最旧完成语义对齐（改拒最旧未冻结卡）、删零消费的 pendingAsks 预留、awaitLine/WebAnswerer 打断不重设线程标志（防审批审计事件落盘被 NIO 炸——工单 02 模式 1 在交互链路的重现）。
- **审查发现的设计缺陷（已修）**：应答等待期 /stop 被闸门吞作应答（deny 的"回答"）——中断入口被挡死；修法 = 斜杠行逃逸闸门走 busy 分发。代价：自由文本答案以 / 开头会被命令分发吞掉（未知命令报错）——罕见场景记档（无转义语法，需要时答非斜杠表述）。
- 记档不修（串行架构下不可达，M24 多标签前无消费方）：多卡在队时 POST 按位置（最旧）回填而非按卡片身份关联；Web 卡无显式次序指示（事件堆叠隐含）。

## Checklist
- [x] CLI answerer 升级队列形态：pending 计数「第 i/N」逐个呈现，y/n/空回车语义，答完即进下一个（实现为「本轮第 i 项」i≥2 显示——串行架构下 N 恒 1 无信息量，i 计数保留）
- [x] Web answerer 由单 Pending 升级排队，卡片 Esc=deny，排队展示次序清晰（FIFO 队列 + 事件堆叠渲染；Esc 拒最旧未冻结卡）
- [x] 中断/暂停时余项全部合成 deny 移除（测试覆盖，无悬挂请求）
- [x] 亲和路由（谁发起谁作答）在队列形态下不回退（测试锁定）
- [x] 测试（先例 InteractionRegistryTest / ConsoleAnswererTest / WebAnswererTest seam）：队列时序、键位语义、fail-closed
- [x] 工单级验收件：连续多审批排队作答 + 中断清队演示，用户手动确认（2026-09-22 通过）
- [x] CHANGELOG 未发布段记账

## 审查轮（2026-09-22，双轴 + OCR 委托，基点 f1ae65b）

**覆盖**：代码文件 + 4 测试文件全审（测试由双轴覆盖；覆盖率 100% 全集口径）

### 阻断（已修）

- **CHANGELOG 未记账**（红线 6，Standards 轴）——已补 Changed 条目。
- **Esc 与 FIFO 语义错位**（双轴同点）：前端 Esc 拒最新未冻结卡 vs complete() 完成最旧——多卡时视觉与语义错位。修复：Esc 改拒最旧（单卡场景等价，多卡对齐 FIFO）。
- **交互链路的残留标志炸 NIO**（实现中发现）：awaitLine/WebAnswerer 的 InterruptedException catch 重设线程标志 → 审计桥落 approval/decided 的 NIO 写炸（「会话事件落盘失败」实测复现）。修复：两处不重设——中断语义由 interruptRequested 布尔承载（工单 02「唤醒即清」纪律在交互链路的落实）。

### 建议（处置）

- **pendingAsks() 零生产消费**（两轴同点，Speculative Generality）——已删（review-log「能力与消费方匹配」模式）。
- **/stop 逃逸闸门的设计代价**：自由文本答案以 / 开头被命令分发吞——记 Comments 档（罕见场景；加转义语法留待真需求）。
- 测试「归零」改为同实例验证 beginTurn 真复位——已修。

### 测试覆盖

- WebAnswererTest +2：并发排队 FIFO 完成最旧、中断清队不污染后续
- ConsoleAnswererTest +1：审批序号呈现（首项零噪声、第 2 项起序号）与 turn 归零（同实例复位验证）
- InteractionRegistryTest +1：并发 ask 各归各回答者不串位（队列形态下亲和不回退）
- CliPluginTest +1：审批等待中 /stop → 逃逸闸门 → 余项 deny（approval/decided deny + 中断标记落日志）
- 视觉（红线 5）：browser-use 实拍 Esc=deny 全链路（卡冻结「✗ 已拒绝」→ 工具失败卡 → 模型继续）；Esc 改点最旧后单卡场景等价无需重验
