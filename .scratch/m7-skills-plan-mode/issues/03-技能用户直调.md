# 03: 技能用户直调

**What to build:** 用户点名即生效：AgentRepl 输入以 `/` 开头且首段匹配已注册技能名时，该次 send 的用户消息前面注入技能指令全文（技能指令 + 空行 + "用户输入：其余内容"），模型当轮按技能行事；不匹配技能名的 `/` 输入维持原行为（未知命令提示）。极简文本前缀注入，不做通用命令框架。

**Blocked by:** 01（技能系统核心——需要 SkillRegistry 按名查询）

**Status:** implemented（2026-09-13，待用户验收）

## Checklist

- [x] `/name` 前缀识别：首段精确匹配 SkillRegistry 已注册技能名
- [x] 注入形态：指令全文 + 空行 + "用户输入：<其余内容>"（无其余内容时仅指令全文）
- [x] 不匹配的 `/xxx` 输入给出未知命令提示（列出可用技能名），不发送给模型
- [x] 测试（AgentReplMainTest 风格）：直调命中注入 / 未知名提示 / 无其余内容形态（resolveSkillInvocation 静态方法 4 断言）
- [x] 文档同步：运行Demo.md M7 段含直调示例（随工单 05 统一收口）

## 实现记录（2026-09-13）

- AgentReplMain：resolveSkillInvocation 静态方法（可测）+ 主循环接线（/exit /new 内置命令优先于技能名）+ AgentSkillsView 视图；未知名提示列出可用技能
- 内置命令优先约定入档：/exit /new 由 REPL 先行处理，技能名不得与之重名（重名被内置命令遮蔽）

## Comments

spec：[../spec.md](../spec.md)。grill 定案：文本前缀注入而非"注册表临时片段"（后者需要带生命周期的片段机制，复杂度不成比例）。
