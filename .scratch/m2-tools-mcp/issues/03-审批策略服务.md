# 03 — 审批策略服务（ask 三态）

## What to build

工具执行的审批语义：pre-execute 的决策从两态扩为三态（allow / deny / **ask**）——治理插件或工具可声明"此调用需要审批"；`ask` 委托**审批策略服务**裁决。M2 交付两个预设策略：`always-deny`（默认，未配置即拒，错误结果注明"被审批策略拒绝"）与 `auto-approve`（按配置白名单放行）。审批决策（决策结果 + 策略来源）有日志可查（安全审计）。MCP 工具与本 地工具走同一审批路径——审批策略一旦配置，远端工具的调用同样被治理。完成的判据：配置 always-deny 后工具调用返回带策略来源的错误结果；切 auto-approve + 白名单后放行；决策日志可查（接缝 B）。

## Blocked by

None — can start immediately（tools 模块已存在，与 MCP 线无依赖）

## Status

ready-for-agent

## Checklist

- [ ] 审批策略服务接口（ask → 放行/拒绝 + 理由）+ 两个预设实现（always-deny 默认 / auto-approve 白名单）
- [ ] 策略服务以服务形式发布，yml 可配置选择预设与白名单
- [ ] pre-execute 的 ask 委托改造：决策三态贯通，审批拒绝 → error 结果（含策略来源）
- [ ] 审批决策日志：结果 + 策略来源 + 工具名（L-04 占位符规范）
- [ ] 接缝 B 测试：always-deny 拒绝且日志可查；auto-approve 白名单内外行为差异；MCP 工具与本地工具同路径
