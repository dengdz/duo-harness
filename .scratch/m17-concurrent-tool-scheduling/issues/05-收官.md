# 05: 收官——CHANGELOG、limitations 终审与验收件

## What to build

0.12.0 版本收口：CHANGELOG 记账、limitations 全清单终审、README 能力核对、真机验收三件、CI 绿与双轴审查（ADR-0016 里程碑仪式：每期合并前 CI 必绿 + 双轴审查）。

## Status

ready-for-agent

## Blocked by

01、02、03、04 全部

## Checklist

- [x] CHANGELOG 0.12.0 段记账：并发调度、管线缺省超时、todo_write 分解抓手、Web 迭代上限可见化（用户可见变更同 diff 记账，红线 6）
- [x] limitations 终审：M15#6 销账（分解抓手已落地）；遗留 #2/#3 复审执行（工具并发落地后 bail/serial 等价性与容器 recheck 风险面重评，结论留痕）
- [x] README 能力清单核对：并发执行 / 工具超时 / todo 分解入册
- [x] 真机验收三件：多 read 并行墙钟对比（并发可感）、Web todo 面板视觉走查、迭代上限错误卡复现（用户 2026-09-18 真机验收通过，验收途中发现并修复两缺陷：超时迟到覆盖竞态、todo 面板跨会话残留）
- [x] CI 绿（0.12.0 分支 push 触发）+ 双轴审查（Standards + Spec，M15 起惯例）
- [ ] 版本收口：pom 十模块版本升 0.12.0，合并回 main（经用户确认）

## Comments

- 2026-09-18 双轴审查（独立子代理，f964d84..HEAD 全 diff，含 61 用例复跑验证）：裁定"需修复后合并"。P1 三项处置——app.js todoCard 头行摘要解析 bug（tool/call 的 text 是 `{"todos":[...]}` 对象非裸数组，原断言恒 false 致头行恒空，视觉验收因面板与结果体兜底未察觉；已修并待用户视觉复验）；agent/todo 补 package-info.java；CHANGELOG/README 记账（审查基线为 HEAD 未见工作区改动，实际本工单已做）。P2 处置——两处格式事故（JavaDoc 挤行/注释同行）修复；超时判定面 fail-closed 统一（resolveTimeout 加 catch 回落缺省）；CliPlugin/PresenterAssembly 硬编码与全限定名风格统一；补两条证据缺口测试（5 工具 × 并发度 2 的滚动池在飞峰值上限与排队补位、并行组内单工具超时其余照常 model 序成对提交——01+02 交汇场景）；工具目录 fs 六件补并发标注；limitations M15#2 文字更新（分解抓手已交付）；ADR-0018 补"执行中反馈随提交点延迟"的可见性代价注记；backlog 记"双呈现位叠挂超时监听器"（不阻塞）。P2 接受现状——CLI 两行呈现（工单 04 注记细化可辩护）、墙钟断言余量、静态 EXECUTOR 常驻（JavaDoc 已注记）。审查者对 M15#6 的"销账对象悬空"为误报（其读到的是工作区已删除后的版本）。处置后全量 verify BUILD SUCCESS。
- 真机验收详情见工单 04 Comments（todo 双表面走查 + 并行墙钟 + 迭代错误卡，三项通过）。
