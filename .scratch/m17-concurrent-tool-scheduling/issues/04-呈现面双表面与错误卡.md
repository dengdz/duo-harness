# 04: 呈现面——todo 双表面、CLI 摘要与迭代上限错误卡

## What to build

用户看得见 agent 的计划与进度、Web 不再无提示停住（ADR-0018 决策 6/7）：Web 会话流工具行显示 todo 摘要（"done/total 已完成 · 活动任务"），输入框上方常驻折叠面板展开为圆圈状态清单（带勾实圆 = 完成、圆环 = 进行中、虚线圆 = 待办，空清单不渲染）；CLI 在 todo_write 调用时打一行计数摘要（跟随既有工具行风格）；agent 达迭代上限返回未完成时，Web 面直推一帧 run/error 错误卡（补 BUG-20260917-03 验收 B 实测缺口：页面无提示地停住）。

## Status

done（2026-09-18，用户真机验收通过：todo 双表面走查 + 迭代错误卡复现；验收途中发现并修复清单跨会话残留 bug）

## Blocked by

03（消费 todo/write 事件与投影）

## Checklist

- [x] Web 工具行摘要：todo_write 调用行渲染 done/total 与活动任务内容
- [x] Web 常驻折叠面板：输入框上方、默认折叠一行进度摘要、展开圆圈状态清单、空清单不渲染
- [x] CLI 计数摘要行：todo_write 调用时终端一行（"清单已更新：X 待办 / Y 进行中 / Z 已完成"风格）
- [x] 迭代上限错误卡：completed=false 时 Web 经 /api/message 路径直推 run/error 帧，复现 BUG-20260917-03 场景（页面无提示停住 → 出现错误卡）
- [x] 测试（WebFace HTTP 交换层 seam）：todo/write 事件到前端的数据通道、run/error 帧下发
- [x] 手动验收（duo-acceptance）：Web 双表面视觉走查 + CLI 摘要走查（红线 5：UI 改动必须视觉验证后才算完成）

## Comments

- 2026-09-18 实现完成：Web 三面——`todoCard` 工具行摘要卡（头行 done/total + 活动任务，徽标与结果回填走通用 tool/result 路径）、`todoPanel` 常驻折叠面板（输入框上方 `<details>` 默认折叠，展开为圆圈三态清单：完成实圆带勾 / 进行中品牌色呼吸圆环 / 待办虚线圆，空清单 hidden）、dispatch 加 `todo/write` 分支与 `user/message` 清空（与 `Session.todoProjection` 同语义，回放经单源分发自然重建终态）。CLI 两行——onToolCall 打 `[清单] 更新任务清单…`（不刷整表 JSON）、onToolResult 打 `[清单] 清单已更新：…`（计数文本即结果）。迭代上限错误卡——`handleMessage` 检查 AgentReply.completed()，false 时 `pushTransientFrame(run/error)`（直推不落会话，与异常路径同口径；前端 runError 渲染与 clearSendBusy 均已存在）。
- 测试证据：`WebFaceTest` 新增 2 用例（迭代上限推 run/error 帧且携带失败说明；todo/write 事件帧经尾窗快照完整往返——非投影事件随区间推送）；全量 `mvn verify` BUILD SUCCESS。
- 待办：手动验收（视觉走查）进行中——验收件见 spec Further Notes。
- 2026-09-18 用户真机验收发现 bug 并已修复：A 会话的 todo 清单在 `/new` 新建会话后残留——`resetToHero` 只清消息区不清 todoPanel（切换会话路径经 tail-snapshot 全量回放可自然重建，`/new` 不经回放故残留）。修复落在 `resetForReplay`（整窗替换的公共基线，/new 与切换两路径同治；增量重连不走它、面板正确保留）。前端无 JS 单测，语法经 node --check，行为验证回归用户真机走查。
