# 03: 切换与新建无刷新

## What to build

切换会话与新建会话不再整页重载：前端主动断开 SSE → 调既有换绑端点 → 重连 SSE 收新会话尾部快照 → 整窗替换；输入框与页面状态保留。`location.reload()` 删除（两处）；会话变更回调重建 agent 的换绑 + 独占锁语义不变。

## Blocked by

01（尾部窗口快照——重连首连即尾部快照）

## Status
done（2026-09-16 内置浏览器验证通过——切换无整页刷新（URL 不变）、输入框清空、内容切到目标会话尾部、侧栏高亮与标签页标题跟随；切换后发消息正常（BUG-20260916-01 修复一并验收））

## Checklist
- [x] 切换流程改造：断 SSE → POST switch → 重连 → 整窗替换；侧栏高亮与状态面随新会话刷新
- [x] 新建流程改造：POST new → 重连 → 空态（EmptyHero）
- [x] 删除两处 location.reload()
- [x] 用例：切换换绑语义回归（WebFaceTest——agent 重建、独占锁、id 白名单校验不回归）
