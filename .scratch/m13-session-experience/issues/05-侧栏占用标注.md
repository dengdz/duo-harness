# 05: 侧栏占用标注

## What to build

侧栏一眼分清哪些会话能安全切换：`/api/sessions` 逐会话探测锁状态新增 `occupied` 字段，前端把被占会话灰显 + "使用中"角标；点击行为不变（撞锁明确报错语义保留）。

## Blocked by

None（可立即开工，与 01 并行）

## Status
ready-for-agent

## Checklist
- [ ] sessions 接口锁探测（tryLock）与 occupied 字段；探测失败的降级（不标注，不报错）
- [ ] 前端灰显 + 角标渲染
- [ ] 用例：占用/空闲两态的 occupied 断言（WebFaceTest 先例，双会话夹具）
