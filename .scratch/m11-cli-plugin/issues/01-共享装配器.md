# 01: 共享装配器（prefactor）

## What to build

WebPlugin 内联的执行链装配（LLM 配置 → 重试 adapter → 治理 → ChatAgent 构建 → 交互工具查重注册）提取为 agent 模块的共享单点；WebPlugin 切换到共享装配器后**行为零回归**——为 CliPlugin（工单 02）复用铺路："先让改动变容易"。

## Blocked by

无（可立即开工）

## Status
done

## Checklist
- [x] agent 模块新增共享装配器：入参（LlmConfig、ToolsService、PromptRegistry、会话供给、注册上下文等），出参（ChatAgent + 治理 + 交互工具查重注册）
- [x] WebPlugin 切换到共享装配器，内联装配删除
- [x] 回归：WebPluginAssemblyTest、WebFaceTest、AgentReplMainTest 全绿（装配行为不可见变化）

## Comments

- 实现（2026-09-15）：agent 模块新子包 `presenter`（package-info 齐备）——`PresenterAssembly` 四静态工厂（llmAdapter / governance / chatAgent / registerInteractionTools，查重先到先得收口单点）；WebPlugin 切换为委托装配器，内联装配与私有 registerInteractionTool 删除。
- 审查（委托 OCR + 双轴）修复：① `llm.internal` 渗透 agent 模块（模块划分"internal 即勿用"）——按结构规范"契约包静态工厂"补 `llm.LlmAdapters.openAiCompatWithRetry`，agent 不再触 internal；② WebPlugin 四处 FQN 改 import；③ 重试参数从固定默认切配置驱动的行为变化（默认值两侧核对一致，仅非默认 `llm.retry` 配置生效）记入 CHANGELOG 0.6.0。
- 验证：PresenterAssemblyTest 3/3（装配产物执行一轮落事件 / 交互工具二次注册跳过 / 配置装载失败点名）；全量 285 用例绿（含 WebPluginAssemblyTest、WebFaceTest 26、AgentReplMainTest——工单要求的回归证据）。
- 待手动验证：用户确认后置 done（本工单为 prefactor，无独立用户可见行为——回归绿即实质完成，可与 02 合并验收）。
- 验收通过（2026-09-15，用户合并验收 M11 三单：CLI 终端交互/新建/退出 idle/Web 存活/锁互拒）。