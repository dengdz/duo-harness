# 01: 共享装配器（prefactor）

## What to build

WebPlugin 内联的执行链装配（LLM 配置 → 重试 adapter → 治理 → ChatAgent 构建 → 交互工具查重注册）提取为 agent 模块的共享单点；WebPlugin 切换到共享装配器后**行为零回归**——为 CliPlugin（工单 02）复用铺路："先让改动变容易"。

## Blocked by

无（可立即开工）

## Status
ready-for-agent

## Checklist
- [ ] agent 模块新增共享装配器：入参（LlmConfig、ToolsService、PromptRegistry、会话供给、注册上下文等），出参（ChatAgent + 治理 + 交互工具查重注册）
- [ ] WebPlugin 切换到共享装配器，内联装配删除
- [ ] 回归：WebPluginAssemblyTest、WebFaceTest、AgentReplMainTest 全绿（装配行为不可见变化）
