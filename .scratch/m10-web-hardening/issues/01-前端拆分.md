# 01: 前端拆分（prefactor）

## What to build

现有单文件静态页（约 724 行的 HTML+CSS+JS 混排）拆为 index.html + app.js + theme.css 三个 classpath 静态资源，浏览器加载与视觉行为完全不变。本单是后续所有前端工单（07/08/09）的地基："先把改动变容易，再做容易的改动"。

## Blocked by

无（可立即开工）

## Status
done

## Checklist
- [x] 拆为三文件，静态页经 classpath 正常加载（静态页可达测试绿）
- [x] 视觉与交互零变化：拆前截图留档，拆后 browser-use 截图对照（注意 IAB 物理点击不可信，用 evaluate 合成事件）
- [x] 既有测试全绿（跑前先杀 18080 监听进程）

## Comments

- 实现（2026-09-14）：新增 `/web/` 前缀白名单静态资源 context（单段文件名 + 已知后缀，多段/穿越/未知后缀 404）；index.html 拆为 index.html（骨架 34 行）+ theme.css（173 行）+ app.js（507 行），CSS/JS 原样搬运不重排。
- 验证：WebFaceTest 8/8（含新增 servesSplitStaticAssets / rejectsUnsafeOrMissingStaticAssets 两用例，红-绿流程）；全量 `mvn -o test` BUILD SUCCESS；实跑探针确认 stylesheet/script 加载、三区渲染、SSE 回放、侧栏与状态面数据齐全。截图对照见 `../screenshots/01-baseline-pre-split.png` 与 `01-post-split.png`。
- 待手动验证：用户亲跑确认视觉与交互零变化后置 done。
