# 10: 收官：文档同步与验收件

## What to build

里程碑收口：文档与代码同 diff 同步、全量回归、验收三件套备齐，交付用户手动验收。

## Blocked by

02, 03, 04, 05, 06, 07, 08, 09

## Status
in-progress

## Checklist
- [x] M8 遗留文档不符修正（WebPlugin javadoc 声称"LLM 缺失时状态面仍可看"与整插件 FAILED 的实际不符）
- [x] docs 同步：插件配置参考等受影响文档更新；`cd docs && npm run docs:build` 绿
- [x] 全量 `mvn -o test` 绿（跑前先杀 18080 监听进程）
- [x] 验收三件套：可运行演示 + 详细过程日志（演示与测试两路输出）+ 预期输出对照表——覆盖 M10 全部用户可见行为（MD 渲染、反馈态、占用展示、增量回放刷新对比、会话锁报错、纯 Web 提问卡）
- [x] CHANGELOG.md 未发布段记账（用户可见变更）

## Comments

- 执行（2026-09-14）：
  - **文档同步**：WebPlugin javadoc 修正（原"LLM 未配置时状态面仍可看"与实际整插件 FAILED 不符）+ 补 M10 行为（SSE 游标、会话锁、HITL 工具补全）；插件配置参考 web 行补装配行为；运行Demo Web 段补 M10 用户可见行为（MD 渲染/状态机/占用行/刷新不误杀/toast/安全基线）。
  - **CHANGELOG**：0.5.0（未发布）段记账——8 条 Added + 2 条 Changed，覆盖 M10 全部用户可见变更。
  - **测试套件叙述补齐**：全仓 45 个测试类均带套件叙述行（本次补 6 个缺失类 + 修正 WebFaceTest 过期叙述）；用户跑 `mvn test` 时每套件可见"正在验证什么"。
  - **验收三件套**：`acceptance.md`（本目录）——运行命令、预期日志 A（测试路径，7 条 M10 相关套件叙述）、预期日志 B（演示路径实测快照，按验收点分段：会话独占/SSE 连接/连线对话操作对照表）、视觉留档 `screenshots/10-acceptance-final.png`。
- 验证：全量 `mvn -o test` 281 用例绿；docs:build 绿；实测快照采集自真实运行（占用行 3,321 / 128,000 · 实测、MD 渲染 h2/列表/代码块、状态机「思考中…」→恢复）。
- 待手动验证：用户按 acceptance.md 跑通后置 done。