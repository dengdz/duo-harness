# 09: 交互反馈与错误可见化

## What to build

页面永远诚实：发送消息有禁用与加载态（不再连点重发、不再干等无反馈）；任何请求失败在页面上可见（不再静默吞错），用户能判断该重试还是该报 bug。

## Blocked by

08（同在 app.js，串行避免同文件冲突）

## Status
done

## Checklist
- [x] 发送按钮禁用 + 加载态（请求进行中或 agent busy 时）
- [x] 全局错误提示（toast 或等效）：消息发送非 busy 错误、会话列表/状态刷新失败均可见
- [x] 现有静默吞错路径全部改为可见处理（走查确认 + 冒烟）

## Comments

- 实现（2026-09-14）：toast 全局提示（右下角、5s 自动消失、error/info 两级；持续故障节流只报一次防 5s 轮询刷屏）；发送按钮状态机（受理中"…"→ 202 后"思考中…"，SSE 的 user/message 回显、assistant/message 完成、replay/done、重连 onopen 四路幂等解除，重入防护）；吞错点走查五处全部可见化（refreshStatus 节流 toast、refreshSessions toast、send 网络/非 409 状态码、newSession 与 switch 的失败分支）；409 提示从对话流迁移到 info toast（行为迁移，收口记账）。
- 冒烟抓到并修复一个集成 bug：clearSendBusy 未导出进 app 返回对象——解除点全部失效、按钮卡死"思考中…"（首轮冒烟即暴露，修复后重验通过）。正常流：受理禁用→完成恢复、零误报 toast；故障流（停服）：网络错误 toast 立即可见、按钮恢复、5s 自动消失无堆积。
- 审查：ocr 端点 403 全批失败（如实记录），手工维度补位走查——状态机出口完备、toast 用 textContent 无注入面、吞错点五处闭环、无新增监听器泄漏，无 blocker。
- 待手动验证：用户亲跑确认后置 done。
- 验收通过（2026-09-14），采纳用户反馈：toast 位置由右下角调整为顶部居中。
