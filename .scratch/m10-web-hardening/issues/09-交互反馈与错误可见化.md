# 09: 交互反馈与错误可见化

## What to build

页面永远诚实：发送消息有禁用与加载态（不再连点重发、不再干等无反馈）；任何请求失败在页面上可见（不再静默吞错），用户能判断该重试还是该报 bug。

## Blocked by

08（同在 app.js，串行避免同文件冲突）

## Status
ready-for-agent

## Checklist
- [ ] 发送按钮禁用 + 加载态（请求进行中或 agent busy 时）
- [ ] 全局错误提示（toast 或等效）：消息发送非 busy 错误、会话列表/状态刷新失败均可见
- [ ] 现有静默吞错路径全部改为可见处理（走查确认 + 冒烟）
