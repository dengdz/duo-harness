# 02: 安全与装配加固

## What to build

本机工具的三处防线 + 纯 Web 部署的 HITL 补全：非法会话 id 与超大请求体被直接拒绝、错误响应不再回显内部异常细节；只有 Web 部署（无终端）时，模型的提问与计划呈交照常弹出交互卡片并可作答——HITL 能力不再依赖 CLI 装配在场。

## Blocked by

无（可立即开工）

## Status
done

## Checklist
- [x] 会话切换 id 按格式白名单校验，路径分隔符与 `..` 一律拒绝（测试含目录穿越用例）
- [x] POST 请求体设大小上限，超限拒绝（测试覆盖）
- [x] 404/500 响应体改通用描述，不回显异常消息（异常细节仅服务端控制台）
- [x] `/api/sessions` 端点测试补齐（M8 零覆盖）
- [x] WebPlugin 装配链补注册 ask_user 与计划呈交工具（对齐 CLI 装配清单，装配测试断言工具清单）
- [x] Web 冒烟：纯 Web 装配下提问卡/计划卡弹出并可作答

## Comments

- 实现（2026-09-14）：WebFace 增 `MAX_BODY_BYTES=1MB`（readNBytes 上限+1 读取防内存放大，message/switch/answer 413、session/new 清空同限）与 `SESSION_ID` 白名单正则（Session.newId 生成形态，switch 全匹配校验 400）；session/new 与 switch 的错误响应脱敏（通用文案，异常细节服务端控制台留痕）；WebPlugin 装配链注册 AskUserTool 与 ExitPlanModeTool（会话经 face::currentSession 延迟解析；查重先到先得）。
- 审查抓到并修复一个 blocker：demo 主路径（agent-demo.yml）下 CLI 装配与 WebPlugin **双注册** ask_user/exit_plan_mode → 内核"拒绝重复注册"抛异常，启动炸（ToolCatalogTest 首先炸出）。修复：装配层查重先到先得，WebPlugin、AgentReplMain、ToolCatalogTest（复刻语义）三处同步。
- 审查（委托模式，ocr 模型过期按新策略降级）：preview 5 文件 + 3 测试文件手工补审全覆盖。JS/Java 规则组与 default 组全部通过，无 blocker；两条 low——SESSION_ID 与 Session.newId 的格式锚点分散两处（注释已互锚）；registerIfAbsent 在 WebPlugin/AgentReplMain 重复（跨模块提取成本高于重复）。
- 验证：WebFaceTest 14/14（switch 穿越/杂串 400、合法不存在 404 无内部回显、413、/api/sessions 形态）、WebPluginAssemblyTest 纯 Web 装配断言两工具在册；全量 `mvn -o test` 267 用例绿；HITL 闭环冒烟——提问卡弹出（问题+选项）、点选"咖啡"、卡片冻结、模型续答确认，期间还实证了断连 fail-closed 语义（tab 释放 → 悬空提问被拒）。
- 待手动验证：用户亲跑确认后置 done。
