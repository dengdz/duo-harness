# 07: 上下文占用展示

## What to build

状态区新增一行"上下文：输入 N + 输出 M / 窗口 W（占比 %）"，与治理阈值同屏对照——M9 治理成果对用户可见，数字与治理判定同源同口径（真实 usage 优先，估算兜底时口径不混淆）。

## Blocked by

01（前端结构）、06（数据源）

## Status
done

## Checklist
- [x] /api/status 响应含上下文占用字段，与治理取数同源（JSON 形态测试）
- [x] 状态区渲染占用行与阈值对照（浏览器截图验证）
- [x] 估算兜底值照常显示且可分辨口径（无真实 usage 时不冒充真实值）

## Comments

- 实现（2026-09-14）：agent 域新增 `ContextOccupancy` record 与 `ContextGovernance.occupancy(session)` 只读查询——与 compaction 判定同一段取数（latestUsage 倒查 + 阈值/窗口常量，`contextThreshold()` 提取共用），真实值优先、投影估算兜底；web 域 `WebFace.start` 注入 governance（可 null，无治理时 `context` 字段缺席——接口向后兼容），`/api/status` 增 `context: {tokens, thresholdTokens, windowTokens, fromProvider}`；前端状态区顶部新增"上下文"块渲染 `N / W tokens（pct%，压缩阈值 M · 实测|估算）`，估算口径显式标注，超阈值红色临界态。
- 验证：WebFaceTest 10/10（含占用暴露 2 用例）、治理 occupancy 2 用例，全量 `mvn -o test` BUILD SUCCESS（52 个测试类）；实跑 `/api/status` 返回 `{"tokens": 3298, "thresholdTokens": 102400, "windowTokens": 128000, "fromProvider": true}`（正是工单 06 冒烟那轮的真实值——续接会话倒查生效）；页面渲染探针 + 截图见 `../screenshots/07-context-occupancy.png`。
- 待手动验证：用户亲跑确认后置 done。
