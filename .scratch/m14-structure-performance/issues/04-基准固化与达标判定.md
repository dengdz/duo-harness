# 04: 基准固化与达标判定（含 limitations M13#3 消账）

## What to build

框架维护者获得可复跑的性能基准与达标判定（ADR-0014 决策 3/4）：grill 期 80K 事件临时探针转正入库（默认跳过，不拖全量套件，手动触发可跑），优化后数字落档 ADR-0014 与验收件，判定单次"分页定窗 + 投影回放"≤ 15ms 达标线。兑现 M13 债务承诺：limitations M13#3（"增量投影 / O(n) 单趟优化属 M14"）**同 diff 删除**，CHANGELOG 同 diff 记账（红线 3/6）。

## Blocked by

02 事件快照 CoW 零拷贝、03 消息窗口单趟化（基准测的是两项优化后的最终数字）

## Status

done（2026-09-16 用户验收通过——基准 7.60ms 达标（线 15ms，审查期计时修正后复测）；默认跳过形态验证；M13#3 消账 + CHANGELOG 记账）

## Checklist
- [x] 基准探针转正：80K 事件程序化生成（JSONL 直写 + load）+ 计时，默认跳过（`Skipped: 1` 已验证）、`-Dperf.benchmark=true` 手动可跑
- [x] 优化后数字实测落档：ADR-0014 实测验收注记 + 本工单验收记录（对照 grill 基线 0.13 / 8.78 / 8.31ms）
- [x] 达标判定：80K 单次"定窗 + 投影回放" ≤ 15ms——**复测 7.60ms，达标通过**（首次 9.00ms 的计时含整数截断偏差，审查期修正）
- [x] limitations M13#3 同 diff 删除 + CHANGELOG 0.9.0 段同 diff 记账（01-04 条目）

## 实测数字（2026-09-16，M 系列 Mac，20 轮均值，预热 3 轮）

| 指标 | grill 基线（优化前） | 实测（优化后） | 变化 |
|---|---|---|---|
| `events()` | 0.13ms | 共享引用（O(1)） | 拷贝消除 |
| `tailWindow(50)` | 8.31ms | 2.11ms | -75% |
| `deriveMessages()` | 8.78ms | 7.48ms | -15% |
| **定窗 + 投影** | ~17.1ms | **7.60ms** | **-56%，≤ 15ms 达标 ✓** |

## 验收指引（用户手跑）

```bash
# 1) 全量套件中基准默认跳过（应见 Skipped: 1，零耗时）
mvn -pl duo-harness-session -am test -Dtest='SessionTest,SessionPerfBenchmarkTest' -Dsurefire.failIfNoSpecifiedTests=false

# 2) 启用基准复跑数字（预期 80K 定窗+投影 ≈9ms，达标断言 15ms 通过）
mvn -pl duo-harness-session -am test -Dtest=SessionPerfBenchmarkTest -Dperf.benchmark=true -Dsurefire.failIfNoSpecifiedTests=false

# 3) 全量
mvn test
```

预期：全绿。数字有 ±2ms 环境波动属正常（达标线留有 6ms 余量）；若显著超出 15ms 说明环境异常或回归，报我排查。
