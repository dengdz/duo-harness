# 06: 状态面、装配打磨与验收对照

**What to build:** M8 收官件：右状态区实时刷新接线（快照表 + 工具清单，SSE/定时刷新）、agent-demo.yml 的 web 行接线（全量装配：技能 + AGENTS.md + 交互 + 计划模式 + Web 同场）、CLI 与 Web 双入口并存的使用说明、七场景验收对照表、文档同步。验收：浏览器走通对话 / 审批 / 提问 / 计划复核全链路，CLI 面回归不受影响。

**Blocked by:** 01, 02, 03, 04, 05（集成单，全部在场）

**Status:** done（2026-09-14 用户裁定按现状收官——开发与自动化验证完成（213 例全绿），手动验收部分通过：三轮验收发现的缺陷已全部根治（BUG-20260913-04、BUG-20260914-01/02），仍存遗留问题转持续使用反馈；理解关卡延后，见 comprehension-pending.md）

## Checklist

- [x] 状态区：/api/status 渲染插件六态表 + 工具清单（含 MCP 远端工具与 skill 工具），SSE 事件触发刷新
- [x] 会话侧栏（**范围收缩已取消**：初版记档"Session.list 批量 API 与 /api/sessions 未做"，后补齐——`/api/sessions` 列表（含 current 标记）+ `/api/session/switch` 切换 + ＋新话题 + 相对时间 meta + 当前高亮）
- [x] agent-demo.yml：web 行接线（config {port: 18080}）+ 全量装配同场
- [x] 双入口说明：CLI 与 Web 的单入口使用约定写入运行Demo M8 段
- [x] AgentReplMainTest 回归：Web 加入后 CLI 面不受影响（全量回归含全部既有用例）
- [x] 验收对照表（本工单 Comments）：运行命令 + 浏览器操作步骤 + 预期输出逐段对照
- [x] 文档同步：运行Demo.md 补 M8 段；CHANGELOG 未发布段收口；CONTEXT.md 词汇表补 3 词条（状态面 / SSE 事件流 / Web answerer）；limitations.md 新增 M8 段三条
- [x] 用户手动验收（done 的定义）——**部分通过收官（2026-09-14 用户裁定）**：三轮验收发现审批推送未接线、分脑、计划卡缺失等缺陷（全部根治，见 .scratch/bugs/BUG-20260913-04、BUG-20260914-01/02）；仍有未定位遗留问题，用户选择转持续使用反馈，不阻塞 M8 收官

## 实现记录（2026-09-13）

- 文档同步五处（README 模块表 / 运行Demo M8 段 / limitations M8 段 / CONTEXT.md 3 词条 / CHANGELOG 收口）
- 会话侧栏的完整数据接线（Session.list + /api/sessions + 切换）为唯一范围收缩项，原因与去向已入册（**当晚已取消收缩，见上**）

### Web 单页重构（2026-09-13 晚，用户指示）

- index.html 按"单文件整合 + 模块分区"重写：§样式层（原型冻结 token 逐条平移）→ §骨架层（三区）→ §脚本（api / render / sse / app 四模块）；交互卡全部事件委托（#messages 统一 data-action 派发），结构性避免逐卡挂监听的失效模式
- 样式与形态对齐 prototype.html：合一工具卡（状态徽标 + 可折叠结果 + [提醒] 独立标注）、审批 / 计划呈交 / 提问三张交互卡（提问卡由 ask_user tool/call 呈现、tool/result 冻结，回放安全）、EmptyHero 初始态、侧栏当前高亮
- 用户气泡只由 SSE user/message 渲染（去掉本地 echo 重复，BUG 同期修复）；页面错误可见化（window error / unhandledrejection → 消息流气泡）
- 服务端配套：/api/sessions 补 current 标记

### 用户验收缺陷修复（2026-09-14）

- BUG-20260914-01（流式期间刷新/重连丢内容）：Session.events() 活视图 → 快照语义（根修）；客户端 onopen 复位回放门 + 渲染区清空（重连幂等）；/api/events 回放 catch 收宽。档案见 .scratch/bugs/BUG-20260914-01.md

## Comments

### 验收对照表（M8 端到端，2026-09-13 晚浏览器实测更新）

**前置**：`~/.duo/config.yml` 配置 llm 段；运行同一条命令（WebPlugin 随 agent-demo.yml 装载），控制台打印 `Web 面已启动: http://127.0.0.1:18080`。

| 场景 | 操作 | 预期 | 实测 |
|---|---|---|---|
| 一、页面与状态面 | 浏览器打开打印的地址 | 三区布局：会话侧栏（当前高亮）/ 对话（续接最新会话历史）/ 状态面（插件六态 + 工具清单） | ✅ |
| 二、Web 对话 | 输入框发送消息 | 用户气泡唯一（不重复）→ 流式聚合（光标）→ 最终回复；工具卡状态徽标流转 + 可折叠结果 | ✅ |
| 三、Web 审批 | `新建文件 …` | 审批卡出现 → 批准 → 工具 ✓ 成功 + 卡片冻结"✓ 已批准"；拒绝 → 工具 ✗ 失败 + "✗ 已拒绝" | ✅ |
| 四、Web 提问 | `我想存个笔记，先问我存哪` | ask_user 提问卡（选项+自由输入）→ 回答后模型继续 | 卡片形态已就位，待人工过一遍 |
| 五、断连 fail-closed | 审批卡片出现后关闭页面 | 悬空审批自动拒绝（心跳 ≤15s 摘除 / 10 分钟兜底）；重开页面回放可见"✗ 已拒绝" | ✅（悬空单超时拒绝 + 回放冻结实测） |
| 六、计划模式 | `/plan 整理示例目录` | 指导片段生效 → exit_plan_mode → 计划呈交卡 → 批准执行 / 打回反馈 | 卡片形态已就位，待人工过一遍 |
| 七、/new 与切换 | ＋新话题 → EmptyHero；侧栏点旧会话 | 新会话初始态（品牌字 + 技能直调提示）；切换后回放该会话历史 + 高亮迁移 | ✅ |
| 八、CLI 并存 | 终端 CLI 面照常对话 | 两入口单会话约定；各自功能不回归 | ✅（全量回归含 CLI 用例） |
