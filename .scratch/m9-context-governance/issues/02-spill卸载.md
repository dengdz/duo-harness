# 02: spill 超大结果卸载——管线第一件治理

**What to build:** 治理管线第一件生效：工具结果消息超上限（默认 50K 字符）时，原文落 `~/.duo/agent-sessions/<会话id>/spill/<序号>-<toolName>.txt`，给模型的替换为"头尾预览 + 定位符路径"；写失败保留原结果（治理永不丢数据）；发生卸载时输出一行可观测日志。JSONL 日志不受影响（读侧治理）。

**Blocked by:** 01

**Status:** ready-for-agent

- [ ] spill 环节实现（阈值判断 / 落盘 / 预览+定位符消息构造 / 失败回退原结果）
- [ ] 测试：超限结果被替换且文件存在、内容头尾完整、失败回退、日志不含超大原文
- [ ] 全量回归绿
