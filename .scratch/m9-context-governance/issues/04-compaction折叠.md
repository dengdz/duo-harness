# 04: compaction 远端历史折叠——治理闭环

**What to build:** 管线第三件生效：修剪后估算仍超阈值（默认 0.8 × 模型窗口 128K，均可配常量）时，把远端消息（保留近端 20% 原文）折叠为一段结构化摘要（固定小节：主要请求 / 关键结论 / 已做操作 / 未决事项），注入为请求首条上下文；摘要经 LLM 直答形态生成（mock 注入测试）；折叠只发生在本次请求（日志不动）。至此四件协同成完整闭环：spill → prune → 计量 → 压缩。

**Blocked by:** 03

**Status:** ready-for-agent

- [ ] compaction 环节实现（触发判断 / 近远端切分 / LLM 摘要调用 / 摘要消息注入）
- [ ] 测试：超阈值触发且近端原文保留、未超阈值不折叠、摘要含四小节骨架、LLM 失败时降级（保留近端+最远端截断或原样透出，取简单可靠者）
- [ ] 全量回归绿 + demo 实测一轮长会话（可选，LLM 就绪时）
