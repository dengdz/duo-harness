# 01: 权限规则引擎地基——存储、匹配与裁决序

## What to build

项目级权限规则持久于项目根 `.duo/settings.json` 的 `permissions` 段（duo 首个项目级设置文件写入器，Jackson 直写，写失败降级提示不静默）；会话级规则随会话事件流持久、resume 经投影恢复；`Bash(prefix:*)` 词边界前缀匹配（命令名后须紧跟串尾或空白，`ls:*` 不匹配 `lsof`）；裁决序落 pre-execute 瀑布：deny（查全部命令，恒优先，与 guard 单调否决同构）→ allow（只查非只读命令）→ 既有档位/ask 链；deny 规则本期仅手写（审批卡不产 deny）；`/permission` 扩 `rules list`（项目+会话两级全量、来源署名）与 `rules rm <n>`；项目根以 `.git` 标定（复用技能发现根先例）。

决策依据：ADR-0026 决策一；探测 docs/research/ZCode/网络与数据/存储配置与凭据.md（ZCode settings 型两级持久化参考形态）。

## Blocked by

无（可立即开工）

## Status
done

## Checklist
- [x] `.duo/settings.json` permissions 段 schema + 读写器（缺席=无规则、坏 JSON 降级提示不崩）
- [x] 会话级规则事件类型 + resume 投影恢复（测试锁定）
- [x] 词边界前缀匹配纯函数矩阵（含 `ls:*` 不匹配 `lsof`/`lsblk`）
- [x] 裁决序接线 pre-execute：deny 恒优先、allow 只查非只读、决策署名规则来源
- [x] `/permission rules list` / `rules rm <n>`（busySafe 沿用既有声明）
- [x] 测试（先例 ToolsServiceTest / WorkspacePolicyTest / WorkspaceGatePolicyTest / IgnorePolicyTest）
- [x] 工单级验收件：手写 deny 拦命令 + rules list/rm 闭环演示，用户手动确认
- [x] CHANGELOG 记账（0.19.0 段）

## Comments
- 2026-09-22：实现与两轮审查完成（报告见下），全量 BUILD SUCCESS（774 用例 0 失败，2 既有 skip）。**待用户手动验收后转 done。**
- 2026-09-22：**用户验收实测抓到装配缺口（已修，提交 697517d→fefa1d5）**——三个消费方未把 `permissionRules` 声明进 `optionalInject`，内核视图解析"错误前移"拒读，被降级 catch 吞掉 → 规则面静默未装配（sudo 照常弹卡）。修法：CliPlugin/WebPlugin/WorkspaceApprovalPlugin 三处 optionalInject 补声明；根上下文恢复单测测不出插件上下文的声明闸门（盲区记 experience）。修复后全量回归 BUILD SUCCESS。
- 2026-09-22：**用户手动验收通过（真终端实测）**：sudo whoami → `DENY 策略=permission-rules`（`deny bash sudo*（项目级）`）；ls /tmp → 同拒；lsof -i :8080 → 未命中 ls 前缀（词边界）走交互审批、n 拒；uname -a → `ALLOW 策略=permission-rules` 免审批直出；/permission rules 列 P1/P2/P3 + 会话级（无）；rm P3 重写文件后清单剩 P1/P2。optionalInject 修复后的最终形态，四处全过，转 done。
- **过渡态记档（Spec 轴审查 (a)2 补落）**：工单 Checklist 的「allow 只查非只读」在只读判定器（工单 03）接入前为过渡态——`PermissionRules.verdict` 的 allow 分支本期对 bash 命中全量放行，裁决序落点（deny 后 allow 前）已按 ADR 预留，工单 03 接入三态判定后自动收窄为 ADR 语义。代码 javadoc 两处已记档，本条为工单侧对账。
- 踩坑记档（M23 经验「服务命名惯例」重现）：服务名首版用连字符 `permission-rules`，视图接口解析失败致恢复用例红——改为 camelCase `permissionRules` 并统一视图方法名与服务名逐字一致后修复。
- 已知边界记档：deny/allow 规则只在「被声明 ask」的调用面上生效（bash 恒 ask 必达；read/glob/grep 等免审工具不经过审批链，规则对其无意义也不生效）——与 ADR「deny 查全部命令」的命令语义一致，工具级 deny 规则对免审工具不立案。

## 审查报告（第 1 轮·双轴）：工单 01 全 diff（基点 93c1e23，11 改 + 5 新增）

**覆盖**：16 文件 = 已审 16 + 跳过 0（覆盖率 100%）

### 阻断
- **CHANGELOG.md** — 红线 6 硬违规：新增插件/命令/事件均用户可见变更但未记账（已修：补 0.19.0 段 Added 条目）

### 建议
- `PermissionRules.java` rulesToJson 与 writeProjectRules 序列化循环同形（Duplicated Code）——已修：提取 `toJsonNode(Rule)` 共享
- `CliPlugin.permissionRulesCommand` 会话侧删除逻辑摸 PermissionRules 数据（Feature Envy）——已修：新增对称的 `removeSessionRule(int)` 返回更新后快照
- `Rule.scope` String 常量与 decision 枚举不一致（Primitive Obsession），且 describe() 未知值默认项目级——已修：改 `Scope` 枚举 + `label()`
- 可选服务解析三处异常处理不一致（两处静默一处 warn）——已修：WorkspaceApprovalPlugin 补 warn（CliPlugin 降级本就用户可见，维持）
- 投影 latest-wins 与既有惯例同形——不修（惯例背书）

### 测试覆盖
- 核心逻辑全覆盖：词边界矩阵、deny 恒优先、allow 短路署名、工具级规则、无 command 参数 fail-closed、settings.json 坏文件降级/重写保留其他键、会话 JSON 往返与宽容解析、项目根发现、策略装饰器短路/透传、投影 latest-wins 与 user/message 不清空、恢复三态（恢复/清空/坏 JSON）
- 缺口：`/permission rules` 命令面 handler 无单测（CommandContext 构造成本高）——由工单级验收件手动覆盖

## Standards 轴原样分列

- 硬违规 1：CHANGELOG 未记账（已修）
- 合规确认：插件 config 契约（configType null + yml 无块）、视图方法名=服务名 camelCase、模块依赖方向、事件口径与文档行同步
- 基线 judgement call 4 项（Duplicated Code ×2 其一惯例背书、Feature Envy、Primitive Obsession）+ 处置如上

## Spec 轴原样分列

- (a) 缺失：CHANGELOG 记账（已修）；「allow 只查非只读」过渡态工单侧记档（已补 Comments）；来源署名粒度（作用域分组即署名，生成来源字段归工单 02，提请注意）
- (b) Scope creep：无
- (c) 实现正确性：词边界/deny 恒优先/两级存储恢复/降级提示/命令面/零感回退/裁决落点/项目根标定 全部对照通过；「allow 规则短路档位闸门」为 spec 裁决序直接结果，非偏差

## 审查报告（第 2 轮·OCR）：工单 01 完整 diff（含第 1 轮修复）

**覆盖**：OCR 名单 10 代码文件 = 已审 10 + 跳过 0（覆盖率 100%）；排除 7 文件（CHANGELOG + 2 docs + 4 测试）由第 1 轮双轴覆盖，全集口径 17/17

### 阻断
- 无

### 建议
- `CliPlugin.renderRules` 的「（无）」与标题同行渲染（美观取舍）——记档不修
- `PermissionRules.verdict` 每次调用合并两级列表的小分配——仅 ask 路径触达、量级可忽略，记档不修

### 测试覆盖
- 第 1 轮结论维持；修复引入的 `removeSessionRule`/`Scope` 枚举经全量套件回归锁定

### 处置
- 第 1 轮修复 4 项（CHANGELOG、toJsonNode 提取、removeSessionRule 对称、Scope 枚举）+ warn 统一 1 项；第 2 轮新增 0 修、2 Low 记档
