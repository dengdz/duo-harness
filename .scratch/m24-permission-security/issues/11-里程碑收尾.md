# 11: 里程碑收尾

## What to build

M24（0.19.0）收口对账：验收件汇总——行为迁移正反两面全覆盖（只读免审 vs 高危手写 allow 仍拦；双标签并行 vs 单标签不回归；token 缺省 403 vs 显式关闭横幅警示；plan 无写工具 vs 批准后可写）；limitations 销账 M7#1（计划模式硬禁）、M8#4（标签级会话绑定最小版）；backlog 销账三条（/model 运行时切换、Web 鉴权令牌 + bind、Anthropic-messages 协议适配）；术语表 / ADR / CHANGELOG 0.19.0 段终对账；理解关卡（duo-comprehension）凭证备齐后里程碑转 done。

决策依据：AGENTS.md 红线 8、docs/agents/issue-tracker.md done 定义；M23 工单 11 同构先例。

## Blocked by

01, 02, 03, 04, 05, 06, 07, 08, 09, 10（全部完成后收口）

## Status
done

## Checklist
- [x] 工单 01–10 全部 done 或明示遗留转 backlog
- [x] 验收件：用户手动跑正反两面场景并确认（对照上方四组正反面）
- [x] limitations M7#1 / M8#4 销账
- [x] backlog「M24 对账」段三条销账
- [x] 术语表 / ADR / CHANGELOG 0.19.0 段终对账（含「M24 启动规划落盘」记账）
- [x] 理解关卡凭证（duo-comprehension）备齐后转 done

## 收口对账（2026-09-24）

- **工单 01–10**：全部 done，各自带用户手动验收记录（01 deny 拦截+词边界+rules 闭环；02 a/s 规则生成复用+高危双拦+手写 allow 仍拦；03 git/cat 免审+deny 压过+复合弹卡；04 两轮双源 plan 正反面；05 哈希名共存+guard 生效+拔除可见；06 token 403/双通道/关闭横幅；07 双标签隔离+卡片路由+fail-closed；08 Anthropic 网关真端点直答+工具闭环；09 切换生效+resume 横幅两轮；10 四档+拒切+thinking 兼容）。遗留均明示挂账：跨 provider 路由（1.0 后）、git 破坏子命令排除（backlog）、bind（limitations M8#1）、/effort resume 横幅（工单 10 记档）。
- **四组正反面全过**：①只读免审（git status/cat 直出）vs 高危手写 allow 仍拦（sudo 卡 y/n、always-* 服务端拒）——工单 01/02/03 验收；②双标签并行（隔离/路由/fail-closed）vs 单标签不回归（53 既有用例零改动全绿）——工单 07 验收；③token 缺省 403 vs `web.auth: none` 显式关闭横幅警示——工单 06 验收；④plan 无写工具（注入收缩+deny）vs 批准后可写——工单 04 双轮验收。
- **limitations**：M7#1、M8#4 销账（残留注明：多会话协调 1.0 后、通知按最早标签投递、上下文无淘汰）；M8#1 对账补遗——令牌半边已交付，残留仅 bind。
- **backlog 三条销账**：/model 运行时切换（M24-09）、Web 鉴权令牌（M24-06，bind 半边除外）、Anthropic-messages 适配（M24-08）。
- **文档终对账**：术语表 10 词条在场且口径与实现一致（标签会话绑定词条按 sessionStorage 实现口径更新）；ADR-0024/0026 在位；CHANGELOG 0.19.0 段 01–10 全条目 + Changed 两条（停止入钮、M24 启动规划落盘）+ Fixed 两条（停止链路、BUG-20260923-01）。
- **全量回归**：936 用例 0 失败（2 既有 skip）。
- **理解关卡凭证（duo-comprehension）**：用户全程亲历 M24 验收——规则引擎、审批四值、只读免审、计划硬禁、MCP 哈希、令牌、双标签、/model、/effort、停止链路三轮实测，各机制均以实际操作与输出实证，理解关卡达成，里程碑转 done。
