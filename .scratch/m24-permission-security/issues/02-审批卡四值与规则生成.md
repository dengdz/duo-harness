# 02: 审批卡四值与规则生成

## What to build

审批决策枚举扩四值：allow / deny / always-allow-project / always-allow-session；CLI 审批卡键位 y=允许、a=总是允许（项目）、s=仅本会话、n 或空回车=拒绝（fail-closed 兜底不变）；按 a/s 生成项目/会话两级 allow 规则（落工单 01 的存储与会话事件流）；高危十根命令（sudo/su/doas/rm/dd/mkfs/chmod/chown/shutdown/reboot）卡片不出现 a/s 键（生成拦）且手写 allow 运行时也不生效（双拦，命中走 ask/档位并日志说明）；Web 卡片加「总是允许（项目）/仅本会话」两按钮；`/api/answer` 回填从按位置（最旧）改为按卡片 ID 关联（销 M23 工单 03 记档的坑——总放行错卡的后果是规则+放行双重）。

决策依据：ADR-0026 决策一（键位/高危双拦/回填身份）；M23 工单 03 Comments 记档；探测 docs/research/ 两家 审批与提问交互.md。

## Blocked by

01（规则生成落两级存储与裁决链）

## Status
ready-for-agent

## Checklist
- [ ] 决策枚举四值 + CLI y/a/s/n 键位（空回车=deny 不变）
- [ ] 审批生成项目/会话 allow 规则（会话级走事件流，resume 恢复联动工单 01）
- [ ] 高危十命令双拦：卡片无 a/s 键 + 手写 allow 运行时仍拦（日志说明）
- [ ] Web 卡片两按钮 + `/api/answer` 按卡片 ID 回填（多卡排队不错卡）
- [ ] 测试（先例 InteractionRegistryTest / ConsoleAnswererTest / WebAnswererTest）
- [ ] 工单级验收件：按 a 后同类免批 + 高危双拦演示，用户手动确认
- [ ] CHANGELOG 记账（0.19.0 段）
