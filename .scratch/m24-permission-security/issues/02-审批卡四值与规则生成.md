# 02: 审批卡四值与规则生成

## What to build

审批决策枚举扩四值：allow / deny / always-allow-project / always-allow-session；CLI 审批卡键位 y=允许、a=总是允许（项目）、s=仅本会话、n 或空回车=拒绝（fail-closed 兜底不变）；按 a/s 生成项目/会话两级 allow 规则（落工单 01 的存储与会话事件流）；高危十根命令（sudo/su/doas/rm/dd/mkfs/chmod/chown/shutdown/reboot）卡片不出现 a/s 键（生成拦）且手写 allow 运行时也不生效（双拦，命中走 ask/档位并日志说明）；Web 卡片加「总是允许（项目）/仅本会话」两按钮；`/api/answer` 回填从按位置（最旧）改为按卡片 ID 关联（销 M23 工单 03 记档的按位置回填坑——总放行错卡的后果是规则+放行双重）。

决策依据：ADR-0026 决策一（键位/高危双拦/回填身份）；M23 工单 03 Comments 记档；探测 docs/research/ 两家 审批与提问交互.md；前缀粒度=首词（本会话 grill 增补裁定 Q-02-1）。

## Blocked by

01（规则生成落两级存储与裁决链）

## Status
done

## Checklist
- [x] 决策枚举四值 + CLI y/a/s/n 键位（空回车=deny 不变）
- [x] 审批生成项目/会话 allow 规则（会话级走事件流，resume 恢复联动工单 01）
- [x] 高危十命令双拦：卡片无 a/s 键（生成拦）+ 手写 allow 运行时仍拦（日志说明）
- [x] Web 卡片两按钮 + `/api/answer` 按卡片 ID 回填（多卡排队不错卡）
- [x] 测试（先例 InteractionRegistryTest / ConsoleAnswererTest / WebAnswererTest）
- [x] 工单级验收件：按 a 后同类免批 + 高危双拦演示，用户手动确认
- [x] CHANGELOG 记账（0.19.0 段）

## Comments
- 2026-09-22：实现与两轮审查完成（报告见下），全量 BUILD SUCCESS（826 用例 0 失败，2 既有 skip）。
- 2026-09-23：**用户手动验收通过（真终端 + Web 实测）**：a 键生成项目级 mvn* 规则（含 settings.json 文件首写）并免批复用；s 键生成会话级 date* 规则、重启续接恢复、rm 即时生效；高危 sudo 卡片只有 y/n（a/s 键不出现）；手写 allow sudo 重启后仍弹卡（运行时拦）；Web 卡片四按钮、「总是允许（项目）」生成共享规则免批、高危 always-* 服务端按拒绝处理（与 CLI 对齐）。全场景过，转 done。
- **前缀粒度裁定（grill 增补 Q-02-1，用户裁定）**：bash 生成规则取命令**首词**（`npm run test --watch` → `Bash(npm:*)`）——简单可预期；已知边界：粒度内子命令全免审（`git:*` 含 push），依赖高危排除表 + 手写 deny + `/permission rules rm` 纠正；复合命令（`a && b`）也按首词生成。非 bash 工具生成工具级 allow（无前缀）。
- **提问/计划卡回填维持按最旧（记档）**：审批卡按 id 精确回填；提问（ask_user）与计划（exit_plan_mode）卡保持无 id 旧形态（单飞语义，按最旧完成）——ask_user 的请求 id 与 tool/call 事件 toolCallId 不同源，按 id 路由会断链（Spec 轴审查抓出的回归点，以此收窄修复）。
- **Web 高危 always-* 服务端裁决（记档）**：Web 卡片无法预知候选态（SSE 审批事件不带 args），四按钮恒渲染；高危命令点「总是允许」时服务端生成包装层归一为**拒绝**（与 CLI 非候选 fail-closed 对齐），前端提示「已作答（总是允许请求已提交）」，实际结果以工具错误呈现。
- 高危运行时拦日志为 debug 级（审批决策主链路已有 info 级审计，避免重复刷屏）——ADR「日志说明」以此兑现，记档。

## 审查报告（第 1 轮·双轴）：工单 02 全 diff（基点 e6f3cb6，15 改 + 3 新增）

**覆盖**：18 文件 = 已审 18 + 跳过 0（覆盖率 100%）

### 阻断
- **Spec 轴 (a)1：Web 高危不藏键且 always-* 照常放行**——CLI 非候选按 a/s 一律拒绝，Web 服务端却归一为放行，语义相悖（已修：生成包装层 rule==null 时归一为 deny，与 CLI fail-closed 对齐）
- **Spec 轴 (a)2：提问/计划卡 id 链路断裂（回归）**——ask_user 请求 id 与 tool/call 事件 toolCallId 不同源，按 id 路由必 miss（提问挂到超时 fail-closed）（已修：提问/计划回填回退无 id 旧形态按最旧完成，仅审批卡走 id）

### 建议
- 会话事件类型表 toolCallId 字段与 approval/requested 行未同步卡片 id 通道（红线 3）——已修
- WebFace handleAnswer javadoc「取值非法一律 400」与未知决策静默 deny 不一致——已修：id 分支未知决策词 400
- alwaysAllowCandidate 与 allowRuleFor 判定同构（Duplicated Code）——已修：candidate 委托 allowRuleFor != null（候选 ≡ 可生成规则）
- CliPlugin/WebPlugin 包装逻辑同构（view 跨模块复制为既有约定）——记档不修
- 高危运行时拦日志 debug 级偏弱——记档（主链路 info 审计已覆盖）

### 测试覆盖
- 新增：ConsoleAnswererAlwaysAllowTest（a/s 作用域、高危/缺席抑制、非 bash 候选）、RuleGeneratingAnswererTest（项目落文件/会话快照 sink/高危拒绝+透传/工具级/亲和透传）、PermissionRulesTest（高危运行时拦、候选判定、首词粒度、add 规则持久化）、WebAnswererTest（completeById 路由/未知 id 幂等/always-session/answer 形态）
- 缺口：WebFace handleAnswer 端点级测试未扩（解析薄层，由 WebAnswererTest + 手动验收覆盖）；工单级验收件待用户手动确认

## Standards 轴原样分列

- 硬违规 1：会话事件类型表未同步 toolCallId 卡片 id 通道（红线 3）——已修
- 轻微 1：WebFace javadoc 与未知决策行为不一致——已修
- 基线 judgement call 4 项（candidate/allowRuleFor 同构、包装逻辑跨模块重复、toolCallId 借通道文档缺失、FQN 签名）——前二修/后二记档
- 核对通过：CHANGELOG 同 diff、视图命名 camelCase、高危单一来源、兼容工厂齐全

## Spec 轴原样分列

- (a) 缺失：Web 高危不藏键半做（已修 deny 对齐）、提问/计划 id 断链回归（已收窄修复）、验收件待手动
- (b) Scope creep：无
- (c) 实现正确性：四值语义等价、高危双拦（运行时拦对工具级 allow 也生效）、生成链（首词/词边界/项目文件/会话事件/resume 投影）、包装层位置（CLI/Web 均 Auditing→Generating→真实 answerer）、a/s 只产 allow、缺席降级——全部对照通过；复合命令按首词生成属粒度裁定内，记档留意

## 审查报告（第 2 轮·OCR）：工单 02 完整 diff（含第 1 轮修复）

**覆盖**：OCR 名单 13 文件 = 已审 13 + 跳过 0（覆盖率 100%）；排除 6 文件（CHANGELOG/2 docs/3 测试）由第 1 轮双轴覆盖，全集口径 19/19。名单含 `.duo/settings.json`（用户验收残留）——确认不入提交。

### 阻断
- 无

### 建议
- 无新增（第 1 轮修复后行级复扫干净；app.js cardId 仅审批骨架卡设置，提问/计划回传路径已回退无 id 形态）

### 测试覆盖
- 第 1 轮结论维持；修复引入的高危 deny 归一、WebFace 400 校验经全量回归锁定（826 用例）

### 处置
- 第 1 轮修复 6 项（高危 deny 对齐、提问/计划回退、文档同步、400 校验、candidate 委托、事件表行）；第 2 轮新增 0
