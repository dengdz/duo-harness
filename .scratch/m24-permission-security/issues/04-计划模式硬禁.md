# 04: 计划模式硬禁

## What to build

plan 态非白名单工具定义不注入模型请求（工具集合按 plan 态过滤，模型看不见写/执行类工具）+ pre-execute deny 兜底（异常路径仍到达即拒、理由回模型，不留悬置态）；白名单 = 只读判定器判定通过 + 既有只读工具（read/glob/grep/read_image 等）；web_fetch/web_search 维持只读档 ask 语义不放开；ExitPlanModeTool 恒在（批准闭环不动）；批准后工具全量恢复；plan/mode 事件、续接恢复、指导片段全保留。M7#1（引导式不硬禁）销账对象——术语表口径已改写。

决策依据：ADR-0026 决策三。

## Blocked by

03（白名单复用只读判定器）

## Status
done

## Checklist
- [x] plan 态工具注入收缩（ChatAgent 请求工具集合断言）
- [x] pre-execute deny 兜底 + 理由回模型
- [x] 批准后全量恢复（含续接会话恢复激活态场景）
- [x] 测试（PlanHardDenyTest 5 用例：注入收缩/批准恢复/deny 理由/bash 参数级裁决/白名单放行）
- [x] 工单级验收件：plan 中模型无写工具、批准后可写演示，用户手动确认
- [x] CHANGELOG 记账（0.19.0 段）

## Comments

- 2026-09-23：实现完成。**两道防线**：①注入收缩——`buildRequest` 按白名单过滤 ToolSpec（模型不可见）；②pre-execute deny——`planDenyOrExecute` 在串行/并发两个执行点前置裁决，理由经 tool/result 错误形态回模型（成对落日志无悬置态）。**白名单**（PlanMode.WHITELIST，逐项理由注释）：read/glob/grep/read_image、web_fetch/web_search（维持 ask 不放开）、exit_plan_mode（恒在）、ask_user/todo_write（无副作用交互/状态件）；bash 定义级不注入，异常路径到达经 ReadOnlyBashDetector 参数级裁决（只读放行、写命令拒、判定器缺席 fail-closed）。**批准恢复**：isActive(session) 驱动，exited 落盘自动全量恢复，续接场景同源。**装配**：chatAgent +1 参（planBashDetector 可空），CliPlugin/WebPlugin 从各自 workspace 根同源构建（与裁决链 detector 同参，只读无状态双实例无冲突）。**销账**：M7#1 limitations 已标销、CHANGELOG 已记账。测试 PlanHardDenyTest 5 用例全绿，全量 BUILD SUCCESS。

## 审查报告（第 1 轮·四轴）：工单 04 全 diff（基点 a0bbc92 工作树，9 文件）

**覆盖**：9 个文件 = 已审 9 + 跳过 0（覆盖率 100%）；行级轴名单 5 主代码文件（OCR delegate preview 产物），测试/文档由 Standards/Spec 轴覆盖

### 阻断
- 无（四轴零阻断；Spec 轴逐条对照工单/ADR 全过，scope creep 无）

### 建议（已修 8 项 / 记档 5 项）
- **PlanMode.extractCommand 每次 new ObjectMapper**（Standards/行级/Java 轴三轴同点，CTRL-08）——已提静态共享 ARGS_MAPPER
- **buildRequest 注入过滤每工具重扫事件流**（N+1，三轴同点）——已改为 planActive + WHITELIST.contains 直判
- **"bash" 裸字面量**（Java 轴 C-01）——已提 TOOL_BASH 常量
- **WHITELIST 裸字符串**（Standards）——已改用各工具 NAME 常量（FsReadTool/FsGlobTool/FsGrepTool/ReadImageTool/ExitPlanModeTool/TodoWriteTool；web_fetch/web_search/ask_user 所在插件无常量导出，保留字面量记档）
- **deny 文案未覆盖白名单全貌**（行级）——已改为「仅只读探索、web_fetch/web_search、呈交计划等白名单工具可用」
- **测试 UT-06 粒度**（bash 三场景塞一方法）——已拆三用例；**并发组 deny 路径零覆盖**（Standards/行级缺口）——已补并发双 deny 用例（maxParallelToolCalls=4）
- **测试类命名 N-06**（PlanHardDenyTest 未以被测类开头）——已改名 PlanModeHardDenyTest
- **assertTrue(!x) 风格**——已统一 assertFalse

### 记档（不修，附理由）
- chatAgent 重载伸缩至 8 个（Standards）：参数对象收口超出本工单，沿既有演进模式
- deny 落 agent 层而非 spec 测试面 1 的裁决链（Spec）：plan 态感知在会话层，裁决链无 session 访问——测试面归属偏差已在工单记档
- 非 plan 态执行直通无显式断言（Java）：denyReason 首 isActive 短路，PlanModeTest 既有覆盖同域
- argsJson 非法 fail-closed 分支无直接用例（Java）：空串 → verdict NOT_READONLY 已核验安全
- session.close() 不在 @AfterEach（Java）：既有测试范式（ToolCallingAgentTest 同款 per-test close）

### 四轴原样分列

**Standards 轴**：阻断无；建议 5（ObjectMapper/N+1/NAME 常量/重载伸缩/风格）；候选 15 过滤 10（detector 双实例只读无状态、并发读 Session 有 CoW 快照、事件类型表确无遗漏、术语表口径一致、记账准确）。测试覆盖：5 用例与两道防线一一对应；缺口 = 并发组路径（已补）。

**Spec 轴**：(a) 十项条目逐条对照全实现（引申项 ask_user/todo_write 有注释理由，可接受）；(b) scope creep 无；(c) 七项正确性检查全过（fail-closed 方向、deny 不触达、并发点收口、isActive last-wins、detector 同源）。缺口：续接激活态无独立用例（机制同源低风险）、验收件待人工。

**行级规则轴**：4 条 low（N+1 扫描、ObjectMapper、组路径解析时机行为分歧确认项、deny 文案）——修 2、记 2（组路径解析移位实际更优：池任务异常被 awaitResult 收敛为 error 成对落日志，与串行路径行为分歧是有利方向）；误报剔除 6 组（白名单名错配逐一比对一致、线程安全、拼写、FQN、isActive NPE 超范围）。

**Java 规范轴**：MAJOR 5（C-01/CTRL-08×2/N-06/UT-06）均已修；无 BLOCKER/CRITICAL。过滤记档：CON 系虚拟线程新范式、E 系 fail-closed 既有惯例、F-09 中文 javadoc 惯例、UT-12/S 系无触发。

### 修复复核

修复均为小改（常量提取/过滤直判/测试拆分改名），由 PlanModeHardDenyTest 8 用例 + 全量 BUILD SUCCESS 锁定——按 SKILL 第 4 步未触发四轴复跑。
- 2026-09-23：**验收实证（用户终端 + 隔离环境真实 LLM 双源）**——用户终端前 3 步通过：plan 态模型自列工具仅 8 个只读件（无 write/edit/bash，模型自述「处于计划模式没有写工具」）、要求写文件被模型以无工具拒绝、exit_plan_mode 呈交与批准闭环正常。用户批准当轮模型自称「无法执行」——经隔离环境真实 LLM 端到端复现定性为**模型当轮幻觉**（延续计划文本里反复自述的旧信念）：批准后同 turn 下一迭代请求已含全量工具，实测 `[调工具] write` 出现并成功（「Created file test-plan.txt」）、read 读回校验通过、产物落地（内容 hello）。**验收判据修正：批准后需发一条新消息触发新一轮请求观察 write**，批准当轮的自述不可作凭据。deny 兜底路径（bash 到达裁决/并发组）终端不可自然触达，由 PlanModeHardDenyTest 8 用例锁定。
- 2026-09-23：**验收通过（用户终端，/new 干净话题全流程）**——①注入收缩：plan 态模型自列工具仅 8 个只读/交互件（read/grep/glob/read_image/web_fetch/todo_write/ask_user/exit_plan_mode），无 write/edit/bash，并主动声明「没有写文件、执行命令工具」；②批准闭环：exit_plan_mode 呈交 → console 批准 → 「计划已获批准」；③**批准后恢复：write 工具调用出现且成功**（Created file + read 回读校验 hello），write 正常走 workspace 审批链；④**对照实证：plan 退出后 bash 调用正常**（用户验证指令触发 bash ls/cat，走 interactive 审批 ALLOW）——硬禁严格限域于 plan 态；⑤deny 兜底与并发组路径终端不可自然触达（模型看不见写工具），由 PlanModeHardDenyTest 8 用例锁定。首验「批准当轮模型自称无法执行」经隔离环境真实 LLM 复现定性为模型幻觉（旧上下文信念延续），非机制缺陷——验收判据「批准后发新消息观察 write」已入档。
