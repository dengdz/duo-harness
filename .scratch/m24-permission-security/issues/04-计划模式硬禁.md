# 04: 计划模式硬禁

## What to build

plan 态非白名单工具定义不注入模型请求（工具集合按 plan 态过滤，模型看不见写/执行类工具）+ pre-execute deny 兜底（异常路径仍到达即拒、理由回模型，不留悬置态）；白名单 = 只读判定器判定通过 + 既有只读工具（read/glob/grep/read_image 等）；web_fetch/web_search 维持只读档 ask 语义不放开；ExitPlanModeTool 恒在（批准闭环不动）；批准后工具全量恢复；plan/mode 事件、续接恢复、指导片段全保留。M7#1（引导式不硬禁）销账对象——术语表口径已改写。

决策依据：ADR-0026 决策三。

## Blocked by

03（白名单复用只读判定器）

## Status
ready-for-agent

## Checklist
- [ ] plan 态工具注入收缩（ChatAgent 请求工具集合断言）
- [ ] pre-execute deny 兜底 + 理由回模型
- [ ] 批准后全量恢复（含续接会话恢复激活态场景）
- [ ] 测试（先例 SteerInjectionTest / ToolCallingAgentTest）
- [ ] 工单级验收件：plan 中模型无写工具、批准后可写演示，用户手动确认
- [ ] CHANGELOG 记账（0.19.0 段）
