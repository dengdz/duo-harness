# 审查报告（第 2 轮·OCR）：M19 里程碑（main..9664559，range 模式）

**OCR 运行**：ocr v1.12.7 / glm-5.3-flash / 并发 3 / 排除 `**/resources/**` / 216 请求 0 重试 / 耗时 58m38s / 6.2M tokens。
**覆盖**：`git diff main..HEAD --stat` 全集 66+ 文件 = 已审 34（OCR reviewable 名单）+ 已审 32（测试与文档由第 1 轮双轴覆盖——OCR 名单不含 src/test 与 md，按技能口径标注）（覆盖率 100%，跳过 0）。
**注意**：OCR 审的是已提交区间，第 1 轮修复（未提交）不在其视野——下表"已被第 1 轮修复"的条目系 OCR 审到修复前代码所致，非重复发现。

## 阻断（high，3 条全部成立并已修）

- **`Session.java` permissionModeOf POSIX 释放陷阱**（#15）——第 1 轮引入：占用继承路径关闭读取 fd 会按 POSIX 语义释放本进程在被占会话上的全部独占锁（load/isOccupied 两次记档的陷阱），双开下 Web 的锁被静默蒸发。✅ 修复：HELD_LOCKS 预检，本进程持锁时只读 fd **有意不关**（低频路径、泄漏有界、javadoc 记档）；他进程/无人持锁照常关闭。回归：`permissionModeOfDoesNotReleaseHeldLock`（同进程探测通道 tryLock 必须因重叠锁失败）。
- **`WebPlugin.java:106` parsePageSize 在 try 内求值**（#17）——PluginException 不被 IOException catch 捕获，pageSize 配错时 session.close() 被跳过、独占锁泄漏且无兜底。✅ 修复：解析挪到 try 之前。
- **`ExitPlanModeTool` approvedCallback 单一绑定**（#22）——Web-first 装配（agent-demo 生产拓扑）下 CLI 批准计划：exited 事件落对会话，但跑的是 Web 的空回调——CLI 的 plan.active 不复位、指导片段永不卸载。✅ 修复：bindSession 升格为绑定（会话供给 + 批准回调）按呈现位各记各账，批准跑发起方的回调；装配处同步传参；回归 `approvalRunsOriginPresentersCallbackNotFirstRegistrants`。

## 建议（medium/low）

- #3 CommandsRegistry busy 探针 check-then-act 窗口：✅ Web 已由第 1 轮互斥闭合（非 busySafe 命令占住 busy）；机制核契约补 CommandEnv javadoc（探针须在 handler 全程有效——呈现位负责）。
- #4 命令名带 "/" 前缀成静默死命令：✅ 构造校验拒绝并提示注册裸名。
- #5/#11/#19 forward 语义（last-wins 未约定、Web 不消费、WebFace 忽略）：✅ 三处 javadoc 契约注记（last-wins；转发型命令应声明 CLI 面）。
- #6 split 重复解析：挤行已修（第 1 轮）；解析复用属微优化，不立案。
- #7 异常无 message 回显 "null"：✅ 回退 e.toString() 保住异常类名。
- #10 WorkspaceGatePolicy 三参无测试：✅ 补 `threeArgDecidePassesPresenterIdToInnerOnAskOnly`（ASK 透传 + ALLOW 短路与标记无关）。
- #13/#16 压缩替换头双处硬编码：✅ 提取 `SessionEvent.COMPACTION_SUMMARY_HEADER` 共享常量，Session 投影与治理 summaryHead 同源引用。
- #14 bindSession 非 ExitPlanModeTool 实例静默跳过：✅ else 分支 WARN 点名（装配处）。
- #18 busy 注入 check-then-act 残余窗口：✅ 大部已修（第 1 轮 agentRunning 预检）；微秒级残余自愈（滞留文本下次 send 首迭代排干），不立案。
- #20 pageSize() 死代码：✅ 删除。
- #21 parsePageSize 无上界：✅ 补 MAX_PAGE_SIZE=1000 上界（越界点名——尾窗缓冲与页长成正比，配置错误不应演变为 OOM）。
- #23 /compact TOCTOU：✅ 已被第 1 轮互斥修复覆盖。
- #24/#26 restorePermissionMode parse IAE：✅ 已被第 1 轮修复覆盖（catch + WARN）。
- #25 JSON 响应走 respondText：✅ 三处改 respondJson（injected/command/拒绝文案）。
- #1/#2 ConsoleAnswerer/WebAnswerer FQCN：✅ 改 import 短名。
- #27 CliPlugin "四命令 CLI 面" 注释失实：✅ 注释改为三命令 CLI 面 + /permission 双面 + 共享注册的双面命令。

## 测试覆盖

- 新增回归 6 个：#15（锁不释放）、#22（回调按发起方）、#10（透传/短路）、blankSummary（第 1 轮）、restore 非法档位（第 1 轮）、originDeclined（第 1 轮）。
- 既有测试同步：压缩语义断言重写（治理 10 用例）、CliPluginTest 14、WebFaceTest 47、SessionTest 48。
- 收口验证：`./mvnw verify` 全模块 **BUILD SUCCESS**（pom 全模块已升 0.14.0）。

## 统计

27 条 = 已修 21（含并入第 1 轮修复 6 条）+ 文档化 3（#3/#5/#11/#19 javadoc）+ 记 backlog 1（#6 解析复用不立案并入挤行修复；异步化已记）+ 不立案 2（#18 残余自愈、#6 微优化）。
