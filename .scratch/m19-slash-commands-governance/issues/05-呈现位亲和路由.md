# 05: 呈现位亲和路由

**What to build:** "谁发起谁作答"——工具执行携带发起呈现位标记（presenterId），审批/提问/计划呈交的 ask 请求优先路由给发起方的回答者，发起方缺席或放弃才轮注册序；交互工具（ask_user / exit_plan_mode）的会话供给同改发起方亲和；hooks 载荷顺带携带 presenterId。双开部署下 CLI 发起的审批在终端作答、不再跳 Web 卡片终端零提示（M12-02 验收事故销账）；limitations 交互工具绑定条销账。

**Blocked by:** None (can start immediately，独立线)

**Status:** ready-for-agent

语义权威：ADR-0020 决策 7；spec 实现决策"呈现位亲和路由（tools 域 + agent 域）"节。

- [x] ToolExecution 新增 presenterId 只读字段：呈现位构造 ChatAgent 时注入自己的呈现位标记，agent 执行工具时携带进管线（M18 裁定 args final 的同量级只读扩展，不动既有字段）
- [x] ask 请求（审批 / ask_user / 计划呈交）携发起方标记；InteractionRegistry 路由规则改为发起方回答者优先，缺席/放弃才轮注册序（注册序兜底保留，单呈现位部署零感）
- [x] 交互工具（ask_user / exit_plan_mode）会话供给按发起方亲和（limitations"交互工具的会话绑定先到先得"销账——双开下计划状态不串位）
- [x] 交互工具查重先到先得的注册模式保持不变（只改会话供给与路由，不改注册归属语义中与发起方无关的部分）
- [x] hooks stdin 载荷增 `presenter_id` 字段；backlog"钩子载荷上下文透传"条目更新（presenterId 消化、session_id/transcript_path 仍缺席）
- [x] 测试走缝 4（InteractionRegistry 发起方优先/缺席轮注册序单测 + ToolExecution.presenterId 传导断言，组合矩阵风格）+ 缝 3 双开装配用例（cli 发起审批 → console 回答者作答而非 web）
