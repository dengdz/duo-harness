# 03: /compact 手动压缩 + 压缩点事件

**What to build:** 用户在上下文逼近窗口时 `/compact` 手动压缩——治理管线总结老历史后落 `context/compacted` 事件（总结文本 + 压缩范围），投影按最后压缩点拼接（之前以总结替换、之后照常）；预算触发的自动压缩同事件化。刷新/重开会话压缩态天然恢复、不重复总结；日志里"上下文为何变小"可审计（M9-M11#3 销账）。

**Blocked by:** 01（/compact 注册为命令）

**Status:** ready-for-agent

语义权威：ADR-0020 决策 6；spec 实现决策"/compact（治理域 + 命令）"与"会话事件与投影"节。

- [x] 新增 `context/compacted` 会话事件（总结文本 + 被压缩范围）
- [x] 投影扩展：以最后压缩点为准——之前以总结替换、之后照常（latest-wins 家族，与 todo 同款）；压缩事件不占消息窗口计数、不参与 tool 配对（同批断言 M10/M13/M16 零牵动）
- [x] /compact 注册为 busySafe=false 命令（双面 ANY）：执行 = 触发治理压缩（LLM 总结）→ 落压缩点事件 → 回显结果摘要；执行中输入得"执行中需等待"提示
- [x] 自动压缩统一：预算触发的压缩同样落 `context/compacted`（一处语义两处触发，消除"同变换两种事实地位"分裂）
- [x] busySafe=false 的既有安全语义保持：压缩需要调用 LLM 且动上下文结构，绝不运行中执行
- [x] 测试走缝 2（投影拼接与零牵动）+ 缝 1/3（命令触发路径：治理压缩用 mock LLM 总结、压缩点事件落盘断言、busy 行为）
