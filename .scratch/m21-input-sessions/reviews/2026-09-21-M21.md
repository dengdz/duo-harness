# M21 审查报告（0.16.0 分支，74c859e...6ac7077 + 两轮修复）

审查日期：2026-09-21 · 流程：duo-code-review 两轮流水线（双轴 → 修复 → OCR 委托 → 修复 → 测试收口）

## 审查报告（第 1 轮·双轴）：M21 全量

**覆盖**：diff 全集 133 文件 = 已审 56 代码文件（OCR 名单口径）+ 测试/文档由双轴与修复轮覆盖；跳过 pom/yml/md 之外无

### 阻断
- **`sessionquery/InvertedSessionIndex.java:217`** — 索引扫描用 `Files.newBufferedReader` 直读目录全部 JSONL（含本进程 HELD_LOCKS 独占的活跃会话），try-with-resources 关闭读取 fd 即按 POSIX 语义释放本进程属主锁——另一进程可乘虚打开活跃会话形成分脑写（review-log M19 模式①：新加读会话文件的助手必须先查 HELD_LOCKS；Session 三处 javadoc 记档，此处全无规避；测试零防护）

### 建议
- **`web/WebFace.java:582/1108`** — `e.printStackTrace()` 与 `new Exception("[诊断]…").printStackTrace()` 调试残留（类内已有 slf4j log）
- **`agent/fileref/`** — 新包缺 `package-info.java`（duo-project-structure）
- **`presenter/PresenterAssembly.java:417`** — registerExportCommand 的 `currentSession` 参数从未使用（Speculative Generality，且误导单一绑定）
- **`attachment/ImageFileDelivery.java`** — `invalidate` 无生产调用方（失效重传未接线，YAGNI）；`persistIndex` 吞 IOException 无日志
- **`attachment/ImageFileDelivery`** — 去重键缺 scope 前缀（spec 决策 5"键 = scope+variantId"）
- **`tools/fs/ReadImageTool.java:98`** — 入库标记硬编码字面量，未引 `Session.READ_IMAGE_REF_MARKER`（漂移风险）；`Session.readImageRef` 与 `SessionExport.collectReadImageRef` 提取逻辑两处重复
- **`attachment`** — FilesApiUploader javadoc 仍写"无去重/回收"与已接线实现矛盾（工单 06 曾多报完成的残留）

### Spec 轴
- (a) 缺失/偏差：`file_id 失效重传一次`未落地（limitations M21#3 自认）；去重键缺 scope；**单消息 20 张/100MiB 总量仅在 AttachmentConfig 定义、WebFace.handleMessage 未执法**；子代理播种对 user/attachment 块无过滤测试（实际播种原样落子日志，仅靠子代理投影端 vision 关闭兜底）
- (b) scope creep：0ee1f81（技能瘦身，用户提交不处置）；FileReferenceService 未命中重建（验收反馈修复，保留）；带附件斜杠命令 400（防御性明示，保留）
- (c) 疑似错误：**伪造附件引用炸请求**——tool/result 文本含"附件已入库: <64hex>"即被投影为 AttachmentRef，`variantFor` 抛"附件不存在"炸整轮请求；FilesApiUploader javadoc 矛盾（同上）
- 已核实真实覆盖：vision 三入口、工单 06 补课接线、@file 防护、检索懒构建/增量/子代理排除、/export busySafe+屏障、variantId 确定性

## 审查报告（第 2 轮·OCR）：M21 全量（含修复）

**覆盖**：56 个代码文件 = 已审 56 + 跳过 0（覆盖率 100%）；测试与文档不在 OCR 名单（由双轴轮与修复轮覆盖，覆盖率按 `git diff --stat` 全集口径 133 文件标注）

### 阻断
- 无新增（第 1 轮 P1 修复经全量回归验证）

### 建议
- **`sessionquery/EventTextExtractor.java:23`** — todo 解析每次 new ObjectMapper（static 化）
- **`session/SessionExport.java:88`** — jsonl() 双次调用 jsonlLines()

### 测试覆盖
- 新增/强化回归 8 处：P1 锁完整性 OS 级断言（扫描后他 channel tryLock 必撞 OverlappingFileLockException）、活跃会话跳过、伪造引用降级、播种过滤附件块、闸门回填三态（默认拒/true 放行/false 再拒）、配额回收重试、索引跨实例持久化、/export 全链
- 判定：vision 三入口、/export 屏障、@file 防护矩阵、投递回退均有对位用例；不足项 = file_id 失效重传（未接线，limitations 记账）、Web 端 413 消息级限制无 HTTP 级用例（本地逻辑简单，接受）

## 修复处置（两轮合计）

| # | 发现 | 处置 |
|---|---|---|
| S1/P1 | 索引扫描释放活跃会话属主锁 | ✅ 修：`Session.heldByThisProcess` 探针（只查登记表不开 fd）+ 索引跳过并摘除陈旧条目 + OS 级回归用例（tryLock 必撞重叠锁异常）；代价 = 当前会话不入检索（limitations M21#1 已记） |
| S2 | printStackTrace 残留 | ✅ 修：slf4j debug |
| S3 | fileref 缺 package-info | ✅ 修 |
| S4 | CliPlugin static 配置态 | ⏸ 接受（沿工单 05 既有 visionEnabled 静态模式，单实例部署无实害） |
| S5 | export 死参 | ✅ 修（签名收敛为 2 参 + 目录重载） |
| S6 | invalidate 无调用方 / persist 吞异常 | ✅ 修：invalidate 删除（失效走手清索引 + limitations 记账）、persistIndex 补 slf4j warn |
| S7 | 标记常量/提取器三处共繁 | ✅ 修：ReadImageTool 引 `Session.READ_IMAGE_REF_MARKER`、抽 `Session.readImageRefOf` 供投影与导出共用 |
| S8 | files 成功仍驻留 base64 / 适配器重复 | ✅ 修：懒编码（MessageImage 允许 null base64 当且仅当有 fileId，紧凑构造器内联判定）；适配器重复接受（两分支形态本就不同） |
| SP-a2 | 去重键缺 scope | ✅ 修：indexKey = "chat:" + variantId（一期单一域，spec 形态对齐） |
| SP-a3 | 消息级 20 张/100MiB 未执法 | ✅ 修：AttachmentStore 补访问器 + WebFace.handleMessage 413 点名 |
| SP-c1 | 播种原样带附件引用块 | ✅ 修：SubagentManager.launch 种子处物理拦截 + 回归用例 |
| SP-c2 | 伪造引用炸请求 | ✅ 修：resolveImages 对 AttachmentException 降级为丢弃该部件 + 回归用例 |
| SP-c3 | FilesApiUploader javadoc 矛盾 | ✅ 修 |
| SP-a1 | file_id 失效重传未落地 | ⏸ limitations M21#3 记账（手清索引即自然重传）；聊天层自动重试属后续 |
| SP-b | 三项 scope creep | ⏸ 保留并记档（用户提交 / 验收反馈修复 / 防御性明示） |
| OCR1/2 | mapper per-call、jsonl 双调用 | ✅ 修 |

**收口**：`./mvnw test` 全仓 BUILD SUCCESS（tools 180 / attachment 30 / session-query 40 / session 30(+导出) / agent 169 / web 69 / cli 24 / llm 29(+file part) 等）；新增/强化回归 8 处。
