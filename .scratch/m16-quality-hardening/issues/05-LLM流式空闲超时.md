# 05: LLM 流式空闲超时

## What to build

LLM 流式空闲超时（术语：流式空闲超时）——流式响应连续 N 秒无新字节判流已死：尚未交付任何 chunk 时按可重试错误走既有重试链；已交付增量后终止本轮并保留已输出文本（对齐"已交付不重试"的流式安全语义）。provider 半开连接不再永久挂死 agent 循环：CLI 最多 90 秒拿到可重试错误、Web 单飞标志恢复。落点为响应体流的包装委托（单点覆盖直答与工具轮两条读取路径，不改两处逐行循环）。

## Status

done（2026-09-18，用户裁定按 agent 实测证据收口）

## Checklist

- [x] 响应体流包装空闲检测（IdleTimeoutStream，虚拟线程泵 + 队列超时消费），缺省 90 秒不配也生效
- [x] `llm.streamIdleTimeoutSeconds` 可配（llm 配置段可选字段，非正回落缺省）
- [x] 桩服务三用例：发响应头后停发字节 → 超时可重试；已交付增量 → 不可重试且已输出保留；短停顿 → 不误伤思考模型（MockOpenAiServer 增空闲挂起模式）
- [x] 实测：03/04 批量验收期间多轮真实 LLM 对话零误伤（断流触发形态由 mock 用例锁定；真实半开连接无法手工稳定模拟，经用户裁定以 agent 证据收口）

## Comments

- 2026-09-17 首版实现用 available() 轮询——OpenAiCompatAdapterTest 全量跑挂（17 用例 11 败、90.06s 超时形态）：JDK 响应流的 available() 恒返 0，轮询方案不可行。重写为虚拟线程泵 + 队列超时消费（阻塞读无法直接限时，泵+队列是可移植形态）。教训：**"可移植"的 IO 超时方案必须先在目标流类型上验证 available() 语义**。
- 2026-09-18：与工单 08 会话崩溃恢复互补——08 治"调用缺 result"（崩溃/中断），05 治"等待中的永久阻塞"（半开连接），两修复覆盖会话卡死的全部已知形态。
