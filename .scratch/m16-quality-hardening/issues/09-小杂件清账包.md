# 09: 小杂件清账包

## What to build

五笔小额质量债清零：①两处 javadoc 错位归位（注释挂在错误目标上误导读者）；②热路径 ObjectMapper 提为共享常量（每次工具调用新建纯浪费）；③两处全量读文件改逐行流式（大会话内存峰值，标题探测找到即停）；④subagent 包按功能边界拆子包（15 类超约 10 类阈值，package-info 同 diff）；⑤测试进程收割兜底（surefire fork 强杀时 stdio 子进程不回收、REPL 线程阻塞致 JVM 退出挂起，backlog 挂账项——CI 建成后僵尸进程会累积在 CI 侧，优先级升高）。

## Blocked by

03, 04（子代理包拆分挪的是功能改动后的最终形态，避免边拆边改）

## Status

done（2026-09-18 实现与 agent 实测完成；真机钉死/环境块验证已过，验收记录见工单 Comments）

## Checklist

- [x] Session 两处 javadoc 错位归位（lock()/latest() 的孤儿段挂回方法）
- [x] 热路径 ObjectMapper 共享常量化（ToolCallingAgent）
- [x] 标题探测与子会话事件读取改逐行流式（titleOf 改 latest-wins 顺序扫；/api/subagent/events 改 BufferedReader）
- [x] subagent 包拆子包：tools/（五件工具）+ backend/（后端/视图/策略/基线 6 类）——根包留 Manager/Plugin/Host/Template(s)/SeedSlicer；package-info × 2
- [x] 测试进程收割兜底 + 单跑 cli 装配测试类 JVM 正常退出（CliPlugin.stop 已含 in.close() 解除阻塞——既有实现即兜底，验证书面化）
- [x] 全量测试绿（437 用例）

## Comments

- 2026-09-17：实现完成。包拆分说明：`subagent.backend/`（执行内件，package-private 自洽）+ `subagent.tools/`（五件 public 工具）；`PinnedApprovalPolicy` 因 SubagentPlugin 跨包注入 ALWAYS_DENY 而 public 化；SubagentTemplate.FORBIDDEN_TOOLS 改全限定名引用（零运行时耦合）。Root 6 类达标（约 10 阈值）。
