# 04: 子代理环境块

## What to build

子代理 system 追加环境段——工作目录、操作系统平台、当前时间等运行环境事实（ZCode `<env>` 块对照、limitations M15#5）：子代理回答可引用正确的 cwd 与时间，不再对运行环境两眼一抹黑。父 agent 呈现位顺路补时间上下文片段（DSH time-context 同源事实）。

## Status

in-progress（实现与 agent 实测完成：E2E 断言环境段在场；待用户 CLI 实测）

## Checklist

- [x] 子代理 system 追加环境段（基线 → 环境段 → 模板提示，顺序在 SubagentBaseline 固定并注释）
- [x] 用例：子代理首轮请求的 system 含环境事实（cwd/OS/时间三断言）
- [ ] CLI 实测：问子代理"你在哪个目录、现在几点"，回答正确（待用户）
- [x] 父呈现位时间上下文：**降记 backlog**——prompt 注册表仅静态片段（注册时刻冻结，长会话内变陈旧），动态时间上下文需注册表扩展
