# 08: 会话崩溃恢复

## What to build

会话崩溃恢复（术语：合成闭合）——打开会话取得文件锁后，探测日志尾部未闭合的工具调用事件，追加一条合成 tool/result（注明"结果因进程中断未知，只可重试只读/幂等操作"）后再建事件快照。崩溃过的会话打开即可继续聊（此前悬空调用会使下一轮请求协议非法、会话永久无法续聊）；日志自包含、投影零特判；子代理会话走同一加载路径自然受益。

## Status

ready-for-agent

## Checklist

- [ ] load 路径尾部探测 + 追加合成闭合，再建事件快照
- [ ] 投影零特判（合成闭合后消息序列自然合法）
- [ ] SessionTest：撕尾用例（悬空调用闭合 / 正常会话不受扰动 / 边界形态）
- [ ] 子代理会话加载路径同验证
- [ ] 实测：手工撕 JSONL 尾部模拟崩溃 → 打开会话继续对话成功
