# 05: 收官——文档、对账、双轴审查、版本收口

**What to build:** M18 作为 0.13.0 版本收口：用户可见变更记账、配置参考成文、挂账对账、已知债销账、双轴审查过关、全模块升版——里程碑达到"可合并 main"状态。

**Blocked by:** 01 / 02 / 03 / 04 全部

**Status:** done（2026-09-18 收官执行完毕：双轴审查处置闭环、全量 0.13.0 回归绿；待用户确认合并）

惯例先例：M17 工单 05 同构（CHANGELOG + limitations/backlog 对账 + 双轴审查 + pom 升版 + 快速回归）。

- [x] CHANGELOG 0.13.0 段：hooks、插件可选依赖、awaitStartup 超时三条用户可见变更（Fixed 含 PipelineTimeout 双挂修复）
- [x] 插件配置参考：hooks.json 章节（格式、事件、matcher、timeout、载荷）+ stdin 字段与 duo 对应物映射表 + "钩子不是执法边界"（fail-open）警告；README 版本口径与模块表复核（映射表以"同名 + 缺席说明"形态落地——四字段收敛后 ADR 映射义务事实上消解）
- [x] backlog 对账：销"插件可选依赖""awaitStartup 超时语义""PipelineTimeout 双挂"三条；增"项目级 hooks 配置""UserPromptSubmit/Stop 事件""updatedInput 改写"三条；测试进程收割按 02 评估结论落置
- [x] PipelineTimeout 双挂修复：挂载查重先到先得（与 registerTodoWriteTool 同模式）+ 测试
- [x] 双轴审查（Standards + Spec，独立子代理）与全部处置
- [x] pom 全模块升 0.13.0 + 快速回归（全量 verify 而非单测——M17 教训：收官阶段任何改动都全量验证）
- [x] 术语表、ADR、spec 与实现的最终一致性核对（工具目录对账不含 hooks——非工具，但配置参考须与实现逐字对齐）

## Comments

**收官执行摘要（2026-09-18）**

- 文档四件：CHANGELOG 0.13.0（Added 三条 + Fixed 一条）；插件配置参考新增 hooks.json 章节（格式/事件/matcher/处理器/超时/载荷/fail-open 警告/审批顺序/解析失败行为）；README 版本口径 0.1.0-0.13.0；backlog 三销（可选依赖、awaitStartup、双挂）三增（项目级 hooks、事件扩面、updatedInput）+ 载荷上下文透传条目（04）。
- PipelineTimeout 双挂修复：按 ToolsService 实例查重先到先得（WeakHashMap 弱键）；挂载标记随注册方作用域回收（`ctx.effect` 清标记，与"作用域副作用"契约一致，先挂方停止即解锁重挂）；摘除器先清标记再摘监听器。测试改行为断言（双挂跳过后超时仍由首挂恰触发一次 + 摘除后重挂恢复生效），不做内部簿记断言。
- pom 十一文件 0.12.0 → 0.13.0。

**双轴审查处置（Standards + Spec 独立子代理，2026-09-18）**

- **P0-1（Standards，已修）**：HookRunner 的 stdin 同步写在限时器启动之前——不读 stdin 的钩子遇大载荷（PostToolUse 的 tool_response 易超 64KB 管道缓冲）写管道永久阻塞，超时预算被蚕食、"超时放行"承诺不成立。修复：stdin 喂入转后台虚拟线程（与输出收集同款），waitFor 覆盖进程全生命周期。
- **P1×4（已修）**：① HooksConfigTest 套件叙述行 7→9 用例失真；② docs/04-架构/模块划分.md 补 hooks 模块行 + 关键语义条目（AGENTS.md 红线 3）；③ hooks 包补 package-info.java（全仓唯一缺失）；④ PipelineTimeout 挂载标记与"作用域副作用"契约矛盾（scope 自动摘除路径不去标记、重挂被静默跳过）——标记改随 scope effect 回收。
- **P1-5（已修）**：双挂测试断言内部标记（isMounted 为测试开缝）——删 isMounted，改行为断言（跳过后超时恰触发一次 + 摘除后重挂恢复）。
- **P2 全部处置**：matcher 计数词统一（CHANGELOG/配置参考去"三档"计数，与 ADR"两档做齐"不再对账生疑）；配置参考裁定条补两事件 JSON 语义限定 + 处理器仅 command 句；PostToolUse exit 0 的 legacy JSON block 补实现（与 exit 2 同义结果改写，+1 E2E）而非仅披露；测试注释变更叙述改状态句；timedOut 工厂死参数删除（改名 ofTimedOut 避让 record 访问器）；example 增 hooks-demo.yml 示例装配（依赖不空挂）；spec 故事 10 加实现修正括注、Testing Decisions 缝 2 披露实际形态（拦截语义经缝 3 全链路覆盖，管线缝留作结构选项）。
- **未处置留观察**：先挂方作用域销毁后另一呈现位不自动补挂超时（当前 cli+web 同树同生命周期不可达；PipelineTimeout JavaDoc 已明示"呈现位停止后如需恢复须重新挂载"）。
- Spec 轴结论：无 P0/P1，12 条决策全部有落点、拒绝项零越界、Out of Scope 零侵入；已批准偏离（载荷七→四字段）留痕闭环齐备。

**OCR 终审意见处置（2026-09-19，用户裁定全修）**

升级版 duo-code-review 技能的 OCR 段对 0.13.0 分支整期行级审查（16 文件 / 46min / ~1.85M token / 7 findings，流程实录见技能「M18 实测补遗」），七条全处置：

- PipelineTimeout 注册失败路径标记泄漏（bug·medium）：两步注册（on + marker effect）包 try/catch，失败回滚 MOUNTED——回归锚 `mountFailureRollsBackMarker`（tools 8 用例）
- 摘除顺序瞬时叠挂窗口（bug·low）：改先摘监听器、finally 清标记（清标记即解锁重挂，先清会开窗口）
- PluginInstance.inject() 死访问器（传导已改 dependsOn）：删除
- PluginInstance FQCN 混用：List/ArrayList/TimeUnit 提顶 import，describe 改短名
- **permissionDecision 三值补全**（bug·low，含金量最高）：Claude Code 实为 allow/deny/ask 三值，原实现 ask 落"等价放行"——对显式收紧请求放行不合"只收不放"。现 ask → `requestApproval()` 交审批段（解析在 pre 瀑布后、注册序无关；无人解析未配置即拒）；E2E `askDecisionRoutesToApprovalSegment`（hooks 15 用例）。ADR-0019 Consequences 注记 + 配置参考裁定条 + CHANGELOG 同步
- deny() 死参数 event 删除；HookRunner 未用 Duration import 删除

**验收件（OCR 处置修复，待用户回归验证）**

测试路径：`./mvnw package` 全绿 + 两条新叙述行（tools `PipelineTimeoutTest —— …注册失败回滚（8 用例）`、hooks `HooksPluginEndToEndTest —— …ask 转审批（15 用例）`）。

演示路径（ask 三值，三场景）：

| # | 动作 | 应出现 |
|---|---|---|
| A1 | ask 钩子 + bash 命令 + 批准 y | INFO `PreToolUse 钩子转审批: 工具=bash` → `[待审批] 工具 bash 请求执行` y/n → 按 y → `审批决策：工具=bash 结果=ALLOW 策略=interactive` → 命令正常执行 |
| A2 | 同上按 n | `结果=DENY 策略=interactive` → `[工具错误] …执行被拒绝`（拒绝来自审批段，非钩子文案） |
| A3 | 对照：改 deny 钩子同命令 | 无 y/n 弹窗，直接 `[工具错误] …被 PreToolUse 钩子阻断`（ask 与 deny 路径区分实证） |

PipelineTimeout 两修复（标记回滚/摘除顺序）为内部簿记，真机不可直接观察——由 `mountFailureRollsBackMarker` 等 8 用例覆盖；死代码/风格四条无行为变化，全量绿即证。

手工补位两条建议（stdout.get 2s 上限对超大输出、readAll 后显式 close）未动代码——fail-open 语义安全，属丢弃档。

**测试证据**

- 全量 `./mvnw package` 十一模块 0.13.0 BUILD SUCCESS：core 89 + tools 106（+双挂两用例）+ hooks 35（+Post 段 JSON block）+ llm 28 + session 42 + agent 115 + web 46 + cli 19 + example 12。
- 工单 04 真机验收（守门钩子拦 rm -rf、deny 占先审批、y/n 交互、read 直执行）为本期用户侧证据。
