# 05: 收官：文档同步与验收件

## What to build

M12 收口：文档同步、CHANGELOG 0.7.0 记账、验收三件套（可运行演示 + 过程日志 + 预期对照表），交用户手动验收。

## Blocked by

01, 02, 03, 04

## Status
done（2026-09-15 里程碑验收通过——M12 收官）

## Checklist
- [x] 文档同步：插件配置参考（fs 子包行 + workspace/预设配置）、limitations 复核（bash 无沙箱、glob/grep 无 gitignore、预设不持久——ADR-0012 已留档的三条落表）
- [x] CHANGELOG 0.7.0 段记账（六工具 + 三档预设 + 读前写闸门 + MCP 行移除）
- [x] 全量 `mvn -o test` 绿（先杀 18080）——用户验收执行
- [x] 验收三件套：六工具端到端演示 + 三档切换演示 + 路径安全演示；对照表覆盖 M12 全部用户可见行为

## Comments

- 插件配置参考全量重写（对齐 M12 装配）：逐行注解补 fs-tools（mode/root 两配置 + workspace 服务依赖说明）与 approval（WorkspaceApprovalPlugin 三态语义 + 无 fs-tools 时 PENDING 指引）；"二选一"改审批策略**三选一**；编程挂载表更新（交互工具改"呈现位插件查重注册"、MCP 连接标注 agent demo 默认不挂 + DemoMain M2 段演示）；状态行 M8 → M12 版。——04 审查轮 blocker 就此关闭。
- limitations 复核：M12 段落三条（bash 写范围不受 workspace 约束 / glob/grep 无 .gitignore 语义 / 预设切换不持久）；M7#1 计划模式条目的防线描述更新（交互审批 M6 → 档位审批 M12）。
- CHANGELOG 0.7.0 段：Added 四条（六件 / 三档预设 / 读前写闸门 / 档位审批插件）+ Changed 两条（demo MCP 行移除 + 文档对齐）。
- 里程碑验收件：`acceptance.md`（17 验收点对照表 + 实测快照按点标注 + 通过/不通过条件）；全量 `mvn -o test` 按用户既定分工由用户在验收第一步执行。
- 验证证据（本机）：test-compile 全绿；ToolCatalogTest 对账 9 工具（文档改动后复跑）。
- 附带说明：术语表 workspace/权限预设/读前写闸门三条已在 02 落库，本单复核无漂移。
- 验证期发现并修复：ToolCatalogTest **单跑**时测试本体通过但 JVM 退出挂起——cli 行 REPL 线程阻塞在 System.in（同 fork 先跑 AgentReplMainTest 时被其 System.setIn 置空掩盖）；修法与 agentDemoYmlBootsCleanly 同法（Boot 前置空流），单跑复验 0.3s 正常退出。案例补进 backlog"测试进程收割"。

## 验收记录（2026-09-15 用户里程碑验收通过）

- 全量 `mvn -o test` 10 模块零失败；acceptance.md 17 验收点逐条过（六工具行为、读前写闸门、三档联动 danger 直通/read-only 全 ask、bash 全语义含截断+spill 回读、呈现零特化、装配收敛）；视觉确认两处（状态面标题"工具（本机 + MCP 远端）"、插件表 10 行含 cli——上轮粘贴缺行嫌疑就此销案）。M12 五单全 done，0.7.0 具备发布态。
