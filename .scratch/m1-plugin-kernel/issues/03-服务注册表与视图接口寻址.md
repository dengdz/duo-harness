# 03 — 服务注册表 + 视图接口寻址

## What to build

插件间的服务注入：服务提供者发布具名服务（provide / 服务基类），消费者声明 inject 依赖列表后经 `ctx.as(视图接口)` 取服务——动态代理按方法名解析到字符串服务名。注册表内部 key 从第一天就是 (服务名, 作用域) 二阶结构（isolate 预留，仅全局默认作用域）。服务缺失时注入方进入等待而非报错。完成的判据：发布→注入→视图寻址全链路可用例复现；未声明 inject 读服务得到点名报错（spec 用户故事 9/12/13，接缝 B）。

## Blocked by

01

## Status

done

## Checklist

- [x] 服务发布：provide 原语 + 服务基类（构造即发布，插件停止即注销）
- [x] 视图接口寻址：ctx.as(XxxView.class) 动态代理，方法名即服务名；第三方视图接口不改核心
- [x] 注册表 key = (服务名, 作用域)，当前仅默认作用域（isolate 预留）
- [x] inject 声明：声明后可读；未声明读取报点名错误；服务缺失时插件进入等待（PENDING）
- [x] 服务注销时通知依赖方（与 04 的 epoch 联动点在此埋好 notify 钩子）
- [x] 测试（接缝 B）：发布/寻址/未声明报错/缺失等待/注销通知

## Comments

- 03/04 边界：本单交付三态（PENDING/ACTIVE/STOPPED）与"依赖缺失挂起、注销停依赖方、提供方停止级联传导"；服务回归自动重启与换实现重载（完整 epoch）属 04，onServiceAvailable 的唤醒钩子即其接入点。
- awaitStartup 自本单起具有真实阻塞语义（PENDING 等待，虚拟线程友好），PluginHandle JavaDoc 已同步。
- ocr 审查修复两项：ServiceRegistry.remove 改值感知（ConcurrentHashMap.remove(key, value)）——修复旧注销器在与新 provide 竞态中误删新实例并误停依赖方；Service 基类 JavaDoc 补构造器逃逸约束（super 发布会同步唤醒依赖方调用服务，子类状态必须字段声明处初始化）。
- 已知残留（归 04）：值感知移除后仍存在"移除成功 → onServiceUnavailable 执行前，新实例注册并唤醒依赖方 → 依赖方被误停"的顺序窗口——DSH 的 epoch 依赖指纹正是该问题的系统性解法，04 落地。
- 44 用例（12 闭环 + 18 事件 + 14 服务）三连跑全绿。
