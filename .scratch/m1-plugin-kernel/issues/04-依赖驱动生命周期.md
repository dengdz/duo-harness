# 04 — 依赖驱动生命周期（epoch 状态机）

## What to build

插件实例的六态状态机与 epoch 依赖指纹：PENDING（依赖未全就绪，等待）→ LOADING → ACTIVE / FAILED，依赖消失或 dispose → UNLOADING → DISPOSED。epoch = 依赖实现集合的指纹，指纹变化自动触发重载或停止：依赖全就绪才启动、任一消失自动停止、换实现自动重启。插件 apply/config 失败只使自己进入 FAILED，兄弟插件无感。完成的判据：spec 决策浓缩形状中的状态机全部迁移路径有用例锁定（spec 用户故事 2/3/4/11/19/22/24，接缝 B 的难点票）。

## Blocked by

03

## Status

done

## Checklist

- [x] 六态状态机实现，迁移发内部状态事件（供 07 的 Demo 观测与后续状态页）
- [x] epoch 指纹：依赖集合变化驱动 reload/unload；加载中途依赖变化能正确放弃或二次迁移
- [x] 失败隔离：apply/config 失败 → FAILED，不影响兄弟；错误可被 await 重新抛出
- [x] dispose 语义：逆序回滚 effect、级联停子插件、清理错误不中断兄弟清理
- [x] 每个插件实例状态可查询（等待/加载/激活/失败/停止/销毁）
- [x] 测试（接缝 B）：全部状态迁移路径；依赖消失停止/回归重启/换实现重启；失败隔离

## Comments

- 语义升级（行为变更，CHANGELOG 已记）：启动失败不再阻断 plugin()——错误统一经 handle（awaitStartup 重抛 / state=FAILED），对齐 DSH fiber 的"错误经 await"语义，同步与异步唤醒路径一致；01 的一处测试随语义升级改写。
- 依赖消失 → UNLOADING → 回 PENDING（非终态）：服务回归自动重启由 recheck 循环原地完成；换实现（remove v1 + provide v2）经历卸载-重启一轮，读到新实例。
- 03 记录的残留窗口（注销传导误停刚被新实例唤醒的依赖方）由 epoch 系统性化解：误停实为一次多余重启，最终一致。
- recheck 并发控制：状态推进串行（ReentrantLock + LOADING 态 dirty 标志兜底同线程传导）；apply 在锁外执行。dispose 可打断 LOADING（scope 由 dispose 侧回滚，apply 侧尊重已有迁移）。
- ocr 审查修复三项：scope 补 volatile（重建/置空跨线程可见性竞态，high）；inject() 改防御拷贝（防外部变更指纹输入）；epoch 指纹加类名双因子（单 identity 碰撞会漏判换实现）。
- 54 用例（12 闭环 + 18 事件 + 14 服务 + 10 生命周期）三连跑全绿。
