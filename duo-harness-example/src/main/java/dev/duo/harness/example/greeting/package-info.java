/**
 * greeting 示例域：服务提供者 / 消费者对——GreetingPlugin（provide +
 * config 绑定）、GreetingClientPlugin（inject + 视图寻址）、
 * GreetingService / GreetingView（契约与类型窗口）。
 */
package dev.duo.harness.example.greeting;
